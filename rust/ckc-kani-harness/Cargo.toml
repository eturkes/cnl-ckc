[package]
name = "ckc-kani-harness"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0 WITH LLVM-exception"
publish = false

[workspace]

[dependencies]
ckc-kernel = { path = "../ckc-kernel" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
