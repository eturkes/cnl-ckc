---
paths:
  - "rust/**"
---

# Rust + Verus law

- Toolchain (lost ⇒ repeat): `.toolchain/dl/<verus-asset>.zip` (weekly release, tag+digest = `rust/verus.lock`) unzipped → `.toolchain/verus-x86-linux/`; `.toolchain/{rustup,cargo}` = `RUSTUP_HOME`/`CARGO_HOME` (rustup `--default-toolchain none`, then `rustup toolchain install 1.97.1-x86_64-unknown-linux-gnu --profile minimal`). Every rust Bash call exports both homes + PATH prepend `$R/.toolchain/verus-x86-linux:$R/.toolchain/cargo/bin` beside the swipl/python shims. Kani 0.67.0 pinned (secondary gate).
- Gate = `cargo verus verify --workspace --locked --offline -- --no-cheating` (never `focus`; the flag = verifier-level rejection of assume/admit/external_body/assume_specification/assume_termination). A `&mut` reference inside a big loop invariant can kill Z3 outright — `expected rlimit-count in smt statistics` = solver death; remedy = per-iteration local + hide the expansion in the caller, never a bigger rlimit. A timeout/rlimit change = a proof change, never a retry.
- Verus results are content-hash cached — `touch` never re-verifies; force = `cargo clean -p <crate>` scoped to first-party crates ONLY: a cold target re-verifies vendored vstd, which cannot pass `--no-cheating` (it axiomatizes std) → warm up with one plain `cargo verus verify --workspace --locked --offline` first, then the gate form rides vstd's cache.
- Workspace: `ckc-spec` (trusted, MAIN-authored, human-read) + `ckc-kernel` (impl+proofs, UNINSPECTED — never read impl files; the verifier + gates = their whole review) + `ckc` (unverified std+sha2 shell). Committed `Cargo.lock` + `rust/vendor/` + `.cargo/config.toml` vendored-sources; deps grow only via `rust/trust/deps-allowlist.tsv` (no-default-features closures, no proc-macros in kernel).
- `ckc trust-audit`: scans workspace members only (never `vendor/`); 15 word-boundary + 5 raw tokens + whitespace-collapse surplus pass over 6 punctuation-joined tokens (surplus never allowlistable); comments count → member comments spell around token words; `rust/trust/escape-allowlist.tsv` multiset-exact BOTH directions; Cargo.lock package set == `deps-allowlist.tsv` exact; `spec-manifest.tsv` byte-pins the trusted surface (ckc-spec/src/*, contract.rs, both allowlists, verus.lock, rust-toolchain.toml, 4 Cargo.toml, Cargo.lock, .cargo/config.toml). Accepted residual = comment-split INCLUSION vectors only. Green meter `ckc: trust spec=<lines> shell=<files> assumes=<sites> deps=ok`. Allowlist+manifest regen after any trusted-surface edit = `python3 -P .scratch/m5u1/gen_trust.py <repo-root>` (needs built ckc binary; rerun-to-green = its check).
- Standing M5 differential harnesses (scratch-local, lead-authored; `.toolchain/bin` FIRST on PATH — lane A shells the swipl shim, without it every file reads `unwind(halt(0))` divergent): K1 `python3 -P .scratch/m5u2/diff/run_diff.py --rust-bin rust/target/release/ckc --lanes A,H`; K2 corpus `python3 -P .scratch/m5u2/diff/k2_corpus_diff.py [--lanes answer,recursion,aggregate]` (aggregate lane derives per-doc proof payloads through the legacy stage into `.scratch/m5u2/diff/k2state/`; answer manifest = pl path in both columns). M5.1 evidence replay: `python3 -P .scratch/m5u1/diff/run_diff.py`; suite `RUST_BIN=$R/rust/target/release/ckc python3 -P .scratch/m5u1/suite/runner.py`; trust battery `python3 -P .scratch/m5u1/trust-battery/run.py`; rev2 mutation replay = `.scratch/m5u1/rev2-mutation-evidence/`. M5.2 suite = `.scratch/m5u2/suite/` (case manifests + pinned reject bytes embed repo-root-relative paths → relocations re-derive path pins; machine check = old.replace == actual).
