# Project operations

- Toolchain (sole dev workstation): system swipl/python too new → gitignored `.toolchain/`: `swipl-9.2.9/` prefix built from `https://www.swi-prolog.org/download/stable/src/swipl-9.2.9.tar.gz` (`cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DINSTALL_DOCUMENTATION=OFF -DSWIPL_PACKAGES_JAVA=OFF -DSWIPL_PACKAGES_X=OFF -DSWIPL_PACKAGES_ODBC=OFF` + `ninja install`) + `bin/` shims `swipl`→that prefix, `python3`→uv cpython 3.13. EVERY Bash call: `export PATH="$ROOT/.toolchain/bin:$PATH"` (shell state resets per call); a PATH without the shims leaks system swipl into goal.py → `ace_to_pl_error(uncaught,unwind(halt(0)))` rc=2 = toolchain leak, not a corpus defect. Lost ⇒ repeat recipe. CI digest-pinned swipl:9.2.9 container = reference runtime; python floor = 3.11 (`-P` flag; hold to the 3.11 stdlib surface — pathlib `follow_symlinks` kwarg = 3.12+).
- Rust work adds (same call): `export RUSTUP_HOME=$R/.toolchain/rustup CARGO_HOME=$R/.toolchain/cargo; PATH="$R/.toolchain/verus-x86-linux:$R/.toolchain/cargo/bin:$PATH"`. Gate + workspace law → `.claude/rules/rust.md` (fires on `rust/**` touch).
- Deterministic SWI: always `-f none -F none`; writer options explicit.
- Standing decisive chain = `goal.py check` → `regen.py --check` → fresh `goal.py compile cdc-2022-opioid` → `git diff --quiet -- guidelines/`; rust units add `cargo verus verify … --no-cheating` + `ckc trust-audit` (`rust.md`). Full check >10 min + block-buffered → background to a log, read at completion. Close order = stage THEN decisive check (`git ls-files` reads the index; a pre-`git add` green can commit red).
- Two `/goal` vehicles: (1) phase runs = `Session flow` (body pasted by the user; state = `.agent/spec.md`); (2) the corpus loop — body + procedure = `docs/REFERENCE.md` § Operating; queue = `.agent/queue.md` (one source doc in progress at a time); round roles + close = `.claude/rules/corpus.md` (MAIN orchestrates + rules + commits; teammates absorb bulk; compiler/lexicon edits stay MAIN-authored). Keep `.claude/commands/` absent — a project command masks the built-in `goal`.
- Unit contracts (IMPLEMENT): `.agent/contracts/<unit>.md` = testable predicates + tier + gate identity + rulings, written before code, moved to `.agent/archive/contracts/` at unit close; review ledger = `.agent/review.md`. Legacy contracts: M5 = `archive/contracts/m5u*.md` (`.scratch/contracts/m5u*.md` = read-only symlinks serving frozen brief text); M3/M4 = scratch-only, pointers in the archive records. Milestone history: `git log --grep "(M<m>[. ]"` (compendium replan restarted numbering → topic sorts earlier-era hits).
- Authority map: simplified overview → root `README.md`; pipeline + audit story → `docs/REFERENCE.md` (holds the pre-split root README's technical content — archive/scratch refs to `README § …` resolve there); guideline provenance/rights/coverage → `guidelines/<id>/README.md`; ACE→Prolog compiler contract → `vendor/ape/prolog/ace_to_pl.pl` header; E-- language → `vendor/e--/docs/spec.md`; TCB + fork boundaries → `vendor/*/PROVENANCE`; clinician design law → `.claude/rules/clinician-design.md`; gates → `.github/workflows/ci.yml`; M5 plan of record → `.agent/archive/rust-rewrite-plan.md` (read at unit start); language-boundary charter → `.agent/spec.md` Decisions (history: `archive/standing-instructions.md`).
- Fork edits: a modified upstream file needs a prominent in-file notice — Apache-2.0 §4(b) (`vendor/e--`), GPL-3.0 §5(a) via LGPL (`vendor/ape`; notice + ISO date, write-once = first fork-edit date). `goal.py check` `fork-notice` enforces per `vendor/<tree>/PROVENANCE`; an unmodified file carrying a notice = a violation; CI needs `fetch-depth: 0`.
- Scratch/probe work → gitignored `.scratch/`; standalone git ops there set `GIT_CEILING_DIRECTORIES="$ROOT/.scratch"`; teammate worktrees = `.scratch/worktrees/<name>`, reports `.scratch/agents/<name>.md`, roster `.scratch/agents/roster.md`.
- WebSearch safety layer false-positives on medical vocabulary → phrase queries by tool/org names only.
- Presentation rule (user): durable files read as this project's own findings — reference only this project's own paths, commits + findings.
