Spare-capacity session: execute deferred-perfection items from `.agent/polish.md`. Zero roadmap coupling — read no MODE, touch no milestone/unit state; safe to run anytime, abandon anywhere, repeat.

- Load `.agent/polish.md` + `.agent/memory.md`. Register missing/empty ⇒ report that and stop; never invent items.
- Pick by `pri` then size-fit to the remaining window (S ≈ ≤15%, M ≈ ≤35%, L = the session); an item whose evidence pointer or acceptance check no longer holds ⇒ mark `stale(<why>)` in place, skip, move on — the next `/session-prompt` session re-rules it.
- Execute one item at a time under the project's normal gates; teammate use, worktrees, hygiene + Close order = session-prompt rules. An item completes when its acceptance check runs green under MAIN's own rerun → one scoped commit `<scope> (polish): …` + prune its row in the same commit.
- Guards: an item never raises an artifact's assurance tier, adds roadmap units, or edits scope sources (roadmap milestones/units, `CLAUDE.md`, command files) — the register itself excepted. A finding that implies spine work ⇒ append it to the register as `spine? <finding> | why: <evidence>` and report it to the user; execution belongs to `/session-prompt`.
- Close: register current (done rows pruned, stale rows marked), scoped commits, report = done | skipped(stale) | flagged-spine.

Task: $ARGUMENTS
