# polish register

Deferred-perfection items. Any session appends at deferral time — acceptance check written while the evidence is fresh; `/session-polish` alone executes. Row: `- [ ] <artifact>: <upgrade> | why: <evidence pointer> | accept: <check> | size: S|M|L | pri: 1-3`. Done rows are pruned in the completing commit.

- [ ] ace_to_pl existential facts (`There is ...`): admit only on a named source sentence | why: M3 plan draft polish | accept: named source case + total translation + red boundary | size: M | pri: 3
- [ ] ace_to_pl formula/arithmetic DRS beyond source-shaped cardinality | why: M3 plan draft polish | accept: named source case + engine-portable documented semantics | size: M | pri: 3
- [ ] ace_to_pl ditransitive/passive/phrasal coverage beyond corpus need | why: M3 plan draft polish | accept: named corpus sentence + positive/red/oracle fixtures | size: M | pri: 3
- [ ] port CDC scratch ledger pins to generic declared-key validator when a second corpus exists | why: M3 plan draft polish; multilingual invariant | accept: no corpus paths/hashes/English atoms in tools/ or schema; byte-equivalent verdicts | size: M | pri: 2
- [ ] property/fuzz campaign across 27 ulex categories + nested DRS operators | why: M3 plan draft polish | accept: committed generator + replayable divergence corpus; accepted grammar unchanged absent source need | size: L | pri: 3
- [ ] tests/: port committed suites — m3u2 73-case + m3u3 64-case + m3u7 96-case suites + drivers into tests/ with one repo-root runner | why: committed gate alone kills 4/35 rev2 M3.7 mutants; exact-detail + validator-boundary kills live only in scratch suites (kill table .scratch/agents/rev2-m3u7.md) | accept: suites rerun green from a fresh clone via one documented command; every rev2 KILL-GAP(C) row kills via a committed case | size: XL — exceeds one polish session, decomposition belongs to `/session-roadmap` | pri: 2 | note: two constraints measured at pick time. (1) `regen.py` `unauthorized_python_paths` admits a tracked `*.py` only under `vendor/e--/src/` or as the generated twin of a `tools/*.emm`, so a committed runner is strict E-- (no comments, indexing, postfix chains) — the 747-line m3u3 harness (dicts, closures + `nonlocal`, regex, tempdirs, hardlink trees, `git init`, argparse) is a rewrite, not a port, and `check_prolog_inventory` bars tracked `.pl` fixtures outside `vendor/ape/` + `guidelines/*/pl/`. (2) The kill set is wider than the three suites: 28 of 35 mutants need scratch evidence whose killers include `.scratch/m3u7/rev2/cases/` (Q-S2-*, CHK-*, LEDGER-*, HEADER-*, PROD-*, LEX-OR-LAST, SELECTOR-DISCRIM, MANIFEST-TOTAL, ALL-CORPORA) and the `S:diff` 100-case differential with its independent oracle, so porting the 233 suite cases alone leaves the acceptance unmet
- [ ] ace_to_pl aggregate replay: detect a dropped LAST variant of a multi-variant sentence | why: M3 review U3-F1 fix closes empty/truncated/duplicated/misattributed payloads, but coverage stays complete and variant numbering contiguous when the final variant is dropped; rule-head `Deps` are variables, so per-sentence variant counts are not derivable from the frozen v1 product (`.scratch/m3rev/payload_battery.py` case `dropped-variant`) | accept: a per-sentence variant count reaches the replay (schema v2 indicator or manifest column), the dropped-last-variant battery case fails rc1, and every other battery case keeps its current verdict | size: M | pri: 2
- [ ] ace_to_pl object-operator closed set: no executable kill for a widened cardinality vocabulary | why: M3 review U1-F-07 — corpus exercises only `eq,geq`, so both widening and narrowing mutants of the closed set leave `gate_m3u1.py` and `goal.py check` green (`.scratch/m3rev/redtests/test_object_operator_gate.py`) | accept: a banked synthetic `object(...,approximately,...)` DRS seam rejects with `unsupported, object_operator(approximately)` through the live projector, and a closed-set mutation fails the gate | size: M | pri: 2
- [ ] ace_to_pl root-NAF precedence: `v1_naf_items/10` and the root tagger disagree on which detail a root-level NAF earns | why: M3 review U2-F-01 — internal inconsistency with no CLI divergence today (both paths reach `forbidden_operator(root,naf,S)` for every reachable ACE), latent under refactor (`.scratch/m3rev/redtests/test_mrev_m3u2_root_naf_precedence.py`) | accept: one rule owns the root-NAF verdict, a direct-helper test pins it, and the shipped ACE controls keep their current details | size: S | pri: 3
- [ ] cdc lexicon proposition-compound splits: decompose the rev-w1-1 med-set nouns (s39-01 taper schedules 12/17 tokens, s38-04 outcome propositions, s9-05 duration classes, s23-10/s23-13/s23-14 risk compounds, s18-07 policy compounds, s19-07 access pair) + low set into entity+property/relation encodings; restore source numerics as cardinalities (s9-05 <1/1–3/>3 months, s10-04 <18 years, s39-01 10%/30%) | why: rev-w1-1 wave-wide lexicon table (.scratch/goalrounds/ace-wave1/rev-w1-1.md) — compounds internalize quantities/alternatives/possibility the compiled product cannot query | accept: named compounds gone from lexicon.ulex, replacement rules carry explicit guideline_cardinality rows for the numeric bounds, compile+check green, notes rows updated | size: L | pri: 2
- [ ] goal.py check_lexicon per-surface liveness: flag lexicon entries whose exact surface form no ACE document uses (current pass marks a lemma live when any sibling surface occurs) | why: rev-w1-1 — tv_infpl(indicate) passed while only finite `indicates` occurs | accept: gate red on an injected unused-surface entry, green on the cleaned corpus | size: S | pri: 3
- [ ] goal/regen vendor-inventory closure + first-party headers: reject tracked vendor code outside (import-commit tree ∪ PROVENANCE `First-party files:`) and verify each declared first-party file carries its origin/license header | why: charter audit ALIGN-02/-07 (`.scratch/agents/rev-align-1.md`) — whole-prefix allowlists (`vendor/e--/src/**` py, `vendor/ape/**` pl) are fail-open to undeclared TCB growth; strict.py shipped headerless until the alignment commit; battery prewritten at `.scratch/polish-vendorclosure/fixtures.py` (9 cases; pinned details `tracked vendored file outside import tree:` / `first-party file states no Copyright:` / `first-party file states no license id <id>:`; metadata pair PROVENANCE+MANIFEST.sha256 header-exempt; live preflight: tracked−import = exactly the declared first-party set in both non-pristine trees; update forknotice fixture `undeclared-first-party-addition-fails` to the closure detail) | accept: synthetic undeclared `vendor/e--/src/**/*.py` + `vendor/ape/**/*.pl` each fail `goal.py check` with a pinned detail; removing a first-party header marker fails; clean tree green; battery beside `.scratch/polish-forknotice/fixtures.py` pattern | size: M | pri: 2 | resolved: M4.5 chose modes-in-file; row stays deferred
- [ ] .agent/queue.md row diet: id | url | lifecycle | next-item only; docid conventions, wave history + dispatch-kit state move to their owners (guideline README, worklist, rounds roster) | why: charter audit ALIGN-15 — the in-progress row inlines modeling rules + kit inventory | accept: in-progress row ≤ 3 lines; every relocated rule findable at its named owner; next /goal round consumes the queue unchanged | size: S | pri: 3
- [ ] tests/red reject-surface coverage manifest: join a source-scanned census of ace_to_pl reject sites/detail families to owning red probes | why: survey audit — 19 committed red probes vs 49 `reject/2` call sites; probe selection is manual, coverage unproven (.scratch/ckc-survey/cnl.md A1) | accept: committed manifest maps every reachable reject family to ≥1 probe; a new unowned reject site fails check; census reproducible | size: M | pri: 2
- [ ] goal.py bounded subprocess wrapper: wall-clock bound every swipl invocation (stage build, compile, load, aggregate, recursion, red probes); timeout = pinned fail-closed detail + `.goal.tmp.*` cleanup | why: survey audit — proof search is depth/inference-bounded but the six subprocess calls have no process bound (.scratch/ckc-survey/rust.md A-01) | accept: sleeper + descendant-held-pipe fixtures yield the pinned timeout detail with scratch removed; clean run green | size: M | pri: 2 | M4: new derive/answer invocations ship wall-clock-bounded from birth (M4.6+), M6 graph likewise; this row keeps the six existing-call retrofits
- [ ] goal.py source-custody rows: machine-validate guideline source bytes + rights against strict rows (README prose stays the human view) | why: survey audit — check_source_record validates existence/type only; live spot-check 5/5 CDC SHA-256 match, so this is gate debt not data debt (.scratch/ckc-survey/spec.md A01) | accept: tampered source byte fails with pinned detail; clean corpus green | size: M | pri: 2 | M7: machine-validated rights/custody rows also gate M7 dist eligibility — land there or here first, one implementation
- [ ] ace_to_pl renderer adversarial probes: ambient operator declarations, escapes, non-ASCII, rational text, read-back round-trip | why: survey audit — writer options pinned + double-compile byte-compared, but no focused adversarial serializer regressions (.scratch/ckc-survey/cnl.md C6) | accept: probes green live, red under a writer-option mutation | size: S | pri: 3 | M4.7 shipped so; the M6 graph serializer likewise; this row keeps the existing-writer retrofit
- [ ] lexicon accretion memory: reject-ledger for refused vocabulary candidates | why: survey audit — candidate decisions have no durable memory (.scratch/ckc-survey/spec.md A04) | accept: ledger consumed by /goal rounds | size: S | pri: 3 | note: append-stability half landed in M4.2 — semantic_clause_sha256 excludes comments/directives/document-record, so a pure ulex append stales nothing; adjudication staleness gate + A-series battery carry the acceptance
- [ ] consumer release manifest: one content-addressed attestation binding source rows + ledgers + ACE + ulex + PL + compiler closure/SWI identity + proof-set identity + replay commands | why: survey audit — the assurances exist separately; a copied artifact names only ace_to_pl + ACE/ulex digests, and consumers get no single verifiable binding (.scratch/ckc-survey/spec.md A06, rust.md A-03) | accept: goal.py check regenerates it byte-identically; documented replay command verifies from a bare artifact copy | size: M | pri: 3 | M7: pulled into the M7 dist unit (both acceptance clauses carried verbatim; prune at M7 close)
- [ ] port M4.1 strict-gate batteries to committed state | why: DR8 negative-state coverage (gatecheck 21 synthetic-worktree cases + 23 detector rows), behavior battery (4 runtime cases), generator identity + red-first replay live only as `.scratch/m4u1/battery/` copies + memory recipes; the committed gate itself carries none of the gate-weakening coverage | accept: committed script(s) rerun from a clean checkout reproduce gatecheck 21/21 + detector 23/23 + behavior 4/4 with `.scratch/` absent; entry point named in CI or goal.py check | size: S | pri: 3
- [ ] goal.py census-map gate: validate audit/census-map.tsv like projection-notes (header bytes, row shape, docid/region join to coverage) | why: M4.2 map S4 — committed but ungated; drift invisible to check | accept: tampered census-map row fails check with pinned detail; clean corpus green | size: S | pri: 3
- [ ] port M4.2 adjudication batteries to committed state | why: mutation 30/30, determinism 10/10, append-stability A01-A04, independent D1-D3 oracle 186/186 + gatecheck negatives live only as `.scratch/m4u2/` copies + memory recipes; the committed gate carries the 46-case fixture differential alone | accept: committed script(s) rerun from a clean checkout reproduce mutation 30/30 + determinism 10/10 + append A01-A04 + oracle 186/186 with `.scratch/` absent; entry point named in CI or goal.py check | size: M | pri: 3
- goal.py `ledger-validate` dies from foreign cwd (module-level `compiler_source` require at goal.py:2320 resolves `vendor/ape/...` against cwd before subcommand dispatch). Move the require into the compile/check paths or anchor to script dir. Acceptance: `cd /tmp && python3 -P <abs>/tools/goal.py ledger-validate <valid ledger> <valid manifest> x` exits without traceback; ui.py keeps its cwd= pin regardless.
- [ ] shared docid/gid grammar max-length: hoist the RV12 250-byte bound (`.html` headroom on NAME_MAX=255) from ui.py into the goal grammar so both tools cite one owner | why: M4.3 RV12 — ui enforces now, goal accepts longer ids that ui then rejects | accept: goal.py check red on a 251-byte docid, ui red fixtures unchanged, bound stated once | size: S | pri: 3
- [ ] port M4.3 UI batteries to committed state | why: mutation 45/53 committed-killed + 6 RV14 scratch-only kills (M24 M30 M31 M36 M47 M52), determinism D00-D22, fixture-generator identity live only as `.scratch/m4u3/gen/` + `.scratch/m4u3/rev2/` + `.scratch/agents/rev2-m4u3.md` recipes; committed gate carries the 63r/11g fixture differential alone | accept: committed script(s) rerun from a clean checkout reproduce mutation + determinism + generator identity with `.scratch/` absent; entry point named in CI or goal.py check | size: M | pri: 3
- [ ] GET Host-check DNS-rebinding hardening | why: only the POST path validates Host; GET review/doc pages remain readable by rebound origins | accept: GET refuses foreign Host with the POST path's law; fixtures updated | size: S | pri: 3
- [ ] port M4.4 write-path batteries to committed state | why: scratch battery 16 probes + rev-m4u4-red.py 4 acceptance probes + rev2 90-row campaign live only in `.scratch` | accept: committed script reruns from a clean checkout with `.scratch` absent; entry point named in CI or goal.py check | size: M | pri: 3
- [ ] goal_py_path argv[0]→__file__ anchor | why: embedded import (WSGI host) resolves the validator from the importer's argv | accept: exec-embedded ui.py resolves goal.py without an argv pin (battery load_ui drops the pin) | size: S | pri: 3
- [ ] pin ui-fixture meter counts inside check (75 red / 13 green as of M4.13) | why: rev2 M63 class — deleting a case dir plus its red_required name silently weakens the gate; a count pin makes list edits deliberate | accept: dropping any case or list name reds the check; case additions update the pin consciously | size: S | pri: 3
- [ ] retire `.scratch/repair-fixture-manifests.sh` | why: manifest-parity gate + AM6 regen supersede the repair path | accept: memory recipe pruned; parity gate documented as sole owner | size: S | pri: 4
- [ ] lexicon liveness × query ACE: rule whether `guidelines/<id>/queries/*.ace` count as liveness reference sources (query-only lexeme live) or document-only law stays with a red fixture pinning query-only lexemes dead | why: M4.6 T53 deferral; probe seed `.scratch/m4u6/polish-t53/` (lexeme glorps) — under current law queries-check yields non-demo `solutions([])` and the per-guideline lexicon gate never scans query trees | accept: ruling encoded as committed fixture (red or green per ruling); full check green | size: S | pri: 3
- [ ] pin queries-fixture meter counts inside check (counts as of M4.6 close) | why: rev-m4u6 phase-2 — the ≥1 red + ≥1 green floor accepts silent case deletion; mirrors the ui-fixture pin row | accept: dropping any case or pin-list name reds the check; case additions update the pin consciously | size: S | pri: 3
- [ ] port G08/G09 mutation-kill seams into the committed check: alternating fake-swipl shim pins `goal: determinism: two query compiles differ for question: <qid>` (+ answer-run cell); two-qid writer seam with later-qid failure asserts zero written paths | why: M4.6 rev2 campaign — 24 mutants, 19 killed committed + 3 by suite harvest; G08 (second-run reuse) + G09 (eager per-qid writes) survive every committed fixture because both need a compiler seam; executed kill evidence = report `# phase 2 (rev2-m4u6-2)` F23/F24 rows in `.scratch/agents/rev2-m4u6.md` (exact seam mechanics + pinned lines; scripts swept by teammate cleanup — re-derive from report); test-m4u6 T50 harness self-probe shapes = same seam class, excluded from committed fixtures by ruling | accept: G08 + G09 mutants red the committed check from a clean checkout; clean tree green | size: M | pri: 2
- [ ] port M4.7 queries suite to committed E-- harness | why: regen law (all first-party tracked .py = E---generated) keeps the 332-check battery + corpus generator scratch-local (.scratch/m4u7/suite/, tag archive/m4u7-suite); committed coverage = fixture corpus + goal.py check only | accept: suite committed as .emm + regenerated .py, `regen --check` green, ported runner `target ok cases=80 checks=332`, generator rebuilds tests/queries tree-hash-identical from clean checkout | size: L | pri: 3
- [ ] kill rev2-m4u7 survivor recipes M41/M42/M48-M50 in the queries suite | why: mutation campaign (50 rows, 41 killed; ledger .scratch/agents/rev2-m4u7.md) left 5 real gaps — instantiated-clause digest (ace_to_pl.pl:1317-1357), live NAF payload vs success-time copy (:1296), Unicode-widened scanner gates (goal.py digit/atom-entry/atom-continuation; v1.3.7 law probe-verified only); contract v1.3.9 ruling | accept: suite gains multi-value variable-rule digest case + later-bound shared-NAF-variable case + non-ASCII scanner attacks; each of the 5 recipes applied → runner target rc1; clean tree green | size: M | pri: 3
- [ ] narrow trace digest-join inventory to the seven schema predicate heads | why: rev-m4u7-2 late finding, ruled residue (contract v1.3.9) — trace_block_table (goal.py trace join) classifies by exclusion (%, :- , schema-version, document record, final dot), so a foreign .-terminated non-schema line in a committed pl/ file is joinable; no end-to-end false accept today (live corpus = compile freshness, fixture trees = golden bytes + generator idempotence; suite M44 comment-decoy kills the widening direction) | accept: allowlist lands with a red case where a trace node joins a non-schema clause line (must reject `trace node resolves to no committed clause line`); queries fixtures + suite target green | size: S | pri: 3
- [ ] durable contrast + focus invariant in `ui.py` selftest | why: S4 ruling (M4.13) — committed selftests cover escaping + palette membership/count only; contrast evidence is the scratch oracle `.scratch/m4u3/contrast.py` (7/7) and focus coverage is golden-byte-only, so a low-contrast palette edit or a deleted `:focus-visible` selector ships green | accept: mutating a palette pair below the project threshold, or deleting any focus-visible selector, reds `ui.py check` from committed state; clean tree green | size: M | pri: 2
- [ ] close rev2 copy-register vocabulary survivors (M04-M06, M08-M10, M12-M17, M19) | why: one red per register leaves list erosion, morphology narrowing, case folding, range endpoints and multi-hit ordering unwitnessed (rev2 M4.13 campaign, 26 survivors) | accept: red fixtures cover both emoji range endpoints, U+FE0F/U+200D/regional indicators, every CSS + marketing + relative-time alternative and morphology, mixed-case variants, and one multi-line multi-register `.expect`; birth counts re-pinned; `.scratch/m4u13-evidence/rev2/survivor_battery.py` rows green with each mutant red | size: M | pri: 3
- [ ] UI/copy fixture-harness self-coverage (rev2 M47-M50) | why: `ui_compare_golden` can be bypassed, a required case can be counted without running, the birth-count guard is redundant, and no committed pin fixes the two copy meter strings | accept: gatecheck cases red a one-byte golden drift, a forged required-name entry, an extra valid red case, and each meter-shape change | size: M | pri: 3
- [ ] L2 sweep negative for review pages (rev2 M35) | why: no committed red has its sole violation on a review page, so skipping `/review/` members of the page sweep survives every gate | accept: red fixture tree whose recorded reviewer/comment payload violates C6 only on the review page; `ui.py check` rc1 names that page | size: S | pri: 3
- [ ] committed twins for the residual committed-corpus cases (M4.13 v5.6) | why: the two `worktree/` git fixtures cover dirty-ACE-shows-committed-text, uncommitted-decision-stays-current, untracked-doc-unlisted and POST-records-HEAD-commit; `.scratch/m4u13-committed-corpus.py` still solely carries a (clean git repo renders byte-identically to a plain tree), e (the server's own ledger write is not dirt — now a white-box assertion on `uncommitted_guidelines`, because no page states committed state), h (a corpus below the git toplevel reads its own files) and i (no snapshot directory leaks) | accept: each of a/e/h/i reds the committed check from a clean checkout when its rule is broken; battery retired or reduced to a regeneration oracle | size: S | pri: 3 | note: e and i need a surface the harness does not have — mode + temp-dir assertions rather than page bytes; h needs a case whose corpus root is a subdirectory of the materialized repository | size-driver: harness, not fixtures
