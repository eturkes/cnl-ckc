# M5.7 — cutover + retirement + scrub (contract)

Unit = the Rust chain becomes the sole authority: CI swap, legacy deletion,
fork shrink (R95), reference scrub, NOTICE re-derivation, release-manifest
regen. Plan of record = `.agent/archive/rust-rewrite-plan.md` § M5.7/M5.8.

## Tier

- shell/docs: no new kernel code. `docs` consistency pass on every prose
  surface; `data` re-pins enumerated (FC2/FC4 rows below); the decisive
  chain = `just gate` = `just rust` + `just check` (native `ckc check`,
  fresh `ckc compile`/`ckc queries` per guideline, `git diff --quiet --
  guidelines/`, fresh `ckc release-manifest`) + `just certify`.

## Predicates (acceptance)

- P1 CI: `.github/workflows/ci.yml` `check` job = the native chain in the
  pinned swipl container over the `rust` job's binary (bootstrap cmp,
  `regen.py --check`, `goal.py check` dropped); `rust`, `certify`,
  `advisories`, `kani` jobs unchanged in shape; Dependabot unchanged.
- P2 deletion: `vendor/e--/`, `tools/` (4 `.emm` + 4 `.py`),
  `tests/strict/`, `tests/dist/red.sh` + every other `red.sh` gone;
  `.gitattributes` `*.emm` line gone; `.claude/rules/legacy-tools.md`
  retired; `just legacy` recipe replaced by `just check`.
- P3 fork shrink (R95): `vendor/ape/prolog/ace_to_pl.pl` keeps
  compile/proof/question modes only (check/aggregate/recursion/answer/
  trace removed); header + REFERENCE compile contract + PROVENANCE
  updated; fork-notice date law kept; measured line reduction recorded;
  `.scratch/m5u2/diff` K1/K2/K3 lanes replayed once against the pre-shrink
  fork and recorded in the commit body (last differential evidence).
- P4 scrub: `git grep -iE 'e--|\.emm'` empty outside git history and
  `.agent/archive/`; README.md + docs/REFERENCE.md (§ Audit story: E-- →
  Rust verified-kernel section; § Running/Operating/Export invocations →
  `ckc`), `.claude/rules/*.md`, `.agent/spec.md` Artifacts current.
- P5 NOTICE: vendor/e-- (Apache-2.0) leaves; the GPLv3 combination
  statement re-derived; release-manifest regenerated (FC4 with M5.6).
- P6 gates: `just gate` green on the closing tree; corpus byte-stable;
  `ckc certify` green; tests/* fixtures unchanged bar the enumerated
  re-pins (meta replay lines name `ckc` commands, R72).

## Gate identities

`just gate` (new shape) + CI; scrub census = `git grep -iE 'e--|\.emm'`
(expected 0 outside `.agent/archive/`).

## Rulings

- R95 (m5u4.md) binds: fork shrink lands here, after the last
  differential replay is recorded.
- Prose surfaces (README, REFERENCE, NOTICE, rules) = MAIN-authored;
  mechanical deletion/CI/scrub census = delegable (`prod-m5u7`).
