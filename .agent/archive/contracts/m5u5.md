# M5.5 — reviewer interface (K5) (contract)

Unit = `ckc ui serve [<port>] | render <out> | check | request <method> <path>`
replacing `tools/ui.py` (map-4 R4 = the byte-parity target: routes, POST law,
render outputs, committed-corpus reading). Behavioral reference = the legacy
pages + `tests/ui` (84 red + 15 green); design law = `.claude/rules/
clinician-design.md`; public law = `docs/REFERENCE.md` § Reviewer interface.

## Tier

- kernel (`ckc-spec/src/ui.rs`, MAIN-authored): viewmodel from the committed
  corpus (K1/K4 bindings supply documents, coverage rows, bundles, ledger
  classes); HTML render = a pure function of the viewmodel (theorems: every
  visible text byte comes from the corpus, the copy registry or a digit
  string; HTML escaping total — `<`/`>`/`&`/`"`/`'` never appear unescaped
  in text/attribute positions; determinism = purity); copy registry = const
  data with the banned-token gate as a spec predicate; POST guard chain
  (Host, Origin, content type, six exact fields, verdict/reviewer/comment
  grammars, token, docid, subject digest = the fresh review bundle digest,
  ledger digest CAS) as an ordered first-refusal law; ledger candidate =
  seven-column row insertion by (docid, date) preserving older rows, byte
  exact; the candidate must satisfy `check.rs` `ledger` (+ classification).
- shell (enumerated, fixture-gated): loopback socket, HTTP request-line/
  header parsing subset (request text → method/path/headers/body given to
  the kernel), per-process random token, `git archive HEAD guidelines` intake
  (committed reading, live ledger overlay), mkstemp/fsync/flock/rename, CSP
  hash of the one fixed script, response headers (H5), render directory
  writes, `ui check` double-render byte compare (retained as a runtime
  probe beside the purity theorem), exit codes.

## Predicates (acceptance)

- P1 `just verify` green with `ui.rs` items bound (`render_page`,
  `post_outcome`, `ledger_candidate`, `copy_ok`); no new escape sites.
- P2 `just trust` green (manifest += `ui.rs`).
- P3 render parity: `ckc ui render <out>` vs `python3 -P tools/ui.py render
  <out>` on HEAD — every emitted file byte-identical (index, per-guideline
  index, records, every document page, copied source assets).
- P4 tests/ui replay: 84 red + 15 green case.tsv streams (argv/rc/stderr/
  stdout, golden trees, after/ + absent-globs) green through the Rust
  harness with fixture bytes unchanged bar ruled FC2 (`python3 -P
  tools/ui.py` argv strings → enumerated re-pins).
- P5 POST protocol: the guard order, refusal bodies/status codes (400/403/
  404/405/409/303), CAS + atomic ledger write, refusal writes nothing —
  verbatim vs legacy on the live request battery (`ui.py request` pairs).
- P6 design gates: banned-token scan over the copy registry (emoji, CSS
  gradients/animation, marketing/exclamatory, relative time, invented
  acronyms) as a `ckc` test + `ui check` page invariants (CSP hash, one
  script, palette, no external refs, sentence case headings).
- P7 `just rust` + `just legacy` green; corpus byte-stable.

## Gate identities

`just rust`; render parity + request battery = `.scratch/m5u5/parity.py`
(diff-blind test teammate from this contract); tests/ui replay = the Rust
harness `rust/ckc/tests/ui_fixtures.rs` over the tracked trees.

## Rulings

- R69 committed reading stays law: pages render HEAD (`git archive`), live
  ledgers overlay; the stale `.claude/rules/ui.md` dirt-probe prose retires
  with `ui.md` (register).
- R70 escaping + copy theorems replace the legacy AST string scan + runtime
  double-render as the fidelity claim; the runtime double-render stays as a
  shell probe (belt and braces, no theorem weight).
- R73 escaping law = legacy `html.escape`: text nodes escape `&` `<` `>`
  only (`quote=False`), attribute values escape all five (`quote=True`);
  `well_escaped` reads per position; byte parity governs the theorem's
  shape. The ≤~1.2K-line target yields to fidelity (alignment tokenization,
  palette/CSS/script bytes are render); the M5 spec audit adjudicates size.
- R76 (revised) copy law domain: the registry's code points are ASCII plus
  an ENUMERATED non-ASCII punctuation repertoire present in the emitted copy
  (em dash U+2014, middle dot U+00B7, `≥` U+2265, `§` U+00A7, … — the exact
  list transcribed from ui.py), all non-word for `\\b`; the banned-token
  predicates run in legacy order over that classification (ASCII word
  boundaries + repertoire = non-word) = exact Python `(?i)\\b` on the
  domain; the transcribed emoji intervals fire the legacy emoji violation
  (the red emoji fixture stays byte-exact); any other non-ASCII code point
  fails closed with a named domain violation (non-ASCII letters never occur
  in the registry or the 12 copy fixtures); no Unicode word table, no shell
  primitive; invented-acronym judgment stays an unmechanized design rule.
- R84 ui.rs draft (wt/spec-m5u5 40881470, 2859 lines) read in full by MAIN
  → accepted verbatim as the trusted spec. Substance of the escaping
  theorem = `escaped_slots`' context automaton (text slots only in context
  0, attribute slots only inside quoted attributes, comments only inside
  `<!-- -->`, fixed pieces registry-bound) + `visible_bytes_from` (text
  slots = copied corpus spans | registry literals | canonical decimals);
  the `escaped(escape_text(b), false)` conjunct restates a lemma, kept.
  POST guard order = `post_outcome`'s: Host → method/405 → models/500 →
  GET read → route/404 → Origin/403 → content type/400 → body/400 →
  fields/400 → csrf/403 → render_error/500 → fresh manifest/500 →
  subject/409 → ledger CAS/409 → candidate validity/500 → 303; P5's live
  battery grades it against legacy, a divergence = spec edit ruled then.
  M5-review register rows: `version_link` embeds the repository commit URL
  as copy; a request without Origin passes the Origin guard (token still
  guards); `records()` pairs decisions with raw rows by index (sound under
  K4 ledger validity, which `prepare_candidate` re-checks); `ledger_
  candidate` drops non-header comment lines. Size 2859 vs ≤~1.2K → R73.
- R87 (prod-m5u5-1 finding, ruled): `guideline_html` emits `text(r.id)`
  per non-ACE coverage row while `corpus_bytes` listed only `r.line` →
  `visible_bytes_from` was unprovable for an unconstrained `Row`; fix =
  `corpus_bytes` enumerates each coverage row's `id` beside its `line`
  (both are coverage-ledger bytes); theorems stay universal, no renderer
  precondition.
- R88 (test-m5u5-1, ruled) P6 gates = legacy's law: `<script>` count per
  page ∈ {0, 1} and, when present, the canonical script bytes + its CSP
  hash; external references banned except hrefs under the commit-link
  base (`version_link`); pinned from legacy pages. Copy fixtures replay
  through `ckc ui copy-check <source.txt>` reading the legacy fixture
  format (lines `NAME = "<literal>"`, double-quoted, escapes `\\ \" \n \t
  \xNN \uNNNN \UNNNNNNNN`), printing `<line> <violation>` per violating
  line in file order: red = rc 1 + stdout == expect.txt, green = rc 0 +
  empty stdout, stderr empty; fixture bytes unchanged. `ckc check` (M5.3)
  carries no copy section: legacy `check_copy_register` scans ui.py's own
  source and retires with it at M5.7; the port's copy gate = the
  `ui_copy_ok` theorem + this battery.
- R89 (prod-m5u5-1, ruled) P1 amended: ONE enumerated `unsafe` site is
  admitted in the K5 kernel — `str::from_utf8_unchecked` called only
  after a verified total validator whose ensures is vstd's `valid_utf8`
  (the pinned vstd specifies exactly that precondition); the site joins
  `rust/trust/escape-allowlist.tsv` with a comment naming the
  precondition; no other unsafe, assume, admit or external_body. Safe
  `str::from_utf8`/`char::from_u32` carry no pinned spec, so the bridge
  is the smaller trust step.
- R92 (mirror dedup, ruled at M5.3 integration): `check.rs` (wt/prod-m5u3,
  read + accepted) carries the K4 exec mirrors `EFileSrc`, `EVerdict`,
  `ERendered`, `EStatus`, `ECoverageRow`, `EEvidence` (census = decimal
  bytes), `ECoverage`, `EBundle`, `EDecision` + view helpers; `ui.rs`
  drops its duplicates (`EStatus`, `ECoverageRow`, `EEvidence`,
  `ECoverage`, `EReviewBundle`) and imports `check::{EStatus,
  ECoverageRow, EEvidence, ECoverage, EBundle}` (`EDocument.bundle`,
  `EPostGuideline.fresh` typed on `EBundle`); MAIN edits ui.rs on main
  once M5.3 lands; the K5 prover imports K4 (k4_*.rs + check.rs) from
  wt/prod-m5u3 dd0a837f meanwhile and codes against `check::E*`.
  Register note: `check::optional_bytes` ≈ `emit::opt_view` (M5 spec
  audit R-01).
- R96 (prod-m5u5s-1 intake question, ruled): the shell's committed-corpus
  intake mirrors legacy `ui.py`'s lightweight coverage/manifest envelope
  (read/TSV/UTF-8/id checks in legacy order — P3/P5 parity governs) and
  builds the `check::ECoverage`/`EBundle` mirrors itself; the K4 bindings
  compose where legacy validated (`check_payload`, `align_check`,
  `check_ledger`, `check_bundle_block`, `check_semantic_input`; strict
  `check_coverage` for the fresh POST derivation). Intake is shell tier
  (Intent: git/filesystem = trusted software under fixture gates); the
  fidelity theorems bind the bytes the shell supplies. No new binding.
