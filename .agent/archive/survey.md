# Predecessor-repo survey distillate

Predecessor-repo survey register = `.scratch/ckc-survey/` (roster + hist/spec/cnl/arch/rust reports; exact refs/SHAs inside): 13 polish rows mined from it; plan-input material stays in the reports for future planning sessions — Japanese frontend laws (closed source-language CNL over a validated/serialized DRS seam, typed lexicon view consumed by grammar+tokenizer+parser+validator+renderer+bridge, longest-match tokenizer + pairwise prefix-freedom audit, escape-as-proposal that parses but never compiles, comparator surfaces 以上/以下/未満/超 → geq/leq/less/greater, non-ACE provenance = explicit schema v2 selector since v1 embeds `ace_sha256`/`ulex`); M2 permission-aware artifact profiles for rights-gated corpora (redistributable|reconstructable|restricted + allowed-artifact classes vs the commit-every-source invariant — explicit product-policy decision, never silent weakening); defeasibility seeds (NAF-vs-courteous divergence truth table, AceRules seam facts: reparses raw ACE + drops sentence/token provenance → not a drop-in oracle; second adoption bar = measured corpus underfit beyond the named consumer question); exact-rational interval algebra + symbolic conflict-pair kernels (port behind adapters only after a consumer defines unit/dimension/opposition semantics); evaluation contracts (source faithfulness ≠ proof discharge; oracle fixed at corpus authoring; predecessor route priors: verdict accuracy masks semantic loss, stability counts only conditioned on correctness); per-resource/version terminology licensing rows before any JA acquisition or mining.
