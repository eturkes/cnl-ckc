# roadmap

Charter = `.agent/initial-prompt.md`. Pipeline: E-- → Python (glue only) | ACE → APE-fork DRS → project IR → Prolog (all clinical semantics). Derived artifacts byte-accepted; corrections via regeneration only.

## Ledger

| id | milestone | deliverable | gate | status |
|---|---|---|---|---|
| M0 | feasibility + governance | upstream pins/licenses provenance doc; APE-on-SWI-9.2.9 probe; /goal persistence contract; Clex decision | — | REVIEWED |
| M1 | E-- trusted bootstrap | vendored hash-pinned e-- fork; strict slot-free canonical entrypoint; minimal glue extensions; self-checking `.emm→.py` regen/acceptance tool | M0 | REVIEWED |
| M2 | deterministic ACE front end | vendored APE fork buildable on SWI 9.2.9; isolated hand-Prolog adapter → canonical byte-stable DRS; user-lexicon mechanism; stable doc/sentence IDs | M0 (APE strategy) + M1 (glue) | REVIEWED |
| M3 | IR contract + vertical slice | versioned IR spec; hand-Prolog DRS→IR lowering + validator; thin e2e slice ACE→DRS→IR→Prolog→answer+explanation | M2 | REVIEWED |
| M4 | rule-language widening | NAF subset + stratification validation over the M3 kernel; program-digest binding in answer records; wh answers; deterministic justifications traced to ACE sentence IDs | M3 | REVIEWED |
| M5 | first guideline + manual pipeline | E---generated CLI orchestrating full chain; one real guideline authored in ACE e2e; guideline registry + English↔ACE mapping store; full-chain acceptance | M4 | REVIEWED |
| M6 | semantics coverage widening | minimal IR/profile/kernel extensions converting the M5 residual inventory into executable semantics (ideal = zero residuals on the first guideline; survivors carry a recorded impossibility rationale); per-class least-intrusive-deviation decision — deterministic Prolog first, other formalisms (ProbLog-class or other) only if required | M5 | IN-PROGRESS (paused → M9; user 2026-08-04) |
| M7 | resumable /goal driver | registry-driven idempotent subagent-orchestrated completion driver; durable cross-session state; bounded termination (registry + discovery frontier empty) | M6 | UNPLANNED |
| M8 | formally verified answer checking | small human-read formal spec + machine-checked (AI-written, unread) checker certifying chain answers — human certifies by reading the spec + running the checker | M7 (proof last — spec stability; user 2026-08-04) | UNPLANNED |
| M9 | total derivational coverage + semantic traceability | Bash acceptance surface into the derived graph; E-- `[[...]]` call-target semantics covered (no concealed Python); hand-Prolog transformation laws represented in natural-language form w/ machine-tied coverage | — (pulled before M6.4–6.8; user 2026-08-04) | IN-PROGRESS |

## M0 — feasibility + governance (REVIEWED)

Pins/licenses/decisions → `docs/provenance.md` (sole authority).
- M0.1 DONE (main=36% 98K/272K, impl=20% 53K/272K): governance doc + contracts → commit `2832a96`.
- M0.2 DONE (main=14% 39K/272K, impl=35% 94K/272K): APE-on-SWI-9.2.9 probe; evidence banked in M2 seed → commit `24a90ee`.

## M1 — E-- trusted bootstrap (REVIEWED)

Governance/pins → `docs/provenance.md`; fork identity/TCB allowlist/fidelity procedure → `vendor/e--/PROVENANCE`; strict contract → `docs/strict-profile.md`; regen contract → `docs/regen.md`; executable gates → `.github/workflows/ci.yml` + `tests/strict-harness.sh` (57) + `tests/regen-harness.sh` (16).
- M1.1 DONE (main=22% 60K/272K, impl=22% 59K/272K): vendored fork verbatim at pin + manifest/patch scaffold → commit `743e90a`.
- M1.2 DONE (main=40% 109K/272K, impl=47% 128K/272K): validated strict entrypoint (patches 0001/0002) → commit `fbafd49`.
- M1.3 DONE (main=20% 55K/272K, impl=46% 124K/272K): TCB language extensions (patch 0003) → commit `eb0aadb`.
- M1.4 DONE (main=33% 90K/272K, impl=36% 99K/272K): self-checking regen tool + trust-bootstrap CI → commit `167d2d7`.
- Review: 15 findings → 11 confirmed, 4 refuted; fixes = fail-loud iterdir traversal + single-pass buffered regenerate (regen.emm), patch 0004 Apache §4(b) notices, provenance patch enumeration, .gitattributes first-party gaps, record collapse → review commit.

## M2 — deterministic ACE front end (REVIEWED)

Context = `docs/research/cnl-ace.md` (fail-closed clinical profile, lexicon policy, §7 design decisions) + `docs/research/ir-rule-semantics.md` §7 (parse success ≠ profile acceptance); full texts in `docs/references/` (README = citations/hashes/rights). Charter boundary: adapter + serializer = hand-Prolog (parser machinery); all first-party Python = E-- generated.

- M2.1 DONE (main=29% 79K/272K, impl=75% 204K/272K): APE vendor + regression CI → commit `7af31ad`.
- M2.2 DONE (main=42% 115K/272K, impl=41% 111K/272K): fail-closed adapter + canonical serializer → commit `0105517`.
- M2.3 DONE (main=25% 68K/272K, impl=40% 109K/272K): optional per-run Ulex → commit `9900e11`.
- M2.4 DONE (main=29% 79K/272K, impl=40% 108K/272K): stable IDs + front-end glue → commit `1981be8`.
- Review disposition: lenses A–D + external audit; 14 validated findings → fix batches 1 (code/harness: strict UTF-8 hand decoder because SWI-9.2.9 `library(utf8)` is unsound, locale pinning, Ulex totality, stream quarantine, glue framing classes, vendor gates — detail in the review commit) and 2 (docs truth-alignment); 3 refuted findings booked in the review commit; M5 resolved the deferral: `mapping_document/4` + guideline-harness docid/path/digest ties bind registry identity externally, `ace_front_end_record(1)` unchanged; gates = 57/16/45/27/10.
- Post-close residual audit (banked, non-blocking; shipped behavior unaffected — zero-write + nonzero exit hold in every case): gate-strength backlog = CI UTF-8 red matrix beyond 0xff (full RFC 3629 matrix probe-verified in-session only), cyclic-DRS + noisy-success-isolation fakes, further Ulex warning families, surface unknown vendor-regression result codes (PASS_COUNT totals hard-asserted in all nine harnesses since the M3 review), spawn-OSError + post-run ulex `read_bytes` tracebacks → sanitized error classes, qsave `make install` lacking `-F none` (vendor-anchor-only surface, needs vendored-Makefile patch); design-level (reaches user before any edit) = transient ulex replace-then-restore is invisible to upfront-read + post-run re-verify → true fix is an immutable snapshot handed to the adapter, which changes the adapter argv contract.

## M3 — IR contract + vertical slice (REVIEWED)

Gate M2 confirmed 2026-07-19 (five harnesses green). Plan-fixed decisions, probe evidence, and deferrals are superseded by the shipped authorities: grammars/semantics/error classes/capability matrix → `docs/ir.md`; byte authorities → `tests/fixtures/{ir,slice}/`; unit detail → the unit commits.

- M3.1 DONE (main=31% 85K/272K, impl=55% 149K/272K): IR v1 spec + validator + `ir_tool.pl` CLI + 34 fixtures + harness/CI → commit `0eb0849`.
- M3.2 DONE (main=42% 113K/272K, impl=74% 201K/272K): DRS→IR lowering, `==`-exact profile matching (aliasing counterexamples pinned) → commit `e2465d9`.
- M3.3 DONE (main=29% 79K/272K, impl=47% 129K/272K): compiler + deterministic least-model kernel + explanation/replay, program/answer v1 records → commit `d8b0261`.
- M3.4 DONE (main=27% 74K/272K, impl=24% 65K/272K): end-to-end slice harness + docs truth-alignment + CI → commit `ad8fcab`.
- Review disposition: lenses A–D + external audit; fix batches = kernel join-fusion (materialize-then-dedup cost `n^k` candidate memory → fused growing-store `nb_setarg` dedup, differential-proven under `ulimit -v`), forced-quote complete-shape proof, `generated_record_call` wrappers, non-binding `==`-decomposed ulex matcher, strict `'[|]'/2` replay checks, harness/CI strength (hard PASS_COUNTs ×9, per-PID scratch, whole-tree cleanliness, memory-bounded wide-join gate), fixture/gate coverage + docs truth-alignment; validator dedup banked; gates 57/16/45/27/10/60/34/39/25 MAIN-rerun green.
- Post-close residual audit (banked 2026-07-19 from review-lens final reports; non-blocking — shipped behavior verified correct in every case, findings are gate-strength/coverage only): ir-run `determinism/red` repeats are not byte-compared to the committed-red first rejection and lack the one-LF-line check (identical multi-line repeats would pass; the same fixture is one-line-pinned via `check_rejection`, so only a nondeterministic tool escapes); rejection gates match stage/class with free `Detail` — extend ir-validate's exact-stderr-line pattern to lower/run reds or parse+reserialize `Detail` as a canonical term (docs/ir.md error-record claim otherwise unpinned); run-stage red for a transitive two-rule cycle (program validator's `reachable/4` is the tamper boundary; only self-loop committed); admitted two-variable rule green pinning `var(1)`/`var(2)` discrimination (collapse-to-`var(1)` mutant survives and generated-IR backstop accepts it); same-sentence multi-clause green pinning clause-ID density (sentence-counter mutant survives); lower first-failure priority exact-line pins (root-domain/question-count swap survives); pass-timing/multi-round kernel green beyond competing-witness clause order; `rule_id`+empty-body identity red; committed UTF-8 boundary matrix beyond the scratch set (3/4-byte overlongs, truncations — probe-verified only, mirrors the M2 banked item); slice env-perturbation pass (HOME/TZ/umask; strict-harness precedent); `acyclic_term` + answer-shape guard before replay output (ground cyclic children would loop; certificate construction is proven acyclic, so internal-corruption-only); upgrade the chain recipe + harness from zero-non-empty to zero-artifact via temp-write+rename (recipe-compatible; docs deliberately state the honest zero-non-empty contract today).

## M4 — rule-language widening (REVIEWED)

Normative grammar, semantics, error classes, and the shipped/deferred capability matrix → `docs/ir.md`; lowering probes → `tests/fixtures/lower/{green,red}/`; end-to-end byte authorities → `tests/fixtures/slice/`; digest runtime TCB → `docs/provenance.md`; gate totals → `docs/regen.md`.

- M4.1a DONE (main=55% 151K/272K, impl=66% 179K/272K): program/answer v2, signed-cycle validation, stratified NAF kernel, absence-checked certificate leaves, exact-raw-byte program digest → commit `290d5d1`.
- M4.1b DONE (main=32% 88K/272K, impl=~42% ~114K/272K): exact `wh(who)` execution, canonical-byte answer ordering, answer self-check, `certificate_node_cap(1000000)` preflight → commit `6bcc50f`.
- M4.2 DONE (main=48% 130K/272K, impl=88% 238K/272K): IR v2 + validator + compile map; probe-pinned P01/P10 wh + P13/P15 NAF lowering; paired IR/program green/red gates → commit `9cf8af2`.
- M4.3 DONE (main=34% 93K/272K, impl=47% 129K/272K): `slice-naf` + `slice-wh` end-to-end chains, crypto provenance/CI pin, closure docs → commit `b5c7e8a`.
- Review disposition: four lenses (correctness/spec, cross-unit integration, conformance, efficiency/obsolescence) → 12 accepted findings (majors: yes/no answer self-check envelope gap + incomplete D8 pairing + stale roadmap/memory durable state; rest gate-strength/text minors), remainder verified clean; fix batch = answer self-check now enforces the v2 envelope/layout for every answer shape (yes/no included; previously only wh) with committed accept/reject probes; D8 pairing completed (8 new program reds incl. `identity-rule-empty-body`, exact-stderr-line pins on all paired reds both sides — pairs proven byte-corresponding modulo stage atom); compile-stage ownership loop (every IR red rerun through `compile`, stderr byte-equal to `validate` stderr modulo stage atom, covers v3 envelope); malformed recognized-`~` inner content now class `negation` with new lower red (cross-DRS redeclaration stays `referent`; docs state the exact split); `first_missing_var` numeric non-binding membership mirroring the kernel; dead `read_utf8_input/3` removed; pipeline second-run stdout pinned; docs heading → capability status, `answers/1` strictly-ascending/duplicate-free wording; memory refresh (authority map version-neutral, digest hashes pre-decode bytes incl. final LF, wh canonical-byte order, digests output-only); harness file modes normalized to 755; gates now 57/16/45/28/10/73/51/93/29 → review commit.
- Durable M5+ handoff: D10 labeled exception IDs + explicit closed-world declarations deferred with direction/strength; validator-pass deduplication stays banked; proof enumeration/DAG sharing stays first-witness/tree-only; digest = integrity binding, not authentication. Expanded-certificate-node/result-byte baselines: `slice` 3/584, `slice-unknown` 0/386, `slice-naf` 3/553, `slice-wh` 3/622.
- Post-close residuals (banked 2026-07-22, non-blocking; the M3 residual backlog stays open except its transitive-cycle red, which M4 shipped): aggregate resource-cap red (individually sub-cap trees breaching only in sum; exact Cap-accepted/Cap+1-rejected boundary fixtures); `run/red/envelope-v1-record.program.pl` deliberately retains the legal-under-v2 NAF payload + old docid to prove envelope-first rejection; harness dedup opportunities (third competing-witness run, lower-harness counting file families it never consumes, slice count-only gates subsumed by exact inventories).

## M5 — first guideline + manual pipeline (REVIEWED)

### Preconditions (closed 2026-07-22)
- Planning admission used real inputs: nine harnesses + `regen: check ok` MAIN-rerun green; 23 full-chain probes on a fresh staged APE tree (greens byte-deterministic ×2, exact first-failure reds); CDC source/rights bytes fetched twice byte-identical. Probe scratch was intentionally non-durable; detail is recoverable from the M5 plan commits `0510838`–`31a51be`. Shipped capability, source/rights, and acceptance authorities: `docs/ir.md`, `guidelines/cdc-2022-opioid/README.md`, `guidelines/registry.pl`, `tests/guideline-harness.sh`.

### Decisions (shipped authority)
- D1–D8 are implemented. Normative acquisition/registry/mapping/Ulex/pipeline contracts live in `docs/registry.md`, `docs/ulex.md`, `docs/pipeline.md`, `src/prolog/registry_tool.pl`, `tools/pipeline.emm`, and the CDC guideline tree; plan rationale stays in commits `0510838`, `262923e`, `31a51be`.
- Live handoffs only: D5's typed `mapping_residual/5` rows in `guidelines/cdc-2022-opioid/mapping.pl` are M6's verbatim-anchored requirements inventory; hard authoring errors (fact-after-rule, zero/multi/non-final query) are rejections, never residuals. D9 keeps conflict detection and the research evaluation battery out of scope. D10's remaining M4 items stay banked in the M4 handoff and the M6/M7 seeds.

### Units
- M5.1 DONE (main=34% 94K/272K, impl=69% 188K/272K) — acquisition + registry/terminology contracts and validator. Shipped authorities: `guidelines/cdc-2022-opioid/{README.md,source/}`, `guidelines/registry.pl` (source row + extraction-evidence row + 16 regions: 12 rec + 4 grp), `docs/registry.md`, `src/prolog/registry_tool.pl`, `tests/registry-harness.sh`; unit commit `8413832`. Mapping split to M5.1b at the subcommand seam (`31a51be`).
- M5.1b DONE (main=33% 89K/272K, impl=55% 148K/272K) — mapping contract + validator. Shipped authorities: `docs/registry.md` mapping v1, the `mapping` subcommand, registry fixtures/harness (94 current gates); unit commit `96ab6de`. The real M6 worklist is `guidelines/cdc-2022-opioid/mapping.pl`, not the generic green fixture.
- M5.2 DONE (main=38% 103K/272K, impl=26% 70K/272K) — transactional pipeline CLI. Shipped authorities: `docs/pipeline.md`, `tools/pipeline.emm` → `tools/pipeline.py`, `tests/pipeline-cli-harness.sh` (17); unit commit `8ec7b82`, record reconciliation `1547e6b`. Banked gate-strength residual: distinguish fresh-disk stage reads from in-memory forwarding via an on-disk interstage mutation, and pin the remaining out-exists/parent/stale preflight adjacencies (dangling-symlink and glob/metachar multi-stale gates are shipped).
- M5.3 DONE (main=27% 74K/272K, impl=36% 97K/272K) — CDC corpus authoring. Shipped authorities: 12 ACE documents + 12 Ulex sidecars, `terminology.pl` (49 entries), `mapping.pl` (12 documents, 16 regions, 12 claims, 229 residuals), `golden/` (50 files); unit commit `6f7d90b`, row-level review history in the 25 commits under `git log --grep '(M5.3 review)'`.
  - Live M6 handoff: `guidelines/cdc-2022-opioid/mapping.pl` is authoritative; 229 is the close-time inventory, not a future acceptance pin. `applicability` claims are rec05 + rec10 only; the other ten are `action_kind`. Every region has residual coverage and all 12 residual classes occur.
  - Adjudication rule: one work item per distinct source-anchored constructor/cause; host structure and branch action/relation may coexist; entity inventory, relations, qualifier properties, temporals/populations, and attachment/scope split only when separately anchored. A rejection reverses only when a later shipped file pattern contradicts it. Recorded reversals: rec04 effectiveness property (`97980ad`), rec05 taper/discontinue relations (`43b6781`), rec12 arrange relation (`f6db4fe`), rec12 offer relation (`155ac3c`).
  - Standing rejected precedents unless a shipped contradiction appears: rec05 NAF-vs-actual-absence is a contract-defined bounded-projection gap already typed by rec05.04; group headings need only `scope_deferred`; deictic “other” qualifiers add no row; no blanket temporal-payload relation twin, second rec11 concurrency row, rec07 reevaluate relation, inner-outweigh transitive twin, rec11.09 polarity disjunction, rec09.03 instrument attachment, rec12.06 causal twin, rec05 based-on attachment, rec11 clinician-prescribing relation, rec05 negative-comparison twin, rec04 any-dosage quantity twin, or rec07 first-sentence benefit-risk `multi_entity`. Row lists, note trims, and rationales stay in the review commits, not the roadmap.
- M5.4 DONE (main=44% 120K/272K, impl=57% 156K/272K) — full-chain acceptance + closure. Shipped authority: `tests/guideline-harness.sh` (45) + CI twelfth harness; `docs/regen.md` is the current twelve-harness inventory, and `docs/ulex.md`/`docs/registry.md` own the production ties. Acceptance covers real-file validation, 12-sidecar cmp, mapping digest/answer/item and 16-region coverage ties, pipeline ×2 vs the 50-file golden, interrupted-run stale-refusal/recovery, Clex-independence, and vendor/repository cleanliness. Closure commit `c1b7a55`; the residual count is deliberately not pinned by acceptance.
- Review disposition: four lenses (correctness/spec, integration/gate-strength, conformance, efficiency) + per-lens sub-probes; MAIN re-verified the full twelve-harness sweep, cross-file digest/answer/item/coverage ties, and pipeline regeneration independently green before adjudication. Accepted findings → fix batch A (docs/memory truth-alignment: residual/range class boundaries + registry/terminology/ulex pass orders in `docs/registry.md`; provenance now explicitly delegates guideline-artifact rights to each guideline's validated registry; `docs/ace-front-end.md` docid/integrity/CI staleness; `docs/ir.md` chain section rewritten around the production pipeline; memory authority map consolidated) and fix batch B (harness gate strength: usage red byte-pinned, stub validates full 10-invocation argv sequence incl. APE/Ulex tails + stage tokens with expected==run1==run2 cmp, `set -o pipefail` + pipe restructuring closes fail-open hash helpers, pipeline-cli/registry golden trees now `-text`); this section's collapse to shipped authorities is the accepted efficiency finding (roadmap 48.8K→23.8K bytes). Refuted: bare `"$SWIPL" --version` probes are init-file-safe (probe-verified; the `-f none -F none` lesson governs interpretation runs). Banked (non-blocking): the same pipefail fail-open pattern is unaudited in the nine pre-M5 harnesses; registry/terminology/ulex multi-fault precedence gates are documented but not harness-pinned. Gates 94/17/45 + `regen: check ok` MAIN-rerun on the repaired tree.

## M6 — semantics coverage widening (IN-PROGRESS)

Directive (user, 2026-07-22; supersedes the 2026-07-18 scope guard on this axis only): the semantics M5 omits get minimal covering extensions; ideal = all semantics covered, zero residuals on guideline #1; any survivor carries a recorded impossibility/deferral rationale, never silence. Per-class least-intrusive order: existing chain constructors < deterministic IR/profile/kernel extension < external engine (ProbLog-class named acceptable; choice per class at planning). Worklist = the 229 typed `mapping_residual/5` rows (transitive_relation 39, temporal 29, scope_deferred 27, property 27, multi_entity 27, certainty 19, population_threshold 18, direction_strength 14, disjunction 11, labeled_exception 9, dose_quantity 6, copula_head 3). `docs/research/ir-rule-semantics.md` = planning basis; conflict detection + evaluation battery stay out.

### Preconditions (closed 2026-07-24)
- Gate M5 REVIEWED; twelve harnesses MAIN-rerun green 2026-07-24 (57/16/17/10/45/28/73/51/93/29/94/45) + `regen: check ok`; tree clean.
- Planning lenses: full-inventory taxonomy; chain feasibility probes on the real vendored APE tree (`.scratch/m6-probe/`, intentionally non-durable: 60-variant battery ×2 byte-deterministic, 79 adapter + 63 lower determinism pairs clean, zero warnings on accepted surfaces); design/formalism lens with web-verified engine status. MAIN independently reran the twelve harnesses and re-proved kernel arity-genericity via `ir_tool` validate/compile/run on a scratch binary-predicate record (`proved`, rc 0).

### Decisions
- D1 formalism: **no external engine for guideline #1** — all 12 classes get deterministic coverage inside the shipped chain. Future deviation triggers recorded: genuinely weighted/numeric uncertainty, non-stratified recursive defaults, true multiple-model head disjunction. Candidates banked with audit-cost notes (s(CASP) SWI pack 1.1.4 declares prolog>=9; ProbLog 2.2.10 maintained, Python-side, no native SWI bridge; clingo 5.8.0 MIT, no model-order reproducibility guarantee); any adoption = full vendor treatment preserving byte-pinned artifacts, deterministic first-failure classes, replayable certificates, regeneration-only corrections.
- D2 versioning: M6 ships IR/program/answer **v3** (v2 versioning law: constructor changes ⇒ new envelope). The one-time v2→v3 fixture/golden regeneration lands in M6.1a; later units grow v3 additively, each leaving all harnesses green; v3 is finalized at M6 review. Wh stays exactly unary `var(1)` (inventory needs nothing richer).
- D3 grades = data, never priorities: recommendation category A/B + evidence type 1–4 become typed region-annotation rows in the guideline registry (registry v2), machine-tied to source quotes; deontic `should`/`must`(-not) boxes → rule-level direction annotation (positive|against) in IR v3; hedges (anticipated/expected/likely/known) → typed scoped qualifier constructors. No probabilistic semantics — the source carries no weights; annotations are evaluation-inert but certificate/explanation-visible.
- D4 disjunction: antecedent `v/2` → deterministic DNF branch split with generated branch rule IDs (origin rule ID + 1-based branch index + per-branch provenance; mixed and/or never flattened; deterministic expansion cap fails closed). Action alternatives (rec05.03, rec12.01) → labeled `alternative_set` constructor (satisfaction = any-member; exclusivity/exhaustiveness not_asserted) — choice obligations, not disjunctive knowledge; no head disjunction, no ASP.
- D5 exceptions: PROLEG-style labeled exceptions over the shipped stratified NAF (banked M4 D10): exception IDs + affected-rule refs + explicit closed-world declarations + certificate leaves carrying the label. Positive `if` branches stay positive guards; explicit "do not outweigh" is a typed comparison outcome; only genuine unless/without lowers to NAF and only under a declared-complete predicate.
- D6 quantities/temporal: typed quantity constructors (integer/exact-rational value + unit atom, open/closed bounds, same-unit comparison only, floats rejected). Inventory needs qualitative dose order (lowest/lower/higher) + exactly one anchored numeric window ("within 1–4 weeks of starting", endpoint openness recorded, not invented); temporal constructors = nominal-PP anchors (before/after/during/within + ongoing/prior-state predicates); no Event Calculus. Qualitative thresholds (many/common/regularly/gradually/rapidly) stay typed qualifiers — no fabricated cutoffs.
- D7 mapping v2: claim-only region coverage becomes admissible (v1's claim⇒residual invariant retired); projection vocabulary widened to the new semantic dimensions; surviving residuals get a machine-distinguishable recorded impossibility/deferral rationale (not prose-only); conversion = row moves (residual deleted ⇔ covering claim(s) added), harness-tied to the regenerated golden chain.
- D8 grouping: the 4 `box3.grp.*` rows = registry v2 grouping/membership rows (document-structure metadata), never clinical antecedents. The other 23 scope_deferred rows have no single constructor: attachment machinery comes from M6.1a role edges + M6.2 branch-local guards + M6.3 state anchors; per-row disposition (author-around vs documented survivor) happens at conversion under D7.

### Probe map (FAST-PATH; authority = this plan commit, scratch non-durable)
Wall = **lowering only**: the fail-closed adapter serializer already emits canonical bytes for every novel operator, and IR v2 validate/compile/run are already arity-generic (binary fact/rule/query chain green end-to-end; only lowering pins arity 1). Pinned canonical DRS shapes: transitive `predicate/4`; adjective `property/3` (predicative + object-attached attributive); comparative `property(A,effective,comp_than,named(_))` / `pos_as`; of-attachment `relation/3` (unpaired-object copula red fires first at roots); antecedent disjunction `v(drs,drs)`; measurement `int(N,Unit)` + unit-bearing `object/6`; nominal temporal `modifier_pp/3` before/after; modality boxes `should/1` `must/1`. Multi-condition consequents fail `consequent_count` before inner constructors. Subordinate before/after **clauses do not parse** (APE `ape_messages` red) → temporal order is authored as nominal PPs. Bare lowercase common nouns need a determiner (`some`) grammatically; since the 2026-07-29 Clex incorporation the staged full Clex resolves general-English content words, so Ulex/terminology entries are needed only for domain compounds, proper names, and sense/valency overrides. `should not` DRS shape unprobed → M6.4 probes first.

### Clex incorporation (user task, 2026-07-29)
Full Clex vendored at `vendor/clex/` (pin/manifest per `vendor/clex/PROVENANCE`) and staged over the APE demo lexicon in all four real-APE harnesses + the documented recipe; upstream regression consumes the vendored file (network fetch removed, CI fully offline). Proven before landing: CDC pipeline ×2 + slice front-end tree + all adapter fixtures byte-identical to committed goldens under the overlay (corpus vocabulary = terminology compounds with ulex priority; demo ⊂ full for every fixture word); new `clex-live` adapter fixture pins a Clex-only word end-to-end (adapter gates 45→47). Load cost ≈ +1s per adapter process. Effect on this plan: M6 fixture/corpus authoring draws general English from Clex; terminology/Ulex stays the domain delta per the binding guard; M6.6/M6.7 lexicon-authoring burden drops, sizes retained as margin.

Paused (user, 2026-08-04): M9 retrofit precedes M6.4–M6.8; resumption authors all new gates in the M9 carrier (M6.8's Bash share dissolves into M9-A) + adds a spec row per new Prolog law under M9-C's coverage validator.

### Units
- M6.1a DONE (main=56% 135K/240K, impl=95% 228K/240K): v3 envelopes; normative n-ary `pred` (Name/arity identity, n≥1, no cap); transitive `predicate/4` + `relation(R1,of,R2)`→`pred(of,[L,R])` lowering across root facts/rule heads/bodies/ground yes-no questions w/ lossless single-occurrence event erasure (reuse → `referent`,`event_in_use` at every position; generated signed cycles → typed `lower`,`cycle` exit 1); scripted v2→v3 regen (157 tracked + 36 new files; CDC golden+manifest via pipeline ×2 byte-identical; mapping ties untouched); review loop killed name-only-predicate-key + local-event-downgrade mutants (same-name cross-arity chain `p/2←p/1`, non-root event_in_use reds), pinned direct-v2 rejection on ir/program surfaces, exact-line lower red enforcement; wh stays unary `var(1)`; gates 77/66/110 (ir-validate/lower/run), rest unchanged, twelve-harness sweep + regen check MAIN-rerun green.
- M6.1b DONE (main=79% 190K/240K, impl=90% 216K/240K): property layer — `property/3` pos (predicative + attributive) → unary `pred`; `property/4` `comp_than`/`pos_as` → binary `pred` named adjective+U+0020+degree atom (`'helpful comp_than'`; injective via the `lemma_space` separator-reject law at every lemma consumption point incl. NAF/wh/question sites); copular-head normalization (noun/adjective/comparative carrier+`be`) across root facts, rule consequents, ground yes/no questions; antecedent carrier normalization w/ shared-carrier single-erasure; wh/NAF profiles unchanged (property NAF → `negation`, other degrees/arities → `unsupported`, unresolved/ambiguous carrier → `referent`). Review loop killed two majors (orphan /21 antecedent-ambiguity clause = silent zero-stderr exit 1 → /20 + committed red; erased-carrier head-coverage leak rc2 `uncaught` → typed `referent`,`unbound_head_referent` via BodyEntityRefs/BodyPositiveRefs split) + 10 gate reds (4 property `event_in_use` call sites, 4 non-root separator sites, 2 majors). 5 green 4-file chains (fact-vs-question arg order proves rule-head derivation) + 24 lower reds total; gates ir-lower 95, ir-run 120, rest unchanged; twelve-harness sweep + regen check MAIN-rerun green.
- M6.2 DONE (main=92% 221K/240K +1 compaction ~40% close tail, impl=100% 239K/240K +1 compaction, fixer peak 82%): antecedent `v/2` → deterministic DNF (complete left subtree then right; conjunction = Cartesian product) w/ dense same-origin `rule_id(S,C,branch(1..N))` (singletons keep `rule_id/2`), saturating `dnf_branch_cap(64)` fail-closed pre-materialization, per-branch provenance = shared+selected anchors, same-origin coherence law (`==`-identical heads; identical alternative members+policies) mirrored in IR+program validators via shared `validation_common.pl`; defined-target NAF → labeled `naf(exception_id(rule(RuleId),literal(Pos)),Pred)` + ordered `closed_world(ExceptionId,affects(RuleId),predicate_key(Name,arity(A)))` rows + labeled certificate leaves w/ exact-label replay; undefined-target bare `naf/1` byte-stable (rec05/slice-naf untouched); consequent two-action `v/2` → `alternative_set/7` metadata (≥2 `==`-distinct members, any_member + not_asserted exclusivity/exhaustiveness, transported IR→program→answer, never derives clauses/edges/store atoms); APE wall pinned: literal `unless`/`without`+VP unparseable → paired-rule authoring (`not provably` guard + defining rules). Council loop (diff-blind probe matrix + 72-mutant campaign): 2 P0 fixed — duplicate `alternative_set` members accepted at all three trust boundaries → pairwise-`==` distinctness + paired reds; identical-sibling branch-ID swap replayed → retained-first-witness clause-ID binding + control/tamper gates; 12 killer gates cover every high/med mutation survivor (rerun = all KILL); final re-review wave cancelled by user close-out — MAIN reran twelve harnesses + regen + both P0 probes directly (green). Banked to M6 review: validator-block centralization; bare-vs-labeled NAF unification. Gates ir-validate 107, ir-lower 115, ir-run 175, adapter 51, rest unchanged. (disjunction, labeled_exception.)
- M6.3 DONE (main=89% 215K/240K, impl=100% 239K/240K +1 compaction, fixer peak 99% 238K +1 compaction): additive v3 typed data layer — `quantity(integer(I),unit(U))`, `quantity_bound/3` w/ five exact operator/endpoint pairs (`eq|geq|leq`+closed, `greater|less`+open), non-binding same-unit `quantity_compare/2` body guards (shape → static cross-unit → binder-order safety; runtime typed cross-unit/non-quantity; same-unit false = ordinary failure) w/ exact ground-leaf certificates + truth-recomputing replay; extensional `temporal/3` (`before|after|during|within`) + `temporal_window/4` (`before|after`, interval validation, seven-condition exact ownership, all-anchor provenance, mixed-sentence rejection); qualitative dose order `'low|high comp'` + controlled `'low|high sup'` (natural superlative/during/tense-state/relative-population = pinned APE walls; controlled DRS admitted); state/population = ordinary predicates + binder+guard numeric thresholds; count `eq,1` excluded by law — APE marks every singular indefinite `eq,1` (`1 year`/`a year` byte-identical, MAIN-probed) → faithful exact-1 = singular measure-noun surface; `within 1 to 4 weeks` false friend rejects; wh universe stays named-only w/ typed `wh_query` run red. Council loop (diff-blind probe matrix + 72-mutant campaign + 3 internal lenses): 5 majors fixed (typed-wh uncaught → typed red; silent zero-stderr scalar exits → typed `quantity` reds; window 4/7-anchor provenance → full retention + splice red; count-1 reframed docs law; controlled sup added), 4 moderates (self-check var-shape depth + honest-depth docs, composition-barrier `root_section_order` red, near-miss error-ownership map + reds, closed-grammar docs + direct-int pins), gate matrices (quantity truth/safety/malformed-pair, temporal direction/bounds/anchors, per-field replay tampers, typed-leaf Cap/Cap+1, fixture-inventory anti-vacuity, adapter inventory pins, red ×2 determinism, `--ignored` symmetry); refuted: dose-carrier scoping (chain deliberately ontology-free). rev2 determinism 288+48+32 A/B executions stable; survivor recheck = 9 KILL + 6 externally-equivalent (named layered defenses). Gates adapter 80, ir-validate 122, ir-lower 151, ir-run 258, rest unchanged; twelve-harness sweep + regen MAIN-rerun green. (temporal, dose_quantity, population_threshold.)
- M6.4 BLOCKED(M9 REVIEWED) (~150K) — normative/epistemic layer: `should`/`must`(-not) lowering → direction annotations; hedge qualifier constructors over the property layer; registry v2 region grade + grouping rows. (direction_strength, certainty, grp scope_deferred.) Scout-pinned (×2 deterministic, 28 probes = 20 admitted/8 rejected, full map `.scratch/agents/scout-m6u4.md`): `should/1` `must/1` `can/1` `may/1` parse distinctly; `should not VP` = outer `-(should(...))` — negation OF obligation, so direction `against` lowers from that shape, never should-of-negated-VP; `must not VP` = parse wall; hedges (anticipated/expected/likely/known) admit predicative + attributive `property/3` only (sentential `It is likely that S` = wall); generic permission surfaces need an explicit determiner.
- M6.5 BLOCKED(M6.2+M6.3+M6.4) (~150K) — mapping v2 contract: `registry_tool` + `docs/registry.md` (claim-only coverage, widened projections, survivor rationale shape), fixtures + registry-harness updates, conversion recipe for the corpus units. M6.1b feeds M6.4 only; M6.5 waits on all constructor units.
- M6.6 BLOCKED(M6.5) (~200K) — corpus re-authoring + conversion rec01–rec06: richer ACE (general English from the staged vendored Clex) + domain-delta terminology growth + residual→claim conversion + golden regeneration.
- M6.7 BLOCKED(M6.6) (~200K) — same for rec07–rec12 + the four grp regions (shared golden manifest serializes after M6.6).
- M6.8 BLOCKED(M6.7) (~120K) — full-chain acceptance + closure: guideline-harness + CI updates, docs truth-alignment (capability matrix flips, regen inventory), residual end-state audit (zero or documented-impossible with recorded rationale).

## M8 — formally verified answer checking (UNPLANNED; seeded 2026-07-29)

Directive (user, 2026-07-29): adopt the exemplar outcome of `github.com/schildep/verified-3d-mesh-intersection` — a human certifies correctness by reading a ~100-line formal spec + running the Lean checker while AI-written implementation (1000+ lines) and proofs (60K+ lines) stay unread — as an eventual goal for this chain's answer computation.
- Planning basis = `docs/research/formal-verification.md` (exemplar re-verified at its pin incl. rerun build/axiom checks; boundary candidates B1–B4 with measured v2 spec surface; Lean-4-first toolchain fit + vendoring profile + alternatives; prior art incl. DatalogCert ITP 2017 + certifying-algorithms framework).
- Candidate target (M8 planning finalizes): one Lean package — reviewed spec module (v3 record byte grammar + decoder relation + stratified standard-model semantics + 4-way contract theorems) + unread AI checker/proofs; phases `check-model` (B2 complete answer relation: soundness + completeness incl. `not_proved`/`answers([])`/wh totality) + `check-cert` (B1 certificate replay against the recomputed model) + separable `check-compile` (B3a IR→program). Prolog kernel/compiler leave the trusted base for answer correctness; ACE→IR meaning stays human-audited via ACE + registry/mapping (charter unchanged).
- Gate = M7 DONE (user 2026-08-04: proof last — pre-final specs churn proofs; M7 scale-out may widen v3 under new guidelines → proofs start once record grammars stop moving; no artifact dependency on M7). v3 final at M6 review stays necessary (est. +40–80% spec surface lands in M6; v2 now = migration churn). Interim trust = replay/self-check + human audit (shipped guarantee). Safe idle pre-gate work: decoder prototype + boundary theorem statements over current fixtures as regression vectors.
- In-flight pressure on M6 units: every constructor/certificate shape added lands in the future trusted spec → keep record/certificate grammars minimal, total, canonical.

## M9 — total derivational coverage + semantic traceability (IN-PROGRESS)

Directive (user, 2026-08-04): reproducibility today is partial — E--→Python (regen-gated) + ACE→IR→Prolog (pipeline/golden-gated) — and must become total derivational coverage with strong semantic traceability. Named gaps: Bash outside the graph; arbitrary `[[...]]` E-- expressions concealing Python semantics; substantial handwritten Prolog defining transformations with no natural-language representation.

Measured gap inventory (session-verified at seed):
- **A Bash**: 13 first-party `.sh` = 7,015 lines (12 acceptance harnesses 6,964 + `.agent/context-gauge.sh` 51) + `.github/workflows/ci.yml` 155 — the entire acceptance authority is hand-Bash, underived, gated only by itself. Charter already names tests as E-- scope ("sole authored source for all first-party Python glue, tests, and tooling"); Bash escapes that sentence only by not being Python.
- **B `[[...]]`**: E--'s call marker (spec: `[[name]](args)`) — the sole call syntax, so elimination ≠ removal; concealment = strict profile admits `IDENT (DOT IDENT)*` targets reaching arbitrary Python callables whose semantics E-- neither defines nor constrains. Production tools saturated: `pipeline.emm` 134 `[[`-lines/203, `ace_front_end.emm` 106/178, `regen.emm` 105/173 (345 sites); imports argparse/hashlib/os/pathlib/shutil/subprocess/sys. E--→py derivation holds; semantic traceability stops at the marker exactly on the lines that matter (hashing, spawn, fs transactions).
- **C hand-Prolog**: `src/prolog/` 10 modules 9,705 lines (drs_to_ir 3,089, inference_kernel 1,824, registry_tool 1,692, ir_validate 1,196, explanation 806, ir_tool 365, validation_common 347, adapter 263, ir_to_prolog 70, drs_canon 53). Charter sanctions the hand boundary ("parser, compiler, validator, explanation machinery, inference kernel" — all 10 fit); directive amends ambition: lowering/validation/kernel/replay laws live only as Prolog + `docs/ir.md` prose, no NL-form representation, no machine tie doc↔clause.

User-fixed (2026-08-04): full retrofit now, C included — M9 precedes M6.4–M6.8. Unit order B → A → C (A authors ~7K fresh `.emm` lines → B's vocabulary first; C ties pre-final v3, M6.4+ laws extend the corpus additively via lifecycle). M6 resumption authors every gate in the derived carrier + a spec/evidence row per new Prolog law; M6.8's Bash share dissolves into A; banked M6-review validator refactors (block centralization, bare/labeled NAF) absorb inside C before long-lived binding. M8 interaction: complementary — C covers the ACE→IR→program side M8 leaves human-audited; C's corpus = candidate input to M8's human-read spec; shared minimality pressure on record/certificate grammars.

### Preconditions (closed 2026-08-05)
Twelve harnesses MAIN-rerun green (57/16/10/80/28/29/122/151/258/17/94/45, sum 907 hard-pinned) + `regen: check ok` + clean tree. Planning: 3 code-map + 2 research teammates → draft → adversarial review (18 findings, all accepted, 3 MAIN overrides) → full redraft → round-2 verification (16 RESOLVED + definitional residuals closed in this section; scratch non-durable — this section = sole plan authority). Committed nonnormative synthesis + external prior art: `docs/research/derivational-coverage.md`. Wave-1 counts spot-verified exact (345 `[[`-lines / 374 markers / 36 declarations / 338 invocations).

### Decisions
Sizing law: nominal→projected peak via M6-observed multipliers (M6 nominal 150–200K peaked 228–239K): H compiler/refactor/spec ×1.60, M port/integration ×1.40, S seam ×1.25; every projected peak ≤150K. Unit sizes below = `(nominal→projected class)`.
- D1 typed checker: vendor patch 0005 = AST source identity + compiler-owned ConstructSpec registry + post-parse/pre-emit semantic check; `strict:vocabulary` = exit 1, zero output/temp, exactly one LF stderr line; byte/BOM/control/token/syntax classes keep priority. Raw module calls, receiver methods, callable values, aliases, malformed signatures/keywords, unchecked projections all reject. Strict transition (M9.1): `06-use-os`/`07-use-os-path` → raw-`Use`/call reds + generated-import-grammar greens; `08-pathlib-path` → typed `path` green; `09-method-call` → receiver red + local/export-call green; `10-dotted-ref-chain`/`14-terminator-dotted-ref` → statically typed projections keeping dotted/terminator parser coverage. Post-M9.1 strict = exactly 77 (14 green + 27 syntax/vocab red + 14 byte/control red + 4 CLI + 11 runtime + 1 Require + 2 determinism + 2 atomic + 1 quarantine + 1 CI-reference); later construct depth lives in vendored unit tests, strict total moves only with every reference updated in-unit.
- D2 compile-time linking + namespace law: `Use` names only compiler-resolved generated E-- library sources declared in the capability profile; compiler parses lib `.emm`, requires acyclic graph + zero top-level effects, resolves explicit exports, deterministically mangles + emits into each consumer → every generated `.py` standalone under `python3 -P`; no package/PYTHONPATH/loader/runtime import. Exactly-one resolution across {construct IDs, local `Define`s, linked exports}; construct IDs + dotted roots reserved against `Define`/param/loop/import/export names; zero/multiple resolution rejects, no precedence. Corpus: missing source/export, source/generated drift, cycles/diamonds, top-level effects, `.py`/cwd/PYTHONPATH hijack, collisions + case/underscore near-collisions, hostile-cwd determinism.
- D3 legacy ledger + permanent audit: pre-migration commit `spec/e--/legacy-surface.pl` (origin commit, source SHA-256s, stable site IDs: 338 calls + 36 declarations + every import/dotted value projection incl. `sys.executable`); each site → exactly one disposition (local/export/construct rewrite | merged-into | deleted-w/-acceptance-case | parser-only red). Patch 0005 keeps read-only `e_minus_minus.surface_audit` after the open resolver dies; permanent gate: `PYTHONPATH=vendor/e--/src python3 -P -m e_minus_minus.surface_audit --baseline spec/e--/legacy-surface.pl --capabilities spec/e--/capability-profiles.json --check tools` = baseline schema/count/disposition completeness + zero current raw import/call/projection.
- D4 registry + capability profiles: production = exactly 45: `sha256_bytes proc.run read_bytes read_text_utf8 dir_entries path_exists path_is_file path_is_dir path_is_symlink write_bytes make_dir remove_tree replace_path path path_join path_with_suffix path_posix parse_cli env_get env_snapshot resolve_tool current_python_tool process_id to_text replace starts_with ends_with remove_prefix remove_suffix split split_lines encode_utf8 decode_utf8 bytes_from_ints count_subsequence write_line write_stderr_bytes sort length set_of is_subset append map_get map_put_all take`. Acceptance = exactly 22: `files_equal walk_tree snapshot_tree sha256_file read_slice slice_value scratch_dir copy_file copy_tree append_bytes remove_file set_mode make_symlink read_symlink lstat stub_tool signal_process regex_search regex_fullmatch regex_capture_all regex_replace glob_match` (TreeSnapshot equality = value equality; no trees_equal). Total 67 unique IDs. `spec/e--/capability-profiles.json` = canonical strict JSON (duplicate-key + canonical-byte gates): per-module allowed constructs/projections/effects, library exports, immutable tool manifests + argv schemas, env policy, readable/writable roots, migration-only capabilities. Acquisition: `current_python_tool()` = immutable pinned-interpreter ToolCap (replaces every `sys.executable` projection); `resolve_tool(name,policy)` = manifest-literal rows only (`$SWIPL` = declared env-key/fallback/PATH rule); fixed git/make/SWI/APE-build caps w/ exact identity/digest/argv schemas; legacy-Bash cap = migration-only, removed at M9.22; no generic Path/String→ToolCap. Production may keep `env_snapshot`/PATH effects where current behavior requires; acceptance profile forbids ambient inheritance.
- D5 record algebra (machine types, never ad-hoc dicts/paths): Repo/Dir/FileReadCap (immutable/no-follow), ScratchCap/StageCap (exclusive writable, cleanup/containment mandatory), CwdCap = RepoCwd|ScratchCwd, EnvSpec (complete kv + explicit absent-key set; A never inherits), StdinSpec = Bytes|DevNull, TimeoutSpec, RLimitSpec (closed fields), FDPolicy (close-all except declared stdio + process-group/session), ToolCap (manifest id, identity/version/digest, argv schema, source/input closure, allowed effects). `ProcessResult{outcome,stdout,stderr}`, outcome ∈ exactly {`Exited(c)`, `Signaled(s)`, `Timeout(deadline,termination)`, `LimitKilled(kind,limit,termination)`, `SpawnFailed(class)`}; no independent returncode (`Exit with outcome` = sole legacy-status projection).
- D6 stubtool: `fixture_tool` rejected. `tools/acceptance/stubtool.emm` → standalone digest-pinned `stubtool.py` (derived/byte-accepted); acceptance authors only closed StubSpec data: ordered argv/stdin expectations, canned stream bytes, `Exited(c)`|self-signal result, scratch-confined file mutations, optional parent-signal, + finite probe actions `block(ms|forever)` / `spin_cpu` / `alloc_bytes(N)` / `emit_bytes(stream,N)` = the sole realization of Timeout/LimitKilled/rlimit controls (expected process-group cleanup + outcome classification pinned per probe). Stub state confined to its ScratchCap; `signal_process` profiled self/parent-only. No E-- data path (script/mode-bit/interpreter-string/scratch file) mints executable authority. APE fake modules etc. = fixed tracked authored-boundary fixtures, C0-discovered.
- D7 backend: constructs lower via compiler expression/statement templates + deterministic used-construct stdlib prelude; helpers unnameable in E--; generated tools/runners import zero trusted runtime.
- D8 B invariant: every B unit runs BOOTSTRAP + CI_REF + surface audit (once present) + FULL_BASH_12 at unchanged counts; zero drift in product stdout/stderr/exit, DRS/IR/program/answer/guideline bytes, non-strict fixtures/labels/counts. Allowed B diff = closed set: patch 0005/vendor manifest/provenance, `spec/e--/**`, canonical migrations + regenerated Python, strict-77 fixtures, exact CI references, normative docs; checker = `git diff --name-only` vs the unit's committed allowlist, `tests/fixtures/**` (non-strict) + `guidelines/**` byte-identical.
- D9 A carrier: `tools/acceptance/{runner,parity,full,ci_refs}.emm` — deterministic suite/case registration, exact PASS/FAIL/SUMMARY, registered=executed=passed=expected, capability-bounded ops, byte/tree/seed-mutation/perturbation-repeat assertions, infrastructure-vs-subject outcomes, optional sorted C evidence records; exports compile-time linked; every suite standalone. No framework (pytest/lit/Bats/TAP/DejaGnu/Sharness), shell strings, raw subprocess/path/env, discovery, XFAIL, or parallel execution.
- D10 PARITY (port-time only; git history = the durable legacy record — `git show <origin-commit>:tests/<harness>.sh`): per port commit `spec/acceptance/parity/<suite>.pl` — legacy origin commit/path/SHA-256; one row per legacy PASS label exactly once; derived case IDs + assertions carried (restating what the Bash gate checked); every derived case referenced or `NEW(reason)`; declared byte/tree authority paths; the single permitted normalization = one scratch-root token substitution w/ exact occurrence + byte-span checks. `PARITY(suite)` = `python3 -P tools/acceptance/parity.py --map spec/acceptance/parity/<suite>.pl --runs 2` (two paired runs total: legacy then derived on the same clean tree, repo-snapshot identity between runs; compares harness outcome, mapped labels, pass counts, mapped byte/tree assertions vs named authorities, both-side subject streams) + MAIN live rerun before approving deletion. Staged partitions (ir-run, guideline): same schema + `partition(core)` field at `spec/acceptance/parity/{ir-run-core,guideline-core}.pl`, same command, non-deleting (Bash stays authority); the closure unit's complete map supersedes. Post-delete standing authority = the derived suite's own committed gates re-pinning the same fixture/golden bytes 1:1; no frozen-record comparison claimed. Seeded reds = positive rejection assertions (exact seed digest/offset/count, outcome/streams, no forbidden delta/temp).
- D11 A order + CI-reference authority: waves = pipeline-cli proof → disjoint {ape-vendor, ir-validate, strict, registry, regen} → {adapter, ir-lower, pipeline} → slice → ir-run staged ×2 → guideline staged ×2 last; every non-staged port swaps CI + deletes Bash in-unit after MAIN parity rerun. `spec/acceptance/ci-references.pl` = exact transition inventory: vendored unittest literal, strict total, bootstrap source/output/cmp, regen command, every direct suite command + aggregate membership, `bash -n` list, shellcheck list, APE/Clex/e-- manifests, SWI/crypto asserts, cleanliness, every `docs/regen.md` literal. Each B/A unit updates exactly the rows it changes same-unit (or proves all unchanged).
- D12 thin CI trust root: final YAML = provider setup + BOOTSTRAP + `host.py` + `prolog.py` invocations only. BOOTSTRAP = declared provider_bootstrap TCB breaking the compiler self-check circle — never moved into derived aggregates, never described as acceptance leakage.
- D13 C carrier: `spec/prolog/{manifest,pending,lifecycle,laws/**,tables/**}` = canonical data-only Prolog records, strict `read_term/3` options + canonical fixed point, never consult. ACE only where honestly expressible (replayed through pinned APE → expected DRS/IR digest); micro-CNL only for finite table rows (round-trip to exactly one typed row); procedural/order/effect laws = controlled English, no false executable equivalence. Selected rows deterministically generate `src/prolog/generated_spec_tables.pl`, consumed by a small hand interpreter.
- D14 coverage algebra: T = all discovered in-scope targets (semantic clause/group, table row, normalized error/invariant site, interface, exact executable authored boundary); R = resolved `(law_id,binding_id,target_id)` triples over active rows; X = structural/declared-TCB records w/ finite reason + alternate evidence; P = exact map target→(future_unit,current_digest), every pending target → exactly one future unit. Interim: `T = resolved(R) ∪ X ∪ P` disjoint + `R = E_pairs` (+ named evidence-case declared=started=completed=passed); closure adds `P = ∅`.
- D15 granularity + anchors: finite relations = exact table-row keys, bidirectional set equality; dispatch/ordered rejection = branch/clause group + each normalized (class,detail)/invariant site; recursion = predicate or coherent mutual group + invariant sites; entrypoints = interface rows. Module/class/line-range/clause-ordinal/regex-proximity never satisfies. `% @law-group <id>` binds the exact following top-level clause/group or smallest marked subterm (duplicate/orphan/intervening/ambiguous reject); digest = SHA-256 over canonical AST (vars by first occurrence; formatting/comments ignored; structure changes invalidate).
- D16 lifecycle (append-only): active | `superseded_by(NewID,final_digest,final_evidence)` | `retired(reason,final_digest,final_evidence)`; same-law refactor = binding revision old→new digest + review evidence; deleted/merged targets, changed table rows, renamed evidence = explicit transition records. Coverage ranges over active rows; history immutable + validator-visible. Append-only proven against git history: `spec_coverage.py --mode history --base <ref>` = every base-present law/lifecycle/binding row byte-identical in current, additions = appends only (CI base = merge-base w/ main; local default = origin pin in `spec/prolog/lifecycle-origin.pl`, established at C0b). This is how M6.5 retires claim⇒residual etc. — "additive" = append lifecycle records.
- D17 C order: C0 carrier+P → bind stable tiny modules/tables only → 3 behavior-preserving centralization units + NAF normalization (affected targets stay P throughout) → post-refactor binding unit → lowering ×4 → kernel → replay → CLI/adapter → registry ×3. UTF-8 decoder dedup stays out: one shared semantic law + per-caller error ownership as exact operational survivors.
- D18 classification: `spec/derivation/tracked-files.pl` + `python3 -P tools/acceptance/derivation_audit.py --manifest … --check` = set equality vs NUL-safe `git ls-files`; every path exactly one class ∈ {derived, source, spec, authored-boundary, config} + role rows; unclassified/duplicate = red. `tests/prolog/m6u3_review_gates.pl` ≠ passive fixture: M9.18–19 port all four exports (`quantity_matrix/0`, `replay_matrix/0`, `answer_self_check/0`, `resource_boundary/0`) to derived declarative cases + delete it; an irreducible check → exact authored-boundary + C law/evidence + charter rationale — silence impossible. `.agent/context-gauge.sh` = authored-boundary `out_of_product(session_instrumentation)`.
- D19 oracle units: exactly the two false-negative roots — M9.1 checker + M9.23 C0a core (MAIN implements; `orc` builds the identical contract in its worktree; `diff` compares accepted/rejected sets + emitted diagnostic/artifact bytes as committed scripts; divergences = review targets; the primary tree ships).
- D20 docs/memory: new normative `docs/derivation.md` (graph/TCB/classification/capability statement) + `docs/semantic-spec.md` (C records/targets/algebra/lifecycle/evidence); memory update at C0b + closure refresh; project CLAUDE.md gains 2 authoring rules at closure (every acceptance gate E---authored/derived; every new/changed Prolog law lands w/ same-unit spec+evidence row or lifecycle transition). `docs/research/derivational-coverage.md` stays nonnormative.

### Gate vocabulary
- BOOTSTRAP = strict-compile `tools/regen.emm` → fresh scratch dir (local: `mktemp -d`, removed after; CI: `$RUNNER_TEMP`) + `cmp` vs committed `tools/regen.py` + `tools/regen.py --check`.
- CI_REF = `spec/acceptance/ci-references.pl` conformance: strict-harness case (≤M9.6) | `python3 -P tools/acceptance/ci_refs.py --spec spec/acceptance/ci-references.pl --check` (M9.7+).
- FULL_BASH_12 (≤M9.6) = BOOTSTRAP + the 12 literal `bash tests/<suite>-harness.sh` at committed pass counts, clean finish.
- FULL_MIXED_12 (M9.7–21) = BOOTSTRAP + CI_REF + `python3 -P tools/acceptance/full.py --inventory spec/acceptance/ci-references.pl` — executes each inventory row as the current exact Bash|derived command; membership never glob-discovered.
- DERIVED_12 (M9.22+) = BOOTSTRAP + CI_REF + `python3 -P tools/acceptance/host.py` + `…/prolog.py` + `…/derivation_audit.py --check`; prolog aggregate gains SPEC_CORE at M9.23, `spec_coverage.py --mode current` at M9.24.
- PARITY(suite) = the D10 command (`--runs 2` = two paired runs total) + MAIN live paired rerun pre-deletion; port-time only.
- SPEC_CORE | SPEC_SELF | SPEC_UNIT(M9.N) | SPEC_CLOSURE = `python3 -P tools/acceptance/spec_core.py --corpus spec/prolog --check` | `spec_coverage.py --mode self` | `--mode unit --unit M9.N` (validates that unit's assigned P rows + every target it introduces/changes) | `--mode closure` (full algebra, `P = ∅`, lifecycle/evidence/certificate fixed point + `--mode history`).

### Units
B — close E-- semantic escapes (every unit additionally: strict=77 + CI_REF + FULL_BASH_12 + D8 closed-diff; surface audit once present):
- M9.1 OPEN oracle (85→136K H) — patch-0005 checker + compile-time linker + exactly-one namespace + D1 strict transition (77 exact) + `spec/e--/legacy-surface.pl` baseline (site IDs, 338+36+projections) + read-only scanner + initial ci-references.pl strict case. Gate: oracle agreement over compiler/import/collision corpus (diagnostics + emitted bytes); baseline pins; patch reapply/manifest/provenance; zero product drift.
- M9.2 BLOCKED(M9.1) (90→144K H) — 45 production constructs + flow types/joins/projections + templates/prelude + `spec/e--/capability-profiles.json` + acquisition constructs/manifests. Production sources stay exact-frozen legacy users. Gate: registry=45; profile canonical/duplicate-key/namespace/capability reds; per-family vendored checker+runtime tests; hostile-cwd standalone output.
- M9.3 BLOCKED(M9.2) (75→120K H) — migrate `pipeline.emm` + `ace_front_end.emm` (every call/import/projection → ledger disposition); generated bytes only via regen. Gate: audit zero-escape both tools, all sites disposed; product outcomes + DRS/chain/golden bytes unchanged.
- M9.4 BLOCKED(M9.3) (75→120K H) — migrate `regen.emm`; complete all dispositions; delete compat resolver/freeze; surface audit permanent. Gate: permanent audit = complete baseline + zero raw import/call/projection; bootstrap/check/regenerate fixed point; unit diff ⊆ D8 closed set (fixtures/goldens byte-identical).
- M9.5 BLOCKED(M9.4) (90→144K H) — complete D5 algebra + `scratch_dir`/`stub_tool`/`signal_process` + digest-pinned `stubtool.emm` + closed StubSpec incl. probe actions + migration-only legacy-Bash cap. Gate: registry=48; env/argv/cwd/stdin/FD/process-group/timeout/rlimit/outcome controls via stub probes; stub matrix (argv/state/bytes/exit/self+parent-signal/mutation); malicious-spec/path/mode/digest/fresh-executable reds; no Path→ToolCap.
- M9.6 BLOCKED(M9.5) (85→136K H) — remaining 19 fs/tree/regex/glob constructs; machine-enforced read-only-repo vs writable-scratch; registry frozen. Gate: 45+22=67 exact; deterministic entry order/snapshot metadata; two independent PDF-slice paths (`read_slice` | `slice_value`∘`read_bytes`); symlink/no-follow/copy/mode/atomic/containment/regex-dialect/glob controls. A new-primitive request later ⇒ A blocks.
A — replace all Bash acceptance authority (port-unit gate shape: PARITY(suite) + MAIN rerun → delete that Bash harness + swap CI same-unit; every unit additionally: BOOTSTRAP + CI_REF + FULL_MIXED_12 + in-unit CI-reference row updates incl. bash-n/shellcheck lists + docs/regen literals):
- M9.7 BLOCKED(M9.6) (85→136K H) — `tools/acceptance/{runner,parity,full,ci_refs}.emm` + runner self-test; parity-map schema + normalization; mixed inventory; infra-vs-subject outcomes; optional C evidence records. No port. Gate: self-controls incl. skipped/duplicate/unexecuted/pass-count reds; byte/LF/tree/seed/repeat + timeout/limit controls via stub probes; parity-map completeness/normalization reds; standalone `-P` under hostile cwd/PYTHONPATH; `ci_refs.py` ≡ legacy strict case; FULL_MIXED_12 over the one-time all-Bash inventory ≡ the 12 direct Bash outcomes.
- M9.8 BLOCKED(M9.7) (80→112K M) — pipeline-cli proof port: 17 gates (argv/call-order StubSpecs, symlinks, bracketed names, stale staging, path/stage errors, hash/tree/repo preservation) + complete correspondence map.
- M9.9 BLOCKED(M9.8) (55→69K S) — ape-vendor port: 10 gates; explicit former-pipefail stage outcomes; shared named APE build-output cap.
- M9.10 BLOCKED(M9.8) (75→105K M) — ir-validate port: 122 gates (12 green + 86 committed reds + generated UTF-8/BOM/CRLF/truncation matrix); exact raw bytes/class/full lines.
- M9.11 BLOCKED(M9.8) (80→112K M) — strict port: post-B 77 corpus + runtime normal/`-O`, cwd/locale/TZ/hash perturbation, atomic output, quarantine, CI-reference case; runner never imports compiler impl; YAML BOOTSTRAP still precedes derived runner.
- M9.12 BLOCKED(M9.8) (90→126K M) — registry port: 94 gates; 1.4MB source digest/ranges via both slice paths; exact repo-status identity.
- M9.13 BLOCKED(M9.8) (75→105K M) — regen port: 16 cases (isolated-Git/non-fail-fast/chmod-000/hidden/drift/missing/orphan/unauthorized/conftest/zero-write/temp/bootstrap); independent BOOTSTRAP retained in YAML.
- M9.14 BLOCKED(M9.9) (90→126K M) — adapter port: 80 gates (18 greens, UTF-8/locale reds, Ulex matrices, fake parser/load/noise/attributed/blob boundaries, vendor clean); runtime-generated executable Prolog fakes → fixed tracked authored-boundary fixtures w/ digests; no E---written executable.
- M9.15 BLOCKED(M9.10) (85→119K M) — ir-lower port: 151 gates (38 greens ×2, 100 committed reds ×2, dispatch inventory, scratch canonical/UTF-8/usage); exact stage/class/full-line/streams.
- M9.16 BLOCKED(M9.9) (80→112K M) — pipeline port: 28 gates (shared fixtures, staged APE/Clex, front-end tree, committed reds); five SWI anomaly/mutation scenarios as closed StubSpecs; zero-publish; no generated shell.
- M9.17 BLOCKED(M9.14,M9.15,M9.16) (85→119K M) — slice port: 29 gates (exact inventory, staged APE/Clex hash, 4 full chains ×2, stage-state diagnostics, lower-stop/no-nonempty artifacts, vendor clean); exact tree/stage/byte determinism.
- M9.18 BLOCKED(M9.10,M9.15) (90→126K M) — ir-run core staging: slice/lower chains, 38 compiled chains, 17 program greens, n-ary, answer/proof/order/digest/stage parity + `spec/acceptance/m6u3-review-disposition.pl` + port `quantity_matrix`/`answer_self_check` to derived cases. Bash + helper stay authority. Gate: core-partition map `spec/acceptance/parity/ir-run-core.pl` via the D10 command non-deleting ×2, no-overlap/exhaustive final-partition plan; legacy ir-run 258 + helper green (staging command = unit gate only, not CI).
- M9.19 BLOCKED(M9.18) (90→126K M) — ir-run adversarial closure: 70 reds, proof/digest/replay/self-check/invariant tampers, direct goals, memory/time/resource boundaries; port `replay_matrix`/`resource_boundary`; complete all 258 map rows; delete `tests/prolog/m6u3_review_gates.pl` + Bash. Gate adds: `Signaled`≠`Timeout`≠`LimitKilled` exact; process-group cleanup; disposition set-equality over all four exports (derived | explicit C-pending authored-boundary only); helper removed from tree + inventories.
- M9.20 BLOCKED(M9.9,M9.10,M9.11,M9.12,M9.13,M9.14,M9.15,M9.16,M9.17,M9.19) (90→126K M) — guideline core staging: ACE/Ulex/golden inventories, registry/terminology/mapping/Ulex/digest ties, staged APE/Clex, pipeline ×2/50-file tree, manifest/claim/answer/rule/query/16-region/coverage/residual ties. Bash stays authority. Gate: core-partition map `spec/acceptance/parity/guideline-core.pl` via the D10 command non-deleting ×2 + final-map plan; legacy guideline 45 green.
- M9.21 BLOCKED(M9.20) (90→126K M) — guideline fault/recovery closure: SIGKILL-parent/partial-staging/stale-refusal/recovery, terminology-row removal/Clex-independence/OOV zero-publish, staged/vendor/repo immutability; complete all 45 rows; delete final acceptance Bash + entire bash-n/shellcheck CI step. Gate adds: parent-signal StubSpec; exact surviving staging/recovery bytes/tree; FULL_MIXED_12 w/ zero test `.sh`.
- M9.22 BLOCKED(M9.21) (80→112K M) — A closure: host/prolog aggregates; YAML → provider setup + BOOTSTRAP + 2 invocations only; `spec/derivation/tracked-files.pl` + derivation_audit; remove legacy-Bash cap; finalize docs/regen + parity-map inventory. Gate: YAML exact (no semantic inline asserts/continue-on-error/Bash lint); tracked-file set equality, every path classified; all parity maps complete; zero Bash capability; DERIVED_12 at literal totals ×2; clean tree.
C — machine-tie all sanctioned hand-Prolog laws (every unit additionally: SPEC_SELF + SPEC_UNIT(unit) + DERIVED_12; product bytes unchanged unless stated; targets transition only via D16 records):
- M9.23 BLOCKED(M9.22) oracle (85→136K H) — C0a: `spec/prolog` v1 grammar/manifest/pending/lifecycle skeleton; safe reader/canonicalizer; source/anchor/AST target discovery; authored-boundary inclusion; exact P assignment (every target → exactly one of M9.24–40); `spec_core.emm`. Gate: oracle agreement (T/digests/P triples/parser errors/certificate bytes); SPEC_CORE + positive controls over `tests/fixtures/spec-coverage/`; interim algebra seed R/X/P; parser never executes directives; spec_core.py joins prolog aggregate.
- M9.24 BLOCKED(M9.23) (85→136K H) — C0b: R/E-pair records + named-case completion, survivor preconditions, lifecycle validator + `--mode history` + `spec/prolog/lifecycle-origin.pl`, deterministic certificate, validator self-spec/evidence, full red matrix (undefined/duplicate/ambiguous/orphan/unexercised/stale/new-row/error/malformed/false-survivor/pending/self-uncovered), `spec_coverage.emm`; `.agent/memory.md` authority-map update. Gate: SPEC_SELF; aggregate member core → `--mode current`.
- M9.25 BLOCKED(M9.24) (75→120K H) — bind `ir_to_prolog.pl` + `drs_canon.pl` + stable finite table seeds (branch/bound/temporal/comparison/template rows → corpus + `generated_spec_tables.pl`); `validation_common` stays P(M9.30). Gate: exact table-key equality + generated-table fixed point; every tiny-module clause tied | exact survivor.
- M9.26 BLOCKED(M9.25) (90→144K H) — validator centralization A: envelope/section-layout/section-state/item-shape/constructor shared core + IR/program adapters; old targets → M9.26 transitions, new stay P(M9.30). Gate: exact accept/reject/error bytes both surfaces; header/query-goal/fact-clause/source-asymmetry mutants.
- M9.27 BLOCKED(M9.26) (90→144K H) — centralization B: identity/ID-order-origin/branch-coherence/section-key/scope/dense-variable machines + adapters. Gate: exact density/payload/order/source walls; name-only/reorder/source-erasure/variable mutants.
- M9.28 BLOCKED(M9.27) (90→144K H) — centralization C: safety/declarations/labeled-exceptions/dependency-edges/signed-cycle ownership + adapters; banked centralization complete; targets stay P(M9.30) through NAF. Gate: exact priority walls; transitive/self-cycle/declaration-label mutants; one-shared-implementation scan + named asymmetries.
- M9.29 BLOCKED(M9.28) (85→136K H) — bare/labeled NAF normalization: one optional-label algebra/deconstructor across lowering/shared validator/kernel/explanation; label-only declarations + distinct certificate constructors retained; P remap → validator M9.30, lowering body M9.33, lowering output M9.34, kernel M9.35, replay M9.36. Gate: bare+labeled bytes unchanged; polarity/vars/stratum/evidence/substitution mutants; ir-lower/validate/run/slice/guideline suites.
- M9.30 BLOCKED(M9.29) (85→136K H) — post-refactor binding: final `validation_common.pl` + `ir_validate.pl` + kernel program-validator prefix (all 3 centralized partitions, exact adapters/asymmetries, optional-label validation, every normalized error/invariant pattern + named evidence). Gate: these P partitions → 0 w/ R=E_pairs; exact mutation evidence.
- M9.31 BLOCKED(M9.30) (85→136K H) — lowering A: `drs_to_ir.pl` envelope/document/preflights/final-question/domain + ordered root dispatch/action-alternative ownership; constructor recognizers stay P(M9.32). Gate: every branch + normalized error detail; first-owner/question/domain/dispatch mutants.
- M9.32 BLOCKED(M9.31) (90→144K H) — lowering B: quantity/temporal/window/measurement/copula/property/predicate/relation recognizers + occurrence/ownership profiles + finite rows. Gate: exact branch/error/table/evidence equality; constructor near-miss mutants; ir-lower 151 + downstream bytes.
- M9.33 BLOCKED(M9.32) (90→144K H) — lowering C: consequents, bounded DNF (saturating cap/order/Cartesian/domain propagation), heads/body groups, binder/quantity ordering, property normalization, NAF DRS profiles (incl. NAF P). Gate: cap-before-materialization/order/branch-ID/provenance/binder mutants.
- M9.34 BLOCKED(M9.33) (80→128K H) — lowering D: yes-no/scalar/copular/wh questions, referent/event accounting, source anchors, counters/IDs, defined-target declaration synthesis, section/order/backstop; close all `drs_to_ir` P. Gate: all remaining normalized lower errors; occurrence/anchor/counter/order/backstop mutants.
- M9.35 BLOCKED(M9.34) (90→144K H) — kernel runtime: strata, install/teardown, outcome/wh order, certificate cap/memo, snapshot fixpoint, first-witness store, quantity/NAF evidence, matching/binding/substitution/groundness/dedup; close kernel+NAF P. Gate: snapshot/store/witness/stratum/cap/wh/quantity/teardown/resource mutants; exact ir-run limits/results.
- M9.36 BLOCKED(M9.35) (85→136K H) — `explanation.pl`: layouts/order/proof cardinality/alternatives, retained witness, evidence algebra, recursive replay, quantity truth/store absence, substitutions/groundness; close replay/NAF P + all 21 invariant patterns. Gate: build/replay evidence pairs; proof/order/binding/label/store mutants.
- M9.37 BLOCKED(M9.36) (85→136K H) — `ir_tool.pl` + `adapter.pl`: command/stage/buffering, raw digest/generated fixed point, quote/error fallback, strict decode/parser framing, APE/Ulex load/messages/EOF/stdio quarantine/shape; decoder implementations = exact survivors under one shared semantic law. Gate: every command/quote/error/load/invariant branch tied; invalid-octet/parse/noisy-load/zero-write mutants.
- M9.38 BLOCKED(M9.37) (90→144K H) — registry A: `registry_tool.pl` CLI/reader/strict-UTF-8/canonical/error framing, registry rows/schemas/enums, references/duplicates/order, shared scalar/path/digest/Unicode laws; mapping + terminology stay P. Gate: exact schema/enum/error/ref/order equality; UTF-8/canonical/source-range/missing/duplicate/order mutants.
- M9.39 BLOCKED(M9.38) (75→120K H) — registry B: terminology grammar/order/references, 27 Ulex templates, gender/signature/closed vocabularies, generated rows; mapping stays P(M9.40). Gate: exact template/enum/table/evidence equality; Ulex malformed/duplicate/order/projection mutants.
- M9.40 BLOCKED(M9.39) (80→128K H) — registry C: mapping documents/regions/claims/items/answers/residuals, 4 row schemas, 12 residual atoms, refs/duplicates/order/coverage, M6-lifecycle-ready claim/residual laws; close the original ten-module P. Gate: all original hand-Prolog target P=0; exact mapping/coverage/evidence equality; claim-residual/region mutants.
- M9.41 BLOCKED(M9.40) (70→98K M) — closure: docs truth-alignment (`docs/{derivation,semantic-spec,strict-profile,regen,provenance,ir,registry,ace-front-end,pipeline,ulex}.md`); final classification/residual/capability statement; lifecycle audit; `.agent/memory.md` refresh; project CLAUDE.md 2 authoring rules; roadmap status. Gate: SPEC_CLOSURE (`P=∅`, R=E_pairs, named cases complete, `--mode history` clean, survivors live); validator self-applied + all positive controls; surface audit zero escape; registry=67; every parity map complete; tracked-file set equality, no unclassified path; exact YAML provider_bootstrap/aggregate contract; DERIVED_12 ×2; all generated artifacts fixed; clean tree.

Parallelism: M9.9–13 disjoint after M9.8; M9.14/M9.16 after M9.9, M9.15 after M9.10, M9.17 after all three; M9.18 may overlap M9.17. B serial (one patch/registry/profile evolves; test-family fan-out behind one patch owner). C serial (cumulative transitions/digests/P) apart from oracle worktrees + in-unit source-vs-probe fan-out; CI YAML/ci-references/docs/corpus-manifest merges single-owner at each close.

## Seeds (banked)

### M5 + M7 (registry, /goal driver)
- Research (2026-07-18): `docs/research/guideline-sources.md` = vetted source catalog + per-artifact rights + registry row schema; CDC 2022 opioid shipped as the M5 first guideline; next-source candidates = WHO SMART ANC CC0 repo subset as differential oracle, then a WHO immunization single-schedule slice. `docs/research/evaluation-methods.md` = layered acceptance gates (claim_completeness = 1, exact-IR match over behavioral verdicts, proof replay, fresh-process hash equality, degeneration probes w/ positive+negative controls).
- Durable state before automation: guideline registry (URL/version/date, content digest, licensing/redistribution status, may-commit flag), mapping store, item states + blocked reasons; M5 proved one manual idempotent round (interrupt → stale refusal → operator cleanup → byte-identical recovery; `tests/guideline-harness.sh`). M7 wraps in subagent fan-out: parallel source research, serialized registry/ACE/compiler writes; no mid-run user input → escalations become BLOCKED proposals.
- Vehicle (user decision 2026-07-30): `/goal` ships as a versioned command file whose session orchestrates subagent fan-out directly (per `/session-prompt`'s execution map). No vehicle offers engine-side run persistence → the repository-files-only contract below is unchanged; every invocation starts fresh from repository state.
- M7 persistence contract = **repository files only**:
  1. **State:** all round state in-repo = guideline registry + mapping store + item states/blocked reasons.
  2. **Round:** work selection = deterministic function of registry state; intake persists each fetched source as an immutable content-addressed record (URL, observed version/date, SHA-256, bytes or digest-verified locator, licensing observation) BEFORE any generative processing; changed remote content ⇒ new versioned item, never mutation of a prior item; writes atomic (temp + rename; registry last).
  3. **Recovery:** content-digest-keyed items + idempotent transitions; restart reconciles staged/orphaned artifacts against the registry (adopt or discard deterministically); rerun after any interruption ⇒ no duplicate or lost work.
  4. **Escalation:** no mid-run user input; licensing/semantic decisions become `BLOCKED` proposal registry entries.
  5. **Termination:** quiescent = every item ∈ `{DONE, BLOCKED}` ∧ frontier empty ⇒ pause; report non-success while BLOCKED proposals pend; resolving a proposal returns its item to runnable. Complete = every known guideline DONE or user-approved terminal exclusion ∧ frontier empty.
  6. **Invocation:** `/goal` = required in-session completion driver over the saved command definition; every invocation starts fresh + resumes solely from repository state; repository files = the only persistence.
