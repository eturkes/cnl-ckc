# roadmap

Charter = `.agent/initial-prompt.md`. Pipeline: E-- → Python (glue only) | ACE → APE-fork DRS → project IR → Prolog (all clinical semantics). Derived artifacts byte-accepted; corrections via regeneration only.

## Ledger

| id | milestone | deliverable | gate | status |
|---|---|---|---|---|
| M0 | feasibility + governance | upstream pins/licenses provenance doc; APE-on-SWI-9.2.9 probe; /goal persistence contract; Clex decision | — | REVIEWED |
| M1 | E-- trusted bootstrap | vendored hash-pinned e-- fork; strict slot-free canonical entrypoint; minimal glue extensions; self-checking `.emm→.py` regen/acceptance tool | M0 | IN-PROGRESS |
| M2 | deterministic ACE front end | vendored APE fork buildable on SWI 9.2.9; isolated hand-Prolog adapter → canonical byte-stable DRS; user-lexicon mechanism; stable doc/sentence IDs | M0 (APE strategy) + M1 (glue) | UNPLANNED |
| M3 | IR contract + vertical slice | versioned IR spec; hand-Prolog DRS→IR lowering + validator; thin e2e slice ACE→DRS→IR→Prolog→answer+explanation | M2 | UNPLANNED |
| M4 | rule compiler + kernel + explanations | definite-rule+NAF subset; safety/stratification validation; deterministic justifications traced to ACE sentence IDs | M3 | UNPLANNED |
| M5 | first guideline + manual pipeline | E---generated CLI orchestrating full chain; one real guideline authored in ACE e2e; guideline registry + English↔ACE mapping store; full-chain acceptance | M4 | UNPLANNED |
| M6 | resumable /goal workflow | registry-driven idempotent dynamic workflow; durable cross-session state; bounded termination (registry + discovery frontier empty) | M5 | UNPLANNED |

## M0 — feasibility + governance (REVIEWED)

Pins/licenses/decisions → `docs/provenance.md` (sole authority).
- M0.1 DONE (main=36% 98K/272K, impl=20% 53K/272K): governance doc + contracts → commit `2832a96`.
- M0.2 DONE (main=14% 39K/272K, impl=35% 94K/272K): APE-on-SWI-9.2.9 probe; evidence banked in M2 seed → commit `24a90ee`.

## M1 — E-- trusted bootstrap (IN-PROGRESS)

Precondition M0 REVIEWED ✓. Env verified 2026-07-18: pin `da8c3b34` reachable (= upstream HEAD; repo alive, 0 issues, Apache-2.0 confirmed via API); clone at pin → 95 upstream tests pass offline BOTH `pytest -q` and `PYTHONPATH=src python3 -m unittest discover -s tests` (stdlib-only ⇒ CI needs no pip installs); tree stays clean after suite; Python 3.13.5 + uv 0.11.29 local. Scratch clone kept at `.scratch/e--` (gitignored) for reference.

Upstream facts (session-verified, explore + probes; spec.md is STALE v0.1.7 — trust tree not doc):
- Language: `Set…to`, `Do`, `Give back`, If/Otherwise if/Otherwise, While, For each…in, `Define [[f]] taking …` (+`defaulting to`, kwargs `name=expr`, `taking nothing`), `[[name]]` calls, `<…>` lists, `{…}` dicts, strings/nums/True/False/`Nothing`, parens required across operators. Missing: break/continue/imports/exceptions/classes/indexing/postfix-chains/nested-attr. `.` = DOT statement terminator; number lexes `1.5` before DOT; `a.b.c` today = parse error; `Use`/`Require that`/`Exit with` = zero collisions in upstream tests+examples (grep rc=1).
- Imports: `__init__.py` eagerly imports transpiler → normalizer+resolver (verified loaded); `anthropic` stays lazy inside both. Core lexer/parser/emitter/ast_nodes/errors import only each other + stdlib.
- Parser: statements dispatch `_statement()` (parser.py:53-147) MIRRORED by `_statement_head()` (:150-190) — new verbs enter both; `_operand()` :297-337, `_call()` :340-353 (single IDENT callee), `Call.name`/`Var.name` = bare strings (dotted fits w/o emitter change); `_block()` recurses w/o depth context.
- Emitter: 4-space indent; joins LF, no trailing LF (CLI adds one); text-mode writes (Windows CRLF risk → we write binary); no hash/set/time nondeterminism in core; TypeError fallback can double-call resolver.
- Holes (probe-verified): `Set class to 1.` emits `class = 1` (invalid Python); `01`, `"\x"`, dup params, top-level `Give back` also pass emitter → post-emission `compile()` gate required. Lexer `splitlines()` = universal newlines (\v \f \x1c-\x1e NEL U+2028/29 silently split lines); identifiers Unicode `isalpha` → Unicode-version drift; string escapes pass through raw.
- Caches `.emm_cache.json`/`.emm_norm_cache.json` CWD-relative, tracked upstream, touched only via normalizer/resolver closures — pure tokenize→parse→emit never does I/O.
- Provenance pins-table bug found: e-- tag `v0.1.0` = `3a21f0b26d3729b29c4d8cae4af465a0c26e2037` (NOT a6cb6cae; only v0.2.0 = a6cb6cae) → M1.1 corrects.

Banked design decisions (bind units):
- Fork = `vendor/e--/` in-repo; pin = upstream base commit + ordered `patches/` (git-format, re-applicable) + sha256 tree manifest in PROVENANCE. TCB-change gate 1 ("bump fork pin") ⇒ patch-set + manifest + provenance update. TCB allowlist = exact filenames: core lexer/parser/emitter/ast_nodes/errors/strict + quarantined transpiler/normalizer/resolver/cli/`__init__` (retained upstream-fidelity; strict path must never load quarantined 3 — deny-list, not "imports only": stdlib + ast_nodes/errors are legal). Upstream tests/ = vendored third-party, not first-party Python.
- Fidelity check (shell): fresh `git archive` at pin → sha256 compare vs vendor tree minus {PROVENANCE, patches/, manifest}; patched files must equal base+patches applied in order.
- `.gitattributes` (M1.1): `vendor/** -text`; first-party `*.emm *.py *.sh *.md *.yml` `text eol=lf`. Interpreter pin: `.python-version` = 3.13; goldens = 3.13 artifacts; CI = 3.13 only.
- Strict input contract: valid UTF-8, no BOM; reject ALL control chars except `\n` (incl. \r \t \v \f \x1c-\x1e NEL U+2028/29, NUL) anywhere incl. strings; identifiers ASCII; non-ASCII allowed inside string literals only; SLOT token anywhere = error (poison resolver as backstop). Post-emission gate: `compile(src,"<strict>","exec",dont_inherit=True,optimize=0)` syntax-only (catches all probe holes); fail → own stable one-line stderr class (CPython detail unpinned), exit 1.
- Strict CLI: input = file path only (no stdin); default stdout = emit bytes + exactly one trailing LF (binary); `-o FILE` = unique same-dir temp (`.tmp.`+pid) + `os.replace`, binary. Exits: 0 ok / 1 language+strict+pyvalid / 2 usage+I/O / 70 internal (catch-all except own diagnostics). Canonical invocation: `PYTHONPATH=vendor/e--/src python3 -P -m e_minus_minus.strict` (+`PYTHONDONTWRITEBYTECODE=1` in harnesses); `-P` kills CWD shadowing.
- Extension grammar: `Use IDENT (DOT IDENT)* DOT-terminator.` (1-segment OK: `Use os.`), top-level-only enforced by post-parse walk of Program.statements in parse(); dotted refs/call-targets = `_operand`/`_call` consume `(DOT IDENT)*` w/ lookahead (DOT not followed by IDENT = terminator); dotted strings ride existing Var.name/Call.name. `[[var.method]]()` = same dotted-callee path. `Require that <expr>.` → `if not (<expr>):` + `    raise AssertionError("requirement failed")` (single eval, -O-immune). `Exit with <expr>.` → `raise SystemExit(<expr>)`. New keyword phrases `("Use",) ("Require","that") ("Exit","with")` — capital-initial, bare `that/with/Exit` stay IDENT; compat break documented in PROVENANCE. Defer: aliases/from-imports/indexing/postfix-chains/exceptions/context-mgrs/classes/decorators/hints/break/continue.
- Regen contract: roots = explicit list constant in regen.emm (M1: `tools/`); fixtures live OUTSIDE roots (`tests/fixtures/`); discovery = sorted POSIX relpaths of `*.emm` under roots. `--check` (default): recompile each `.emm` in-memory → byte-compare committed `.py`; orphan `.py` in roots; unauthorized first-party Python = `git ls-files '*.py'` minus vendor manifest minus generated set must be ∅ + `conftest.py` probe under tests/. Regenerate mode: compile ALL to memory first (any fail ⇒ zero writes), then per-file atomic replace. Env failures (OSError etc.) = uncaught traceback, fail-loud, documented (no exceptions in E--). Exits: 0 clean / 1 drift+violations / 2 usage.
- Trust bootstrap (CI order): vendored suite (`cd vendor/e-- && PYTHONPATH=src python3 -m unittest discover -s tests` ⇒ exactly `Ran 95 tests` + `OK`, no skip token) → `git diff --exit-code -- vendor/` → strict-compile `tools/regen.emm` → `cmp` vs committed `tools/regen.py` (breaks self-check circularity) → run committed `regen.py --check` → fixture harnesses. All shell + strict CLI + cmp/sha256sum; NO inline `python -c`/heredoc Python anywhere; planted-conftest red fixture created by harness `printf`, never committed as `.py`. Actions pinned by full SHA; post-provision offline (zero pip).
- Import-isolation gate (shell, no hand-Python): `-X importtime` run → grep stderr for `normalizer|resolver|anthropic` ⇒ 0 hits.
- Extension acceptance = behavior-first: red fixtures assert generated-`.py` runtime stdout/exit via shell harness; goldens recorded from compiler AFTER behavior passes; goldens = byte-regression only, never hand-authored.

Units:
- M1.1 OPEN — vendor: `git archive` upstream at pin → `vendor/e--/` verbatim (incl. tracked caches/docs/tests/examples; LICENSE retained); `vendor/e--/PROVENANCE` (URL, base pin, patch policy, TCB file allowlist + roles, sha256 manifest, fidelity procedure); empty `patches/`; root `.gitattributes` + `.python-version`; fidelity check green; suite gate green in-tree from `vendor/e--`; provenance.md: vendor status → vendored, fix v0.1.0 tag line, record fork-pin interpretation (base+patches+manifest). Acceptance: sha256 manifest matches fresh archive; 95/95 OK; `git diff` clean post-suite.
- M1.2 OPEN — strict entrypoint: `docs/strict-profile.md` (input contract, CLI table, exits, diagnostic classes — spec'd above); patch 1 `__init__.py` PEP 562 lazy (kill eager transpiler chain); patch 2 add `strict.py` (TCB): validate→tokenize→parse→emit→compile()-gate→bytes-out; green goldens + red fixtures (slot, CRLF, tab, \f, NEL, U+2028, BOM, non-ASCII ident, control-in-string, bad syntax, keyword-target `class`, `01`, `"\x"`, dup params, top-level Give back → stable class + exit code each); determinism harness (perturb CWD/locale/TZ/PYTHONHASHSEED ⇒ byte-equal; import-isolation grep); upstream 95 still green; patches enumerated + manifest/provenance updated. Sole hand-Python = strict.py + `__init__` patch, in-TCB.
- M1.3 OPEN — extensions in TCB (red-first): grammar/emission per banked decisions; patches to lexer/parser/ast_nodes(if node needed)/emitter minimal + enumerated; fixtures: `Use os.`+`Use os.path.`, nested-`Use` rejected, dotted ref/assign-target?(NO — dotted assignment stays unsupported, fixture asserts rejection), `[[pathlib.Path]]`, `[[fh.read]]`, `[[result.stdout]]` chain via intermediate Sets, Require pass/fail (+`-O` immune), Exit with code/string, terminator ambiguity (`Set x to a.b.`), behavior harness green → goldens recorded; upstream 95 green; strict red set rerun green. Compat-break note in PROVENANCE.
- M1.4 OPEN — regen tool + CI: author `tools/regen.emm` (extended E-- only; argparse/pathlib/hashlib/subprocess/os via `Use`; per regen contract above) → bootstrap once via strict CLI → commit byte-accepted `tools/regen.py`; fixed point: regen recompiles itself byte-identical; drift/orphan/unauthorized/planted detections each exercised by shell-harness red case (temp copies, printf-planted files); `.github/workflows/ci.yml` per trust-bootstrap order, SHA-pinned actions, offline post-provision; memory.md upkeep. Acceptance: `--check` exit 0 on clean tree; each red case exit 1 w/ path in report; CI yml lints (actionlint if available, else visual) + full local run of every CI step green.

## Seeds (banked research, 2026-07-18)

### M2
- APE nondeterminism found: global message store gates output (`drs([],[])` on any error); `copy_term` fresh vars → serializer must own canonicalization (`numbervars` in `drs_to_ascii` is deterministic but unquoted → prefer own serializer w/ fixed quoted/canonical options); durations in `get_ape_results` multi-output → bypass, call direct parser API from narrow hand-Prolog adapter; guessing off; fresh SWI process per compilation item; no timings/banners/paths in accepted output.
- Sentence IDs threaded text-wide (StartID monotone); condition annotations = `-SentId/TokId` relative ordinals → durable provenance = doc ID/source digest + sentence ordinal, never raw ordinals alone.
- Python side (M1 glue) starts process + moves bytes only; never inspects DRS semantics.
- SWI 9.2.9 probe @ APE `5f4d5354a45fb772763bf1a9543f508f15b28982`: `make install` builds runnable `ape.exe` (485064 B) with no warnings/errors; source CLI (`swipl -f ape.pl -g ape -t halt -- …`) and direct source load both parse with no load diagnostics. Direct adapter entrypoint = `ace_to_drs:acetext_to_drs/8` (`Guess=off`, `Catch=off`; own message/error policy + serializer).
- User lexicon verified: CLI `-ulexfile FILE`; direct adapter `ulex:discard_ulex/0` then `ulex:read_ulex/1` on a UTF-8 stream; `noun_sg(zorbomat,zorbomat,neutr).` makes `A zorbomat waits.` parse while the bundled Clex has no `zorbomat`.
- Fixture anchor (fresh SWI each): `John waits.` → `drs([A],[-(predicate(A,wait,named('John')),/(1,2))]).`; determiner+relative-clause, if-then, and user-lexicon fixtures also produced non-empty DRSs.
- Determinism: two fresh-process pairs (`John waits.` and relative-clause fixture) each `cmp` byte-identical under fixed quoted/canonical serialization (SHA-256 `42964785…` and `6cc328c3…`); accepted direct output contains no time/path/banner. Upstream `get_ape_results` multi-output is not byte-stable solely because `<duration parser=…>` changed (`0.007`→`0.005`; `cmp` 1).
- Upstream regression: documented full-Clex prerequisite + `test_ape.pl` completed in 7.38 s, exit 0, all 3733 correct (`----` 2813, `0000` 920). Its downloader targets moving Clex `master` (probe SHA-256 `2996fabf…`); without that 3.38 MB prerequisite, 131 cases falsely fail. Loading it over bundled Clex emits 27 static-procedure redefinition warnings.
- Patch inventory — SWI-9.2.9 MUST-FIX: none found for build/source-load/parse. Project MUST-FIX: bypass timed `get_ape_results`, own canonical serializer/error contract, and satisfy the full-Clex regression prerequisite by fetching full Clex at its provenance pin into gitignored scratch (test-only, never committed — provenance Clex scope) or curating a replacement corpus. Probe digest `2996fabf…` = `clex_lexicon.pl` at the Clex pin; Clex `master` currently == pin (review-verified), so fetch-at-pin reproduces the 3733/3733 run exactly. COSMETIC: silence regression Clex redefinition warnings and document explicit `-g ape -t halt` for source CLI.
- Strategy: source-load a narrow hand-Prolog adapter, not `ape.exe`; qsaved `ape.exe` works but is SWI/build-coupled, while the adapter exposes `acetext_to_drs/8` and owns accepted bytes deterministically.

### M3–M4
- IR contract from day one: stable doc/rule/sentence IDs; var scope+safety; explicit truth/unknown semantics; NAF representation + stratification rules; deterministic ordering/serialization; unsupported constructs = hard errors (never lossy lowering). M3 ends with thin executable slice (1 rule, 1 query, 1 explanation) to prevent IR encoding parser accidents; M4 widens subset. AceRules = semantics reference for courteous/stable-model choices.

### M5–M6
- Durable state before automation: guideline registry (URL/version/date, content digest, licensing/redistribution status, may-commit flag), mapping store, item states + blocked reasons; M5 proves one manual idempotent round (stop/restart, no dup/lost work). M6 wraps in dynamic workflow: parallel source research, serialized registry/ACE/compiler writes; no mid-run user input → escalations become BLOCKED proposals.
- Dynamic-workflow availability (verified 2026-07-18): Claude Code `2.1.211` ≥ required `2.1.154`; neither project nor user `.claude/settings.json` sets `disableWorkflows`, and `CLAUDE_CODE_DISABLE_WORKFLOWS` is unset; user confirmed the feature is enabled for their account (2026-07-18) → available, no residual gate. Official refs: `code.claude.com/docs/en/workflows`, `code.claude.com/docs/en/goal`, `code.claude.com/docs/en/skills`.
- Semantics: dynamic workflows = JavaScript scripts orchestrating subagents. Active run state is **not** durable across CLI exit; docs: exiting mid-run ⇒ “the next session starts the workflow fresh.” Definitions under `.claude/workflows/` are durable + invokable as `/<name>`, but invocation reruns the definition rather than resuming engine state. `/goal` = session-scoped Stop-hook completion loop; active goal restoration requires `--resume` of the same saved session. Agent repository writes persist normally.
- M6 persistence contract = **repository files only**:
  1. **State:** all round state in-repo = guideline registry + mapping store + item states/blocked reasons.
  2. **Round:** work selection = deterministic function of registry state; intake persists each fetched source as an immutable content-addressed record (URL, observed version/date, SHA-256, bytes or digest-verified locator, licensing observation) BEFORE any generative processing; changed remote content ⇒ new versioned item, never mutation of a prior item; writes atomic (temp + rename; registry last).
  3. **Recovery:** content-digest-keyed items + idempotent transitions; restart reconciles staged/orphaned artifacts against the registry (adopt or discard deterministically); rerun after any interruption ⇒ no duplicate or lost work.
  4. **Escalation:** no mid-run user input; licensing/semantic decisions become `BLOCKED` proposal registry entries.
  5. **Termination:** quiescent = every item ∈ `{DONE, BLOCKED}` ∧ frontier empty ⇒ pause; report non-success while BLOCKED proposals pend; resolving a proposal returns its item to runnable. Complete = every known guideline DONE or user-approved terminal exclusion ∧ frontier empty.
  6. **Invocation:** `/goal` = required in-session completion driver over the saved workflow definition; every invocation starts fresh + resumes solely from repository state; repository files = the only persistence.
