# roadmap

Charter = `.agent/initial-prompt.md`. Pipeline: E-- → Python (glue only) | ACE → APE-fork DRS → project IR → Prolog (all clinical semantics). Derived artifacts byte-accepted; corrections via regeneration only.

## Ledger

| id | milestone | deliverable | gate | status |
|---|---|---|---|---|
| M0 | feasibility + governance | upstream pins/licenses provenance doc; APE-on-SWI-9.2.9 probe; /goal persistence contract; Clex decision | — | REVIEWED |
| M1 | E-- trusted bootstrap | vendored hash-pinned e-- fork; strict slot-free canonical entrypoint; minimal glue extensions; self-checking `.emm→.py` regen/acceptance tool | M0 | REVIEWED |
| M2 | deterministic ACE front end | vendored APE fork buildable on SWI 9.2.9; isolated hand-Prolog adapter → canonical byte-stable DRS; user-lexicon mechanism; stable doc/sentence IDs | M0 (APE strategy) + M1 (glue) | REVIEWED |
| M3 | IR contract + vertical slice | versioned IR spec; hand-Prolog DRS→IR lowering + validator; thin e2e slice ACE→DRS→IR→Prolog→answer+explanation | M2 | REVIEWED |
| M4 | rule-language widening | NAF subset + stratification validation over the M3 kernel; program-digest binding in answer records; wh answers; deterministic justifications traced to ACE sentence IDs | M3 | REVIEWED |
| M5 | first guideline + manual pipeline | E---generated CLI orchestrating full chain; one real guideline authored in ACE e2e; guideline registry + English↔ACE mapping store; full-chain acceptance | M4 | IN-PROGRESS |
| M6 | semantics coverage widening | minimal IR/profile/kernel extensions converting the M5 residual inventory into executable semantics (ideal = zero residuals on the first guideline; survivors carry a recorded impossibility rationale); per-class least-intrusive-deviation decision — deterministic Prolog first, other formalisms (ProbLog-class or other) only if required | M5 | UNPLANNED |
| M7 | resumable /goal workflow | registry-driven idempotent dynamic workflow; durable cross-session state; bounded termination (registry + discovery frontier empty) | M6 | UNPLANNED |

## M0 — feasibility + governance (REVIEWED)

Pins/licenses/decisions → `docs/provenance.md` (sole authority).
- M0.1 DONE (main=36% 98K/272K, impl=20% 53K/272K): governance doc + contracts → commit `2832a96`.
- M0.2 DONE (main=14% 39K/272K, impl=35% 94K/272K): APE-on-SWI-9.2.9 probe; evidence banked in M2 seed → commit `24a90ee`.

## M1 — E-- trusted bootstrap (REVIEWED)

Governance/pins → `docs/provenance.md`; fork identity/TCB allowlist/fidelity procedure → `vendor/e--/PROVENANCE`; strict contract → `docs/strict-profile.md`; regen contract → `docs/regen.md`; executable gates → `.github/workflows/ci.yml` + `tests/strict-harness.sh` (57) + `tests/regen-harness.sh` (16).
- M1.1 DONE (main=22% 60K/272K, impl=22% 59K/272K): vendored fork verbatim at pin + manifest/patch scaffold → commit `743e90a`.
- M1.2 DONE (main=40% 109K/272K, impl=47% 128K/272K): validated strict entrypoint (patches 0001/0002) → commit `fbafd49`.
- M1.3 DONE (main=20% 55K/272K, impl=46% 124K/272K): TCB language extensions (patch 0003) → commit `eb0aadb`.
- M1.4 DONE (main=33% 90K/272K, impl=36% 99K/272K): self-checking regen tool + trust-bootstrap CI → commit `167d2d7`.
- Review: 15 findings → 11 confirmed, 4 refuted; fixes = fail-loud iterdir traversal + single-pass buffered regenerate (regen.emm), patch 0004 Apache §4(b) notices, provenance patch enumeration, .gitattributes first-party gaps, record collapse → review commit.

## M2 — deterministic ACE front end (REVIEWED)

Context = `docs/research/cnl-ace.md` (fail-closed clinical profile, lexicon policy, §7 design decisions) + `docs/research/ir-rule-semantics.md` §7 (parse success ≠ profile acceptance); full texts in `docs/references/` (README = citations/hashes/rights). Charter boundary: adapter + serializer = hand-Prolog (parser machinery); all first-party Python = E-- generated.

- M2.1 DONE (main=29% 79K/272K, impl=75% 204K/272K): APE vendor + regression CI → commit `7af31ad`.
- M2.2 DONE (main=42% 115K/272K, impl=41% 111K/272K): fail-closed adapter + canonical serializer → commit `0105517`.
- M2.3 DONE (main=25% 68K/272K, impl=40% 109K/272K): optional per-run Ulex → commit `9900e11`.
- M2.4 DONE (main=29% 79K/272K, impl=40% 108K/272K): stable IDs + front-end glue → commit `1981be8`.
- Review disposition: lenses A–D + external audit; 14 validated findings → fix batches 1 (code/harness: strict UTF-8 hand decoder because SWI-9.2.9 `library(utf8)` is unsound, locale pinning, Ulex totality, stream quarantine, glue framing classes, vendor gates — detail in the review commit) and 2 (docs truth-alignment); 3 refuted findings booked in the review commit; DEFERRED to M5 per `docs/ace-front-end.md` = producer/registry identities in records; gates = 57/16/45/27/10.
- Post-close residual audit (banked, non-blocking; shipped behavior unaffected — zero-write + nonzero exit hold in every case): gate-strength backlog = CI UTF-8 red matrix beyond 0xff (full RFC 3629 matrix probe-verified in-session only), cyclic-DRS + noisy-success-isolation fakes, further Ulex warning families, surface unknown vendor-regression result codes (PASS_COUNT totals hard-asserted in all nine harnesses since the M3 review), spawn-OSError + post-run ulex `read_bytes` tracebacks → sanitized error classes, qsave `make install` lacking `-F none` (vendor-anchor-only surface, needs vendored-Makefile patch); design-level (reaches user before any edit) = transient ulex replace-then-restore is invisible to upfront-read + post-run re-verify → true fix is an immutable snapshot handed to the adapter, which changes the adapter argv contract.

## M3 — IR contract + vertical slice (REVIEWED)

Gate M2 confirmed 2026-07-19 (five harnesses green). Plan-fixed decisions, probe evidence, and deferrals are superseded by the shipped authorities: grammars/semantics/error classes/capability matrix → `docs/ir.md`; byte authorities → `tests/fixtures/{ir,slice}/`; unit detail → the unit commits.

- M3.1 DONE (main=31% 85K/272K, impl=55% 149K/272K): IR v1 spec + validator + `ir_tool.pl` CLI + 34 fixtures + harness/CI → commit `0eb0849`.
- M3.2 DONE (main=42% 113K/272K, impl=74% 201K/272K): DRS→IR lowering, `==`-exact profile matching (aliasing counterexamples pinned) → commit `e2465d9`.
- M3.3 DONE (main=29% 79K/272K, impl=47% 129K/272K): compiler + deterministic least-model kernel + explanation/replay, program/answer v1 records → commit `d8b0261`.
- M3.4 DONE (main=27% 74K/272K, impl=24% 65K/272K): end-to-end slice harness + docs truth-alignment + CI → commit `ad8fcab`.
- Review disposition: lenses A–D + external audit; fix batches = kernel join-fusion (materialize-then-dedup cost `n^k` candidate memory → fused growing-store `nb_setarg` dedup, differential-proven under `ulimit -v`), forced-quote complete-shape proof, `generated_record_call` wrappers, non-binding `==`-decomposed ulex matcher, strict `'[|]'/2` replay checks, harness/CI strength (hard PASS_COUNTs ×9, per-PID scratch, whole-tree cleanliness, memory-bounded wide-join gate), fixture/gate coverage + docs truth-alignment; validator dedup banked; gates 57/16/45/27/10/60/34/39/25 MAIN-rerun green.
- Post-close residual audit (banked 2026-07-19 from review-lens final reports; non-blocking — shipped behavior verified correct in every case, findings are gate-strength/coverage only): ir-run `determinism/red` repeats are not byte-compared to the committed-red first rejection and lack the one-LF-line check (identical multi-line repeats would pass; the same fixture is one-line-pinned via `check_rejection`, so only a nondeterministic tool escapes); rejection gates match stage/class with free `Detail` — extend ir-validate's exact-stderr-line pattern to lower/run reds or parse+reserialize `Detail` as a canonical term (docs/ir.md error-record claim otherwise unpinned); run-stage red for a transitive two-rule cycle (program validator's `reachable/4` is the tamper boundary; only self-loop committed); admitted two-variable rule green pinning `var(1)`/`var(2)` discrimination (collapse-to-`var(1)` mutant survives and generated-IR backstop accepts it); same-sentence multi-clause green pinning clause-ID density (sentence-counter mutant survives); lower first-failure priority exact-line pins (root-domain/question-count swap survives); pass-timing/multi-round kernel green beyond competing-witness clause order; `rule_id`+empty-body identity red; committed UTF-8 boundary matrix beyond the scratch set (3/4-byte overlongs, truncations — probe-verified only, mirrors the M2 banked item); slice env-perturbation pass (HOME/TZ/umask; strict-harness precedent); `acyclic_term` + answer-shape guard before replay output (ground cyclic children would loop; certificate construction is proven acyclic, so internal-corruption-only); upgrade the chain recipe + harness from zero-non-empty to zero-artifact via temp-write+rename (recipe-compatible; docs deliberately state the honest zero-non-empty contract today); `docs/ace-front-end.md` OUT_DIR immutability wording could name front-end records explicitly since the chain later adds `.ir/.program/.result` files to the same directory.

## M4 — rule-language widening (REVIEWED)

Normative grammar, semantics, error classes, and the shipped/deferred capability matrix → `docs/ir.md`; lowering probes → `tests/fixtures/lower/{green,red}/`; end-to-end byte authorities → `tests/fixtures/slice/`; digest runtime TCB → `docs/provenance.md`; gate totals → `docs/regen.md`.

- M4.1a DONE (main=55% 151K/272K, impl=66% 179K/272K): program/answer v2, signed-cycle validation, stratified NAF kernel, absence-checked certificate leaves, exact-raw-byte program digest → commit `290d5d1`.
- M4.1b DONE (main=32% 88K/272K, impl=~42% ~114K/272K): exact `wh(who)` execution, canonical-byte answer ordering, answer self-check, `certificate_node_cap(1000000)` preflight → commit `6bcc50f`.
- M4.2 DONE (main=48% 130K/272K, impl=88% 238K/272K): IR v2 + validator + compile map; probe-pinned P01/P10 wh + P13/P15 NAF lowering; paired IR/program green/red gates → commit `9cf8af2`.
- M4.3 DONE (main=34% 93K/272K, impl=47% 129K/272K): `slice-naf` + `slice-wh` end-to-end chains, crypto provenance/CI pin, closure docs → commit `b5c7e8a`.
- Review disposition: four lenses (correctness/spec, cross-unit integration, conformance, efficiency/obsolescence) → 12 accepted findings (majors: yes/no answer self-check envelope gap + incomplete D8 pairing + stale roadmap/memory durable state; rest gate-strength/text minors), remainder verified clean; fix batch = answer self-check now enforces the v2 envelope/layout for every answer shape (yes/no included; previously only wh) with committed accept/reject probes; D8 pairing completed (8 new program reds incl. `identity-rule-empty-body`, exact-stderr-line pins on all paired reds both sides — pairs proven byte-corresponding modulo stage atom); compile-stage ownership loop (every IR red rerun through `compile`, stderr byte-equal to `validate` stderr modulo stage atom, covers v3 envelope); malformed recognized-`~` inner content now class `negation` with new lower red (cross-DRS redeclaration stays `referent`; docs state the exact split); `first_missing_var` numeric non-binding membership mirroring the kernel; dead `read_utf8_input/3` removed; pipeline second-run stdout pinned; docs heading → capability status, `answers/1` strictly-ascending/duplicate-free wording; memory refresh (authority map version-neutral, digest hashes pre-decode bytes incl. final LF, wh canonical-byte order, digests output-only); harness file modes normalized to 755; gates now 57/16/45/28/10/73/51/93/29 → review commit.
- Durable M5+ handoff: D10 labeled exception IDs + explicit closed-world declarations deferred with direction/strength; validator-pass deduplication stays banked; proof enumeration/DAG sharing stays first-witness/tree-only; digest = integrity binding, not authentication. Expanded-certificate-node/result-byte baselines: `slice` 3/584, `slice-unknown` 0/386, `slice-naf` 3/553, `slice-wh` 3/622.
- Post-close residuals (banked 2026-07-22, non-blocking; the M3 residual backlog stays open except its transitive-cycle red, which M4 shipped): aggregate resource-cap red (individually sub-cap trees breaching only in sum; exact Cap-accepted/Cap+1-rejected boundary fixtures); the three byte-identical slice ulex sidecars have no direct cross-stem cmp gate nor a divergence-permitted note; `run/red/envelope-v1-record.program.pl` deliberately retains the legal-under-v2 NAF payload + old docid to prove envelope-first rejection; harness dedup opportunities (third competing-witness run, lower-harness counting file families it never consumes, slice count-only gates subsumed by exact inventories).

## M5 — first guideline + manual pipeline (IN-PROGRESS)

### Preconditions (confirmed 2026-07-22, real inputs)
- M4 REVIEWED; nine harnesses 57/16/45/28/10/73/51/93/29 + `regen: check ok` MAIN-rerun green this session.
- Shipped-chain admission probed end-to-end on a fresh staged APE tree (23 probes, `.scratch/m5-probe/`, greens byte-deterministic ×2). Viable building blocks (all GREEN full-chain): hyphenated + digit Ulex compound nouns (`chronic-pain-patient`, `rec1-candidate`); hyphenated intransitive verbs (`has-chronic-pain`); relative-clause conjunction incl. mixed NAF (`… that waits and that does not provably smoke …`); bare `Every candidate recovers.`; derived-class chaining via iv-bridge (copula heads rejected — rule consequents MUST be intransitive verbs); extra `pn_sg` proper names; wh over derived predicates; 12-rule single documents. Hard walls (exact first-failure): copula consequent `ir_tool_error(lower,unsupported,rule(_,consequent_count(2)))`; fact-after-rule `unsupported,root_section_order` (facts must precede rules in sentence order); zero/two/non-final questions `question_count` (every document = exactly one final query); transitive verbs `copula,root_condition(_,unpaired_object)`; adjectives `unsupported,…property/3`.
- CDC 2022 opioid guideline acquired + rights-verified (`.scratch/m5-source/`): HTML `https://www.cdc.gov/mmwr/volumes/71/rr/rr7103a1.htm` fetched twice byte-identical, SHA-256 `d566cb09a3c17c6a363d9637f2caa5fa5c1d69a2a281de1d248dc7e6c9852812`; PDF twice byte-identical `f4e5098d13e9b3dc5cc27bb90137df57d3667350b2add885fd367f279402d18d`. Box 3 = 12 recommendations + 4 group headings, verbatim extract + HTML char range `[630335,638736)` recorded. MMWR public-domain statement (`https://www.cdc.gov/mmwr/about.html`): "All material in the MMWR Series is in the public domain and may be used and reprinted without special permission; citation as to source, however, is appreciated." No separately credited third-party material inside Box 3; guideline still CDC-current (no supersession banner; training page updated 2026-01-28).

### Decisions (plan-fixed)
- D1 committed acquisition artifact = the PDF manifestation (`rr7103a1-H.pdf`, 1418584 bytes, `application/pdf`, raw bytes, `.gitattributes` `-text`) at `guidelines/cdc-2022-opioid/source/` — CDC presents the PDF as official text and warns automated MMWR HTML conversion can introduce errors — plus one committed verbatim UTF-8 Box 3 extraction evidence file as the region-byte authority. HTML digest stays a registry-row cross-manifestation evidence field, not committed. Region rows cite logical region IDs (`box3.rec.01`–`box3.rec.12` + 4 group headings), the PDF page locator (physical 13–14, printed 11–12), and 0-based half-open byte ranges + region SHA-256 into the committed extraction file — never raw PDF offsets.
- D2 registry = versioned Prolog term files, house canonical bytes: `guidelines/registry.pl` (`cnl_guideline_registry(1).` + source row w/ rights evidence quote/URL/retrieved-at/status-checked-at + artifact digest + 12 region rows) and per-guideline `guidelines/cdc-2022-opioid/{terminology.pl,mapping.pl}` (`cnl_guideline_terminology(1)`, `cnl_guideline_mapping(1)`). Digest field names stay distinct from `source_sha256` (ACE bytes): `artifact_sha256`, `region_sha256`, `ace_sha256`, `ulex_sha256`. Registry v1 reserves item-state vocabulary `{done, blocked, excluded}` (terminal exclusion distinct from done, per the M7 termination contract) + blocked-proposal row constructor for M7; frontier machinery stays M7.
- D3 validation = new hand-Prolog `src/prolog/registry_tool.pl` (allowlist: validator/compiler machinery), ir_tool-mirrored framing (strict UTF-8, pinned reader, canonical fixed point, versioned envelope, first-failure classes, `registry_tool_error(Stage,Class,Detail).`, exits 0/1/2): subcommands `registry|terminology|mapping` (stdin single file, zero-stream success) + `ulex` (terminology.pl → canonical sorted `.ulex` bytes on stdout). Each file is self-contained for its validator; cross-file digest/coverage ties are harness-owned (independent `sha256sum`).
- D4 production Ulex per `docs/ulex.md`: terminology rows are the versioned lexicon registry; `registry_tool ulex` emits the canonical byte stream; committed per-doc same-stem `.ulex` files must byte-equal that emission (cmp gate); hashes recorded in mapping rows.
- D5 mapping store = claims + typed residuals per region, accounting identity `every region ⊇ (≥1 claim ∨ ≥1 residual)` machine-checked by the `mapping` pass; claims are explicit projections (population/action-kind kernel in ACE) carrying docid + `ace_sha256` + rule/query item IDs + registered expected answer term; ALL omitted normative content per region is enumerated as typed residuals — residual classes (versioned in the grammar; M5.1 fixes the exact vocabulary, split by cause): profile walls (`copula_head`, `transitive_relation`, `property`) + semantics without constructors (`dose_quantity`, `temporal`, `direction_strength`, `certainty`, `population_threshold`, `labeled_exception`, `multi_entity`, `disjunction`) + `scope_deferred`. No silent partial claims: a claim is an explicitly named projection (e.g. applicability/action-kind), legal only alongside its region's residual rows; scenario facts are execution scaffolding, never claims. Hard authoring errors (fact-after-rule, zero/multi/non-final query) are rejections, never residual classes. Residual rows carry a short verbatim-anchored detail term naming the omitted content: the inventory doubles as M6's requirements worklist (ledger M6 = semantics coverage widening), so each row must be specific enough to derive a constructor requirement.
- D6 authoring shape per recommendation = one ACE document `cdc2022-opioid-rec<NN>` (facts-first scenario grounding + iv-headed rules + exactly one final registered query), NAF/wh used where the recommendation warrants; guideline docs dir = `guidelines/cdc-2022-opioid/ace/`; accepted full-chain artifacts committed under `guidelines/cdc-2022-opioid/golden/`.
- D7 orchestrating CLI = `tools/pipeline.emm` → generated `tools/pipeline.py`, argv `APE_TREE_DIR DOCS_DIR OUT_DIR` mirroring the front end: sibling staging dir `<OUT_DIR>.tmp.<pid>` (refuse stale `<OUT_DIR>.tmp.*`, refuse existing OUT_DIR); runs `ace_front_end.py` into `staging/front/`, then per sorted docid the four ir_tool stages on fresh prior-stage bytes into `staging/chain/`; `validate` = explicit zero-stream gate; child failure ⇒ verbatim stderr relay, preserved exit status, staging removed, OUT_DIR never exists; success ⇒ root `manifest.pl` (`cnl_pipeline_manifest(1).`, per-doc rows hashing all five artifacts incl. the inner front-end manifest) then one `os.replace` publish. Python moves bytes only; no Prolog parsing.
- D8 acceptance = three new harnesses: `tests/registry-harness.sh` (ape job; registry_tool green/red fixture gates), `tests/pipeline-cli-harness.sh` (test job, offline; stub-SWIPL orchestration matrix — usage/missing-dirs/existing-out/stale-staging/child-failure-relay/zero-write + canned-bytes green publish), `tests/guideline-harness.sh` (ape job; fresh staged APE, registry+terminology+mapping validation, ulex regen cmp, pipeline ×2 byte-identical vs committed goldens, mapping expected answers vs fresh result bytes, kill-mid-run ⇒ OUT_DIR absent ⇒ rerun byte-identical no-dup/no-loss, coverage arithmetic recheck, one Clex-independence red (remove a required compound lexeme from a terminology copy ⇒ APE OOV rejection, proving docs don't silently ride the bundled Clex), vendor cleanliness). CI: test job lints all + runs pipeline-cli; ape job runs registry + guideline after slice.
- D9 evaluation scope stays the minimal ledger subset: claim-completeness identity + expected-answer equality + determinism + proof replay (already kernel-enforced). No conflict detection, no research battery/thresholds (scope guard).
- D10 M4 residual disposition: absorb only the slice ulex-sidecar divergence-permitted note (touched docs anyway); keep banked: aggregate resource-cap red, envelope-v1 retention note (already recorded), harness dedup, D10 labeled exceptions/closed-world, validator-pass dedup, DAG sharing, digest authentication.

### Units
- M5.1 DONE (main=34% 94K/272K, impl=69% 188K/272K) — acquisition + registry/terminology contracts + validator (respec 2026-07-22: `mapping` split to M5.1b at the subcommand seam). Shipped: CDC PDF + Box 3 extraction (both `-text`) + attribution README under `guidelines/cdc-2022-opioid/`; `docs/registry.md` (registry+terminology v1, ulex emission, reserved workflow rows, rights posture, naming, error classes); `registry_tool.pl` `registry|terminology|ulex`; real `guidelines/registry.pl` (source row + extraction row + 16 regions: 12 rec + 4 grp; list-number prefixes outside region ranges); `tests/registry-harness.sh` 62 gates (green/red fixtures, full ir_tool UTF-8 red matrix incl. 3-byte overlong + nonzero offset, native-variable enum/boolean reds, 27/27-template golden ulex, real-file gates two-way tied to registry metadata w/ exact 16-ID inventory, tree-status-preserving end gate) + CI + regen.md ten-harness alignment. Region digests + HTML verbatim 16/16 independently recomputed by MAIN. Strict review (4 findings: detached real-file ties HIGH, missing variable red, thin UTF-8 matrix, 6/27 coverage) fixed + re-verified in-unit.
- M5.1b DONE (main=33% 89K/272K, impl=55% 148K/272K) — mapping contract + validator. Shipped: `docs/registry.md` mapping v1 (mapping_document/4 w/ `ace_sha256`/`ulex_sha256` + stem-bound relpaths, mapping_region/2, mapping_claim/7 — projection enum applicability|action_kind, items ≥1 rule_id + exactly 1 query_id canonical-byte ascending, fact IDs excluded, answer-v2 expected_answer w/ wh `answers/1` byte-sorted — mapping_residual/5 w/ 12-class table split per D5: 3 profile walls / 8 constructorless / `scope_deferred`; coverage identity + claim⇒residual + document-referenced rules; 7-pass first-failure order; new error classes `residual` + `coverage`); `mapping` subcommand in `registry_tool.pl` (shared UTF-8/canonical front, ==-decomposition verified by MAIN native-variable probes on answer/docid/class/projection); green mapping fixture (3 docs, 4 regions, proved/not_proved/wh, all 12 residual classes) + 25 exact-stderr-line reds; harness 62→94 gates (mapping green, 25 reds, mapping UTF-8 stage atom, 5 new stage swaps) ×2 green; regen.md 94; memory authority map + docs/registry.md pointer. Peer-review finding (D5 cause-column divergence: property/labeled_exception/multi_entity/disjunction) confirmed vs roadmap D5 + fixed in-unit incl. fixture exemplar notes.
- M5.2 DONE (main=38% 103K/272K, impl=26% 70K/272K — report landed post-close; every gate MAIN-rerun) — pipeline CLI. Shipped: `tools/pipeline.emm` → generated `tools/pipeline.py` (argv `APE_TREE_DIR DOCS_DIR OUT_DIR`, repo-root `Require` guards on ir_tool + front end, fixed 6-step preflight w/ classes `ape-tree|docs-dir|out-dir|staging|swipl-exec` — stale staging via sorted iterdir + startswith, never glob —, sibling `<out>.tmp.<pid>` staging, front-end child w/ verbatim-stderr + preserved-exit relay and `child-stderr`/`front-out` integrity classes, per-docid lower→validate(explicit zero-stream)→compile→run each on fresh prior-stage disk bytes w/ `stage-stderr`/`stage-stdout` guards, `cnl_pipeline_manifest(1).` rows binding drs/ir/program/result + constant `front_manifest_sha256` digests over raw on-disk bytes, single `os.replace` publish, sorted `pipeline: wrote` lines + `pipeline: ok N documents`); `docs/pipeline.md` full contract; `tests/pipeline-cli-harness.sh` 17 gates offline (stub SWIPL enforcing the exact 13/14-arg argv incl. `-f none -F none`, 4-token poison matrix, green publish vs committed goldens + second-run determinism + harness-recomputed manifest digest ties, 13 reds w/ byte-pinned stderr lines + OUT_DIR-absent + zero-staging + before/after inventory- and sentinel-preservation on pre-existing dir/symlink/stale evidence, tree-status end gate); CI test-job lint + run step; `docs/regen.md` eleven harnesses (17); `docs/ace-front-end.md` version-neutral lint sentence. Read-only peer-review findings fixed in-unit (stub argv exactness, evidence-gate byte depth, absolute-path missing-swipl red, docs preflight numbering + dependency list); banked gate-strength residuals (corrected post-close against the shipped harness + the Agent's late-arriving report): fresh-disk-read vs in-memory-buffer stage chaining is indistinguishable through the hashing stub (bytes identical either way — a real distinguisher needs on-disk mutation between stages); preflight partial-order proof covers ape<docs, docs<parent, and every-class<swipl (each red holds later-checked resources simultaneously broken) but leaves the out-exists/parent/stale mutual adjacencies unpinned. The dangling-symlink OUT_DIR red and glob-metachar + sorted multi-stale evidence shipped (earlier "untested" note was wrong). Spec erratum: the unit prompt's "11 files" golden count was arithmetic slip — the enumerated tree is 10 golden files (+3 committed docs inputs); the Agent implemented the enumerated tree.
- M5.3 DONE (main=27% 74K/272K, impl=36% 97K/272K) — guideline authoring. Shipped: 12 ACE docs `guidelines/cdc-2022-opioid/ace/cdc2022-opioid-rec01..12.ace` (facts-first pn anchors → iv-headed rules → exactly one final query; rec05 = NAF relative clause, rec08 = wh w/ 2 sorted answers, rec10 = not_proved, rest proved; capitalized hyphenated pn_sg proven — exact-case WordForm required, lesson banked in memory); `terminology.pl` 49 entries (12 iv_finsg + 12 iv_infpl + 11 noun_sg + 14 pn_sg, all 47 used compound surface tokens covered, hyphenated compounds Clex-independent); 12 byte-identical generated `.ulex` sidecars (`registry_tool ulex` cmp 12/12); `golden/` = 50-file real published pipeline tree (manifest + front 13 + chain 36); `mapping.pl` 12 documents + 16 regions + 12 claims (one per rec region, projection applicability/action_kind, real rule/query IDs) + 110 typed residuals (rec 6–12 each, grp = 1 `scope_deferred` each, all 12 classes represented, 110/110 quotes verbatim inside their region's registered byte range) — M6 worklist; `.gitattributes` golden `-text`. Post-close peer review (2 fix batches, MAIN-adjudicated): rec10 applicability re-grounded on `subacute-chronic-pain-clinician` vs `acute-pain-clinician` peer (not_proved now tracks source subacute/chronic scope; circular `toxicology-testing-clinician` class dropped from terminology); 17 residuals added (before-phase/initiation/already-receiving applicability conditions, realistic/known + care/gradual manner properties, dosages-or-combinations disjunction cause-split, prescribed/nonprescribed property cause-split, patient-receiving relation, prescribing-event triggers) + 3 re-typed/re-noted (rec06.01 labeled_exception→population_threshold; rec05.04 note names the NAF absence-as-absence epistemic gap; rec12.06 quote extended w/ causal connective); letter-leading digest atom canonical gotcha fixed by MAIN (quoted `'d28…'` rejects — canonical bare atom). MAIN independently reran on repaired tree: terminology/mapping/registry rc=0 zero-stream, ulex cmp 12/12, fresh APE-tree pipeline run diff -r vs golden rc=0, expected-answer byte ties 12/12, item ties (rule_id∈program, query_id∈result) all green, ace/ulex digest ties 12/12, 16-region inventory, region-scoped quote sweep 110/110, rec10 not_proved confirmed, harnesses 94 + 17 (+ 57 pre-repair). Peer semantic candidates adjudicated by MAIN post-close: rec02 disjunction gap accepted → `residual.rec02.12` added (111 residuals, sweep 111/111); rec08 circularity accepted-in-part → claim relabeled `claim.rec08.action-kind`/`projection(action_kind)` (wh over asserted members = action classification witness, not population projection; answer/items/golden unchanged); rec05 NAF-vs-actual-absence rejected as artifact defect — claims are contract-defined partial projections, the absence-as-absence gap is typed in rec05.04's note as M6 evidence-constructor work, and D6 requires NAF exercised where warranted. M6 note: `applicability` claims after adjudication = rec05 + rec10 only. Second cause-split wave (peer-verified, MAIN-adjudicated): 118 residuals — hedge certainties (`anticipated`/`likely`/`expected` rec01.12/rec02.13/rec04.12/rec06.08), rec05.13 while-continuing temporal + rec05.14 warning-sign disjunction, rec12.09 increased-degree property; sweep 118/118.
- M5.4 OPEN (gate M5.3 DONE) — full-chain acceptance + closure: `tests/guideline-harness.sh` + CI; docs truth-alignment (`regen.md` totals, `ulex.md` production-path now-true sentence + sidecar divergence note, cross-refs); memory/roadmap closure. Acceptance: twelve harnesses green; CI wired; interrupted-run gate proves the manual idempotent round. ~130K.

## Seeds (banked)

### M6 — semantics coverage widening (user directive 2026-07-22)

- Directive (user, 2026-07-22; supersedes the 2026-07-18 scope guard on this axis only): real-world use requires processing real guidelines, so the semantics M5 omits get minimal covering extensions rather than staying residuals; ideal end state = all semantics covered. Framework compromise: stay inside the prior work's Prolog framework where possible; where a class is inexpressible there, deviate in the least intrusive way — the user named ProbLog-class probabilistic logic programming as one acceptable example; the formalism choice stays open (user reaffirmed 2026-07-22) and is made per class at M6 planning on least-intrusion grounds.
- Driving worklist = M5's typed-residual inventory (`mapping.pl` rows: class + region + verbatim-anchored detail). M6 planning starts from the real counts: which classes occur, how often, with what exact source phrasing. Ideal = zero residuals on the first guideline; any survivor carries a recorded impossibility/deferral rationale, never silence.
- Per-class least-intrusive order (decide at M6 planning, per class): existing chain constructors < deterministic IR/profile/kernel extension < external engine. First guesses from the CDC inventory-to-be: profile walls (`copula_head`, `transitive_relation`, `property`) = IR/lowering/validator widening only, no semantic novelty; `dose_quantity`/`temporal`/`population_threshold` = exact-rational (dense-Q) interval algebra already cataloged in `docs/research/ir-rule-semantics.md` (SWI native rationals, no floats); `direction_strength`/`certainty` = CDC grades are categorical (recommendation category A/B, evidence type 1–4) → ordinal/typed annotations likely suffice, probabilistic semantics only where a source's own semantics are genuinely weighted; `labeled_exception` + explicit closed-world declarations = the banked M4 D10 handoff; `multi_entity`/`disjunction` = relations/head-disjunction territory — genuinely needed disjunctive/uncertain semantics trigger the deviation decision (candidates include ASP/ProbLog-class; selection deferred to M6 planning).
- Any external-engine adoption = full vendor treatment (pin + license + provenance + determinism audit) and must preserve the audit contract regardless of engine: byte-pinned artifacts, deterministic first-failure classes, replayable certificates, regeneration-only corrections.
- `docs/research/ir-rule-semantics.md` (until now a non-binding catalog) becomes the M6 planning basis for the deterministic classes. Conflict detection + the evaluation battery/thresholds stay out — the directive did not reopen them.

### M5 + M7 (registry, workflow)
- Research (2026-07-18): `docs/research/guideline-sources.md` = vetted source catalog + per-artifact rights + registry row schema; first-guideline shortlist = CDC 2022 opioid 12-recommendation skeleton (public domain) first, WHO SMART ANC CC0 repo subset as differential oracle second, WHO immunization single-schedule slice third. `docs/research/evaluation-methods.md` = layered acceptance gates (claim_completeness = 1, exact-IR match over behavioral verdicts, proof replay, fresh-process hash equality, degeneration probes w/ positive+negative controls).
- Durable state before automation: guideline registry (URL/version/date, content digest, licensing/redistribution status, may-commit flag), mapping store, item states + blocked reasons; M5 proves one manual idempotent round (stop/restart, no dup/lost work). M7 wraps in dynamic workflow: parallel source research, serialized registry/ACE/compiler writes; no mid-run user input → escalations become BLOCKED proposals.
- Dynamic-workflow availability (verified 2026-07-18): Claude Code `2.1.211` ≥ required `2.1.154`; neither project nor user `.claude/settings.json` sets `disableWorkflows`, and `CLAUDE_CODE_DISABLE_WORKFLOWS` is unset; user confirmed the feature is enabled for their account (2026-07-18) → available, no residual gate. Official refs: `code.claude.com/docs/en/workflows`, `code.claude.com/docs/en/goal`, `code.claude.com/docs/en/skills`.
- Semantics: dynamic workflows = JavaScript scripts orchestrating subagents. Active run state is **not** durable across CLI exit; docs: exiting mid-run ⇒ “the next session starts the workflow fresh.” Definitions under `.claude/workflows/` are durable + invokable as `/<name>`, but invocation reruns the definition rather than resuming engine state. `/goal` = session-scoped Stop-hook completion loop; active goal restoration requires `--resume` of the same saved session. Agent repository writes persist normally.
- M7 persistence contract = **repository files only**:
  1. **State:** all round state in-repo = guideline registry + mapping store + item states/blocked reasons.
  2. **Round:** work selection = deterministic function of registry state; intake persists each fetched source as an immutable content-addressed record (URL, observed version/date, SHA-256, bytes or digest-verified locator, licensing observation) BEFORE any generative processing; changed remote content ⇒ new versioned item, never mutation of a prior item; writes atomic (temp + rename; registry last).
  3. **Recovery:** content-digest-keyed items + idempotent transitions; restart reconciles staged/orphaned artifacts against the registry (adopt or discard deterministically); rerun after any interruption ⇒ no duplicate or lost work.
  4. **Escalation:** no mid-run user input; licensing/semantic decisions become `BLOCKED` proposal registry entries.
  5. **Termination:** quiescent = every item ∈ `{DONE, BLOCKED}` ∧ frontier empty ⇒ pause; report non-success while BLOCKED proposals pend; resolving a proposal returns its item to runnable. Complete = every known guideline DONE or user-approved terminal exclusion ∧ frontier empty.
  6. **Invocation:** `/goal` = required in-session completion driver over the saved workflow definition; every invocation starts fresh + resumes solely from repository state; repository files = the only persistence.
