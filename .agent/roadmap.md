# roadmap

Charter: `.agent/initial-prompt.md`. Shipped: fetch→ACE→Prolog guideline
pipeline behind `/goal` — `tools/goal.py compile <id> | check`,
`tools/regen.py --check`, all gates green.

New tasks are planned in a new session on demand.
