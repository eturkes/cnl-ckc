# roadmap

Charter: `.agent/initial-prompt.md`. Shipped: fetch→ACE→Prolog guideline
pipeline (`tools/goal.py compile <id> | check`, `tools/regen.py --check`),
operated through built-in `/goal` rounds — procedure + canonical argument:
root `README.md` § Operating.

Round queue (sole queue; append discovered candidates, drop covered ones;
format `- <id> | <source url> | queued|blocked(<why>)`):

- (empty — next round discovers candidates online)

Covered, checks green: cdc-2022-opioid. Other tasks are planned in a new
session on demand.
