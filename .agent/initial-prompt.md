The goal is to create a minimal research-grade expert system for clinical guidelines.

Use canonical E-- (https://github.com/frmoded/e--) as the sole authored source for all first-party Python glue, tests, and tooling. Generated Python is a deterministic derived artifact and must be accepted as-is: incorporate it byte-for-byte as emitted by the pinned E-- compiler, with all corrections made in canonical E-- or the E-- compiler and applied through regeneration. Accepted E-- must compile directly through the deterministic path, with model normalization disabled and zero {{ ... }} slots present. When a required Python behavior cannot be expressed, fork and extend E-- before implementing that behavior. Treat the imported E-- implementation as a narrowly scoped, hash-pinned trusted computing base, an exception that applies only to the compiler and unchanged external dependencies; all first-party Python outside this boundary must be compiled from canonical E--.

Use Attempto Controlled English (ACE) as the sole authored source for every clinical or guideline-specific rule. Extend or reimplement related ACE components (https://attempto.ifi.uzh.ch/site/resources/) like APE, AceRules, Clex, and RACE as necessary for clinical use. Parse ACE deterministically into a project-owned IR, then compile that IR deterministically into Prolog. Generated IR and Prolog must be accepted as-is: incorporate them byte-for-byte as emitted by the pinned compilation pipeline, with all corrections made in ACE, the IR specification, or the compilation pipeline and applied through regeneration. Hand-authored Prolog is limited to the parser, compiler, validator, explanation machinery, and inference kernel. All executable clinical inference and rule semantics are implemented in Prolog; Python handles glue, orchestration, and integration. Together, these components should cover all required tooling.

The program machinery should also include a resumable `/goal` implemented using Claude Code’s dynamic workflows feature. In each round, the subagents fetch a new source document (if all current documents have been exhausted) and draft reusable mappings that extend the translation layer between ACE and free English. Where needed, this workflow may also extend ACE itself or any other component that needs extensions. The goal continues until all known clinical guidelines have been processed.

To initialize this project, I have provided the seed files needed for agent-driven development. Commit the files as-is, create files such as `.gitignore` as needed, and record this initial prompt verbatim. Defer cloning each external repository until a work unit requires it. Use a scoped commit as instructed by `CLAUDE.md`. The roadmap will be created in the next session without additional context, following the regular plan-session process described in `/session-prompt`.
