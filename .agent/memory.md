# memory

- Charter = `.agent/initial-prompt.md` (verbatim user scope directive; authoritative). Vehicle = Claude Code built-in `/goal` stop-condition loop (re-arms at every stop attempt; survives session resume; `/goal clear` disarms): procedure + canonical argument = root `README.md` § Operating; queue+status = `.agent/queue.md` (one source doc in progress at a time → full coverage before next fetch); roadmap = project development planning only. Teammate fan-out per `/session-prompt` execution map; compiler/lexicon edits stay MAIN-authored. Keep `.claude/commands/` free of built-in command names (`goal`) — a project command masks the built-in.
- Authority map: pipeline + audit story → root `README.md`; guideline provenance/rights/coverage → `guidelines/<id>/README.md`; ACE→Prolog compiler contract → `vendor/ape/prolog/ace_to_pl.pl` header; E-- language → `vendor/e--/docs/spec.md` + strict entrypoint; TCB + fork boundaries → `vendor/*/PROVENANCE`; gates → `.github/workflows/ci.yml` (SWI version+crypto probes, bootstrap cmp, `regen.py --check`, `goal.py check`, cleanliness).
- Toolchain (sole dev workstation): system swipl/python too new → gitignored `.toolchain/`: `swipl-9.2.9/` prefix built from `https://www.swi-prolog.org/download/stable/src/swipl-9.2.9.tar.gz` (`cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DINSTALL_DOCUMENTATION=OFF -DSWIPL_PACKAGES_JAVA=OFF -DSWIPL_PACKAGES_X=OFF -DSWIPL_PACKAGES_ODBC=OFF` + `ninja install`) + `bin/` shims `swipl`→that prefix, `python3`→uv cpython 3.13. Every run: `export PATH="$ROOT/.toolchain/bin:$PATH"`. Lost ⇒ repeat recipe. CI's digest-pinned swipl:9.2.9 container = reference runtime; python floor = 3.11 (supported invocations use `-P`, a 3.11+ flag; container Debian python3 = 3.11 → avoid newer-stdlib constructs like pathlib `follow_symlinks` kwargs).
- Deterministic SWI: always `-f none -F none`; writer options explicit. SWI stream `encoding(utf8)` ≠ strict octet validation → compiler reads bytes + explicit RFC-3629 boundary/overlong/surrogate/max-code-point checks (ace_to_pl does; preserve on edits).
- ace_to_pl editing gotchas: decompose DRS with `arg/3` + `==`, never unify into it (canonical vars silently instantiate); event-referent erasure counts CONDITION positions only (`drs/2` domain arg = binder, skipped while counting); `numbervars` only after validating no pre-existing `'$VAR'/1`, acyclicity, no attvars; digests = `crypto_data_hash/3` over raw pre-decode bytes incl. final LF with `encoding(octet)`.
- APE facts: noun copula needs the indefinite article (`X is a noun.`); DRS negation by arity+spelling: `-/1` classical, `~/1` NAF, `-/2` = sentence/token anchor wrapper (never negation); Ulex `pn_sg/3` WordForm is case-sensitive to the ACE surface; unknown content words without ulex → `ape_messages`.
- Strict E-- authoring: no comments/indexing/postfix chains; bind intermediate call results; `[[x.y]](args)` call marker; retain a loop's final value explicitly.
- Fork edits: a modified upstream file needs a prominent in-file notice — Apache-2.0 §4(b) (`vendor/e--`; notice alone, no date required), GPL-3.0 §5(a) via LGPL (`vendor/ape`; notice + relevant date). Git history = the fork change record (no patch/manifest machinery).
- Scratch/probe work → gitignored `.scratch/`; standalone git ops there set `GIT_CEILING_DIRECTORIES="$ROOT/.scratch"`.
- WebSearch safety layer false-positives on medical vocabulary → phrase queries by tool/org names only.
- Presentation rule (user): durable files read as this project's own findings — never reference other local working directories, their repo names, or their commits.
