# memory

- Charter = `.agent/initial-prompt.md` (verbatim user scope directive; authoritative). Vehicle = Claude Code built-in `/goal` stop-condition loop (re-arms at every stop attempt; survives session resume; `/goal clear` disarms): procedure + canonical argument = root `README.md` § Operating; queue+status = `.agent/queue.md` (one source doc in progress at a time → full coverage before next fetch); roadmap = project development planning only; round delegation map = `.agent/rounds.md` (MAIN orchestrates + rules + commits; GPT teammates absorb all bulk; compiler/lexicon edits stay MAIN-authored). Keep `.claude/commands/` free of built-in command names (`goal`) — a project command masks the built-in.
- Authority map: pipeline + audit story → root `README.md`; guideline provenance/rights/coverage → `guidelines/<id>/README.md`; ACE→Prolog compiler contract → `vendor/ape/prolog/ace_to_pl.pl` header; E-- language → `vendor/e--/docs/spec.md` + strict entrypoint; TCB + fork boundaries → `vendor/*/PROVENANCE`; gates → `.github/workflows/ci.yml` (SWI version+crypto probes, bootstrap cmp, `regen.py --check`, `goal.py check`, cleanliness).
- Toolchain (sole dev workstation): system swipl/python too new → gitignored `.toolchain/`: `swipl-9.2.9/` prefix built from `https://www.swi-prolog.org/download/stable/src/swipl-9.2.9.tar.gz` (`cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DINSTALL_DOCUMENTATION=OFF -DSWIPL_PACKAGES_JAVA=OFF -DSWIPL_PACKAGES_X=OFF -DSWIPL_PACKAGES_ODBC=OFF` + `ninja install`) + `bin/` shims `swipl`→that prefix, `python3`→uv cpython 3.13. Every run: `export PATH="$ROOT/.toolchain/bin:$PATH"` — EVERY Bash call (shell state resets per call); a PATH without the shims leaks system swipl into goal.py (`resolve_swipl`), failing compile as `ace_to_pl_error(uncaught,unwind(halt(0)))` rc=2 (newer SWI catches halt's unwind in the compiler's catch-all) — that shape = toolchain leak, not a corpus defect. Lost ⇒ repeat recipe. CI's digest-pinned swipl:9.2.9 container = reference runtime; python floor = 3.11 (supported invocations use `-P`, a 3.11+ flag; container Debian python3 = 3.11 → hold to the 3.11 stdlib surface; pathlib `follow_symlinks` kwarg = 3.12+).
- Deterministic SWI: always `-f none -F none`; writer options explicit. SWI stream `encoding(utf8)` ≠ strict octet validation → compiler reads bytes + explicit RFC-3629 boundary/overlong/surrogate/max-code-point checks (ace_to_pl does; preserve on edits).
- ace_to_pl editing gotchas: read DRS via `arg/3` + `==` exclusively (unification silently instantiates canonical vars); event-referent erasure counts CONDITION positions only (`drs/2` domain arg = binder, skipped while counting); `numbervars` only after validating no pre-existing `'$VAR'/1`, acyclicity, no attvars; digests = `crypto_data_hash/3` over raw pre-decode bytes incl. final LF with `encoding(octet)`.
- APE facts: noun copula needs the indefinite article (`X is a noun.`); DRS negation by arity+spelling: `-/1` classical, `~/1` NAF, `-/2` = sentence/token anchor wrapper only; Ulex `pn_sg/3` WordForm is case-sensitive to the ACE surface; unknown content words without ulex → `ape_messages`.
- Strict E-- authoring: the language rejects comments, indexing + postfix chains; bind intermediate call results; `[[x.y]](args)` call marker; retain a loop's final value explicitly.
- Fork edits: a modified upstream file needs a prominent in-file notice — Apache-2.0 §4(b) (`vendor/e--`; notice alone, no date required), GPL-3.0 §5(a) via LGPL (`vendor/ape`; notice + relevant date). Git history = the sole fork change record.
- Scratch/probe work → gitignored `.scratch/`; standalone git ops there set `GIT_CEILING_DIRECTORIES="$ROOT/.scratch"`.
- WebSearch safety layer false-positives on medical vocabulary → phrase queries by tool/org names only.
- Guideline-source discovery = `.agent/compendium.md` (org universe + guideline rows; header owns eligibility, independence, promotion + terminal rules). Enumeration independence = distinct source OWNER/dataset, issuer- and developer-controlled listings excluded; all NLM properties (PubMed/PMC/Bookshelf) = one owner → pair an NLM id with Crossref/GovInfo/a membership frame. Bibliographic cells cite stable ids (`PubMed PMID`, `PMC`, `DOI`, `NBK`, `GovInfo`) so claims stay checkable from the file alone.
- Guideline-site access classing: Cloudflare `Just a moment` + Akamai `Access Denied` = anti-bot, NOT an access gate → row access `unverified` + status `provisional(<why>)`; classify from the artifact (DOI landing/PDF), never from transport. `doi.org` resolution 403s under a scripted UA while the publisher page may still be open. The opposite error is the common one: a publisher landing page answers 200 with substantive bytes and then asks for money, so `open` earned from transport must be re-decided against the entitlement prompt (`Get full text access`, a lock-marked `Check Access`, an LWW fulltext→abstract bounce rendering a page-image preview of a 19-page guideline in 3,086 characters). `.scratch/audit_open_oa.py` screens every DOI-carrying `open` row against Unpaywall and NOMINATES only: over M1.2b's 184 it flagged 105 and 4 survived confirmation, because society journals post guidelines free-to-read without an OA licence (JACC, AJG, ALN, JCSM all screen `closed` while returning 85K-250K characters anonymously). Unpaywall also holds pre-platform-move records — both disputed Anesthesiology rows read `bronze` against a `pubs.asahq.org` PDF whose host now redirects to the LWW journal home. Live differential beats both: the reader returns a free ALN guideline on the first try and only Cloudflare's interstitial for the gated pair across three paced attempts.
- Access probing = evidence, and silence is not evidence. A 2xx describes the REQUEST, never the artifact, in two shapes: zero bytes → `empty(<code>)`, and a cookie/session interstitial → `challenge(cookie-wall)`. Atypon and Silverchair (JACC, `pubs.asahq.org`, `journals.lww.com`, JCSM, Wiley, `jospt.org`, `liebertpub.com`) answer a cookie-less fetch with 200 + real HTML + no artifact bytes at `/action/cookieAbsent`, so a body-only test scored 88 of 96 ACC probes `open` — a FALSE open, strictly worse than a false negative because the row then promotes and the fetch cannot succeed. The FINAL url is what separates them (`/action/(cookieAbsent|cookieSet|showLogin)`, `[?&]cookieSet=`); routing through `challenge(...)` keeps the reader rescue reachable, so a proxy retrieving genuine bytes still earns `open(reader)`. Probe evidence predating a prober fix is void for every redirected URL — re-probe rather than reason about which lines survive.
- Split probing from adjudication across the wave: probing is per-host serialised and mechanical, adjudication is neither, so a producer's window buys far more sitting on eligibility. Producers emit every eligible row `unverified` + `provisional(access probe pending)`; MAIN runs one prober pass per report and promotes cells with `.scratch/apply_probe_access.py`, which is convergent (rewrites only where evidence disagrees, so re-probing just the pending rows suffices) and refuses to decide `gated(...)`, since `paywalled(<gate>)` vs `login(<gate>)` names which gate the body showed. Probing is what saturated four of six M1.2b producers; the two that skipped it finished a full org in a third of a window.
- Naming an issuer is a claim about the ARTIFACT; `CPGs` is a claim about the ORG's enumeration evidence. Requiring every org-cell part to read `CPGs=yes` inverted that dependency and made co-issued work unrecordable — ACC's 161 entries alone name 61 society acronyms, 16 of them `yes`, and across the M1.2b wave 73 distinct issuers over 447 rows would have failed. The predicate is instead: every part holds an org row, and none reads `CPGs=no` (a positive finding that the org issues nothing eligible, so a row naming it is a contradiction). A sweep still never widens the universe; `.scratch/fix_m1u2b_new_issuers.py` materialises the missing rows `unverified`/`swept=pending` after the merge, which turns M1.3's worklist into table rows instead of prose. Issuer hosts answer a burst with throttle shapes indistinguishable from refusals, so an unpaced sweep manufactures its own inaccessibility: `www.healthquality.va.gov` drops connections; `www.cdc.gov` 403s. All clear on a paced re-probe. A throttle shape is a hypothesis until the mechanism is read: `library.samhsa.gov`'s 202 + zero bytes is NOT metering and no cooldown clears it — send browser headers and the same 202 carries 2,423 bytes of `awsWafCookieDomainList`/`gokuProps`, and a real browser gets `Let's confirm you are human` escalating to an image CAPTCHA. The guard is path-scoped to `/sites/default/files`; product pages and `/search-endpoint` answer normally, which is what distinguishes an anti-bot gate (needs a human) from a budget (needs waiting). `probe_urls.py` therefore serializes per host behind a lock held across the sleep + retries transient shapes; `probe_until_settled.py` grinds a metered host across cooldowns so each row earns its own verdict. Treat a whole-host `open` generalisation as unproven. Refinements for M2: a 202 + 0 bytes means CHALLENGED, never dead — deadness reads as `Page Not Found` HTML through a browser that has cleared the check. The guard is path- and cache-scoped rather than host-scoped, so try case variants before ruling a URL dead: `PEP20-02-01-022.pdf` serves anonymously (200, 427,172 B, `%PDF-`) while the same path lowercased 202s. `/search-endpoint` is a probe, not an enumerator — it returns page 1 of all 754 products and ignores every query + pagination parameter over GET and POST alike. Delisted TIPs live on NCBI Bookshelf (TIP 42 `NBK571020`, TIP 61 `NBK539583`), already the compendium's host for TIP 39 + TIP 51. The image CAPTCHA refuses scripted tile clicks — each `click_at` advances to a fresh challenge instead of selecting — so clearing it needs the user, once per host. Once cleared, read the artifact by evaluating a same-origin `fetch()` in an already-open page on that host (`mcp__browseros__evaluate`, returning status + `byteLength` + magic bytes): the clearance lives in the browser's cookie jar, so `curl` keeps answering 403 while the page context serves full bytes. That route verifies an artifact at byte level without downloading it, and generalises to every challenge-gated issuer.
- Row re-verification reads the artifact's own text: `pdftotext -l 8` on a PDF, tag-stripped HTML otherwise, `r.jina.ai` only where a direct fetch is refused. A throttled reader returns a ~200-400 byte stub whose `Title:` header echoes the document, so title matching on a stub confirms itself — anything under ~2000 bytes verifies nothing and must be reported `thin`, never clean. `jts.health.mil` PDFs render as stubs through the reader but extract fully via `pdftotext`.
- Sweep manifest vs table: a co-issued guideline may sit in every issuer's index or in only one, and the merged table records issuers but never which index surfaced the row, so no post-merge predicate can decide manifest truth — an interval over the table (`sole <= claimed <= counting_co_issued`) accepts both an org counting a joint row its index omitted and the co-issuer dropping the row its index carried. The reports still hold that provenance, so `merge_rows.py` checks it pre-merge: manifest `<e>/<x>` == the rows that org's own report block emits. Exact while co-issuers are swept by SEPARATE reports (the real `ACIP + HICPAC` case); one report sweeping both counts the joint row for each, so split co-issuers across reports wherever a manifest must be provable. When BOTH issuers' indexes carry the co-issued artifact (ACIP's smallpox topic page and HICPAC's index both list rr5207a1), both reports emit the row byte-identically and both manifests count it — the merge deduplicates to one table row and refuses on any cell disagreement, decided BEFORE lifecycle suppression so a `queued|in-progress|done` row cannot hide a disagreement until it clears. A silent report would instead show `n - (e + x) > 0` and read as a collapse it never made. Exactness rests on the report being the row's provenance, so one report sweeping every issuer of a co-issued row is refused: it records no finer origin than the batch. `indexed-by=<org>[ + <org>...]` names the whole set of carrying indexes (naming exactly the issuers = the default, refused as redundant), which is how an artifact its issuers and a third org all index counts once in each manifest. The two provenance markers answer one question → a row carries at most one, and never two `indexed-by=`. An `off-index(...)` row belongs to no manifest and no index sweep retracts it — silence from an index is not evidence about a row that was never in one — so only a batch re-emitting the artifact (matched on URL or issuers+title) replaces it.
- A gate test that re-runs the tool's own detection regex over the shipped corpus proves nothing: replace the regex with `(?!)` and it stays green, because a detector that finds nothing reports nothing wrong. Drive the TOOL over synthetic positives and negatives instead, and keep the corpus scan as a separate convergence check pinned to the shipped tool (never to `M1U2A_ARTIFACT_DIR`, which swaps tool variants). Mutation is the measure: `.scratch/m1u2c/rev2-1/run_mutants.py` regenerates one-behavior mutants of the attribution predicates from `base/` (refresh it from the live tools first) and scores them against the gate.
- HICPAC's index carries other bodies' guidelines well past rr6007: `rr6904a1` is a U.S. Public Health Service Guideline naming HICPAC nowhere, `rr6210` is CDC guidance where HICPAC supplied liaisons, and both shipped under HICPAC as issuer. The full audit waits on M1.3 because the correct issuers lack organization rows and sweeps never add them.
- An org's guideline index = the BROADEST listing on its own site that enumerates its clinical-guidance artifacts and still enumerates inside the easy tier. A narrower filtered view is not sweepable while a broader own-site listing exists: the header emits no row for material outside the swept index, so sweeping the narrow view records the org as swept while its other eligible artifacts hold neither a row nor a deferral. Where the broad listing outruns the tier, the whole org takes `blocked(<why>)` — deferral is honest, silent narrowing is not. Societies publish one corpus under many labels, and the recorded index often sees a fraction: ACOG's own Coveo endpoint (`POST https://www.acog.org/coveo/rest/search/v2`, anonymous, `groupBy` on `@selectedcontenttype`) counts ~2,500 clinical records across 11 guidance types against the 61 the compendium's `Clinical Practice Guideline` facet carries.
- `merge_rows.py`: an issuer's sweep may REPLACE a co-issued row its own index carried, printing the diff for a MAIN ruling. Tiering splits co-issuers across milestones (ACC in M1.2b, AHA/ADA/ASN in M2), so "sweep every issuer together to replace it" is unreachable and would freeze a co-issued row at its seed-era cells forever. Replacement authority = ownership: one of the row's issuers, or the index that carried it (`claim_orgs(row) & (issuers(stale) | attrib(stale))`), OR an emission naming every issuer the retained row named. That superset clause is what lets a third index improve a row it shares no issuer with — AASM's Endorsed Guidelines subindex carries the benzodiazepine guideline whose seed named ASAM alone, and the sweep emits all eleven issuers plus `indexed-by=` — while an emission that DROPS an issuer stays refused as a competing claim about authorship rather than a better-attributed row. A `queued|in-progress|done` row is suppressed rather than replaced, co-issued ones included.
- A producer's rows and its own prose disagree, and the merge ships the rows. `prod-m1u2b-2` reported two artifacts as `paywalled(publisher entitlement)` and recorded both in `.scratch/m1u2b/probes/asa-access-review.tsv`; the emitted table cells said `open`, so `open` is what shipped. Harvest therefore diffs each report's stated corrections against the cells it emitted, and MAIN's ruling lands as a post-merge fixer — a re-merge replays the producer's cells and would silently undo a hand edit.
- The merge rewrites the two TABLES, never the header, so "the wave re-derives from its reports" is a claim about rows: replaying the reports over the shipped compendium is byte-identical, while replaying them over the pre-merge baseline reproduces every row and differs in the header prose that same commit authored. Record the reproducible check, not a bare SHA — the SHA M1.2c recorded belonged to no shipped state.
- Repair-script idempotence keys on a field's SHAPE, never its exact current text. `fix_m1u2a_samhsa_access.py` admitted a row only when `status` equalled the one string today's `why()` builds, so rows an earlier version wrote against a different host matched nothing, were skipped, and could never be promoted however much evidence landed — two artifacts sat `unverified` blaming a host they are not served from while their own `open` probes sat in the evidence file. Match the prefix (`provisional(access unverified:`) so the script owns every row it has ever written.
- Access probing serializes per host across the REQUEST, not just its start: holding the lock only over the sleep spaces request starts while slow same-host fetches overlap (measured 4 concurrent), which is the concurrency a meter counts. `probe_urls.py` holds the lock across `urlopen` and measures the gap from completion → idle time the host observes.
- ACIP's vaccine-specific index enumerates 27 TOPIC pages, each a subindex whose `CURRENT` section lists several scope-distinct artifacts (Rabies: 2022 PrEP + 2010 4-dose PEP + 2008 baseline, `ARCHIVED` empty) — 67 CURRENT listings = 66 distinct artifacts, against 27 rows for one-row-per-topic. A bounded fan-out is easy tier (compendium header owns the clause): enumerable from the index in one fetch, ≤30 subindexes, each anonymous + static ⇒ `1 + <subindexes>` fetches, method `per-topic-pages`. No aggregate endpoint substitutes: `recs-by-date` stops at 2024 and mixes current with superseded, and one of its rows links the wrong artifact. Parsing a topic page has three traps, each worth one artifact: `Archived DTaP-IPV-Hib-HepB …` capitalizes ARCHIVED differently from every other page, a listing links its own article with a tracking query (`mm7336a4.htm?s_cid=…`), and one listing may link several articles whose extras are its `Print version` and `Appendix` — parts of an artifact, not artifacts. `mm7336a4` is listed under two topics, so raw listings exceed distinct artifacts.
- MMWR states a recommendation's label year in the title (`— United States, 2024`) and its publication year in the volume (`year = 1951 + volume`, readable from every URL shape), and the two differ constantly. Rows record `title (label-year; pub YYYY)` whenever they differ, since the table sorts on version year; a season range (`2024–2025`) names no single label year and keeps the publication year alone, and a month may sit between the scope clause and the label year (`— United States, December 2024`). The volume rule holds across all 75 shipped MMWR rows with one exception in the shape it predicts: a late issue cover-dated into the next January (56(51) → January 4, 2008) takes the date's year. Titles come from the artifact's own `Suggested citation for this article` line — a topic page's link text is display shorthand, and three ACIP rows shipped it. Two current artifacts stay scope-distinct when the later replaced one component of the earlier (mm7349a3 vs rr6909a1's MenB-4C schedules); the earlier row's notes carry the version qualifier, quoted from the superseding artifact.
- A guideline that consolidates another body's recommendations without adding new ones stays eligible: it carries systematically developed patient-care recommendations, which is the test — `does not contain any new recommendations` describes novelty, not content. Check the ISSUER cell instead, since such artifacts are commonly indexed by a body that only reviewed them (HICPAC indexes MMWR rr6007, whose work group, vote and title are ACIP's). A manifest reconciles an INDEX while the org cell records ISSUANCE, so the two diverge routinely; notes carry `indexed-by=<org>` (counts in the indexer's manifest, issuer keeps the org cell) or `off-index(<why>)` (no index carried it, so no manifest counts it — the shape the cross-check's gap rows need). Sweep tooling attributes deletion, claiming and counting by the same rule.
- HRSA women's preventive services = WPSI-developed living recommendations: no version year appears on a WPSI recommendation page (reader proxy or real browser), beside a service in the HRSA index, or on the WPSI recommendations table. Their rows carry `provisional(year unresolved: ...)`; only a row whose notes state a dated adoption (`HRSA-adopted YYYY-MM`) has an evidenced year. `www.hrsa.gov` + `www.womenspreventivehealth.org` 403 scripted UAs.
- Compendium gates split two ways. `tools/goal.py check` (compiled from `tools/goal.emm`) now runs the `/goal`-consumed subset FIRST, before any swipl work: row format/vocabulary (org table + TSV), both canonical orderings, the one-active-row promotion invariant, dup keys, and the terminal meter printed as `goal: compendium ok <O> organizations <G> rows; terminal remaining: orgs=<n> rows=<m> provisional=<p>` — exhaustion ⟺ n=0 and m=0; a non-terminal compendium stays green (meter is report, not violation). Three deliberate strictness deltas vs the scratch validator, contract-recorded: TSV header line required; `CPGs=no`+`pending` counts terminal (header disjunction beats the scratch meter); missing TSV fails (scratch `load()` returns `[]` by design). First violation stops (goal.py style; the scratch tool enumerates). Byte-level unreadability tracebacks — E-- has no exception machinery, matching every other goal.py read. `.scratch/check_compendium.py` stays the deep gate (independence, collisions, LEGAL_SWEPT, manifest grammar/arithmetic/attribution, `--require-swept`); gitignored → workstation-local. Preserve through M1; absent ⇒ re-derive from the compendium header rules, then run it. M1.5 unit gate = `.scratch/m1u5/gate_m1u5.py` (48 fixtures via `fixtures/_build.py`, port-vs-reference agreement per case; `M1U5_PORT` env overrides the port under test, `M1U5_SKIP_LIVE=1` skips the slow live-tree case) + `.scratch/m1u5/rev2/run_mutants.py` (35-mutant campaign over goal.emm copies, 35/35 killed) + `.scratch/m1u5/kill_check.py` (3 targeted mutants vs their killer fixtures); regeneration = `.scratch/contracts/m1u5.md` P1-P14/D1-D3 + the compendium header. Bare run = structure + full ordering (access→class→org→year desc→title), so a partially harvested compendium stays gateable; `--terminal` adds the M1.2 exhaustion gate and prints the progress meter `unswept=<n> unverified=<n> provisional=<n>`. M1.3a widened it: organization-name collisions over the four-rung ladder (ruled-distinct pairs sit in `DISTINCT_ORGS` with a reason), the CPGs×swept legality matrix, manifest grammar + arithmetic on EVERY dated cell rather than `CPGs=yes` alone, and `--require-swept` demanding a well-formed manifest. `.scratch/m1u3a/gate_m1u3a.py` (44 tests) is its unit gate beside `gate_m1u2a.py`, importing that file's fixture helpers; both run per commit, and `.scratch/m1u3a/prechange/` holds the pre-change tools for red-proofing new tests. `.scratch/gate_m1u2a.py` = the unit gate beside it: synthetic regressions over copies of the tools (`M1U2A_ARTIFACT_DIR` redirects which copy, so a test can be proved red against a pre-change tool) plus a `ShippedRulings` class that asserts against the live compendium and re-derives ACIP's index from the cached topic pages. Same gitignored status, never ported; absent ⇒ re-derive from the rulings recorded in the compendium header + `.scratch/contracts/`. Every M1.2 repair is an idempotent `.scratch/fix_m1u2*_*.py` replayable to zero edits, so a wave stays re-derivable from the reports.
- `compendium_io.py` owns every question two tools would otherwise answer separately: org-name normalisation (`norm_dash`→`norm_punctuation`→`norm_affix`→`norm_stopwords`, `name_collisions`), enumeration independence (`OWNERS`, `NON_QUALIFYING`, `FRAME_OWNER`, `CONTROLS`, `qualifying(cell, org)`), the `LEGAL_SWEPT` matrix, and both canonical sorts. Independence needs the CITING org, not just the label: a frame its owner or parent controls proves nothing (`APTA sections/academies` for an `APTA <name>` academy, `AAFP-PG` for AAFP), while a third-party MEMBERSHIP roster stays independent for its members. Validate a `FRAME_OWNER` value against the universe only where the label is CITED — an unused label's owner holding no row says nothing about the table, and failing on it makes every small fixture unpassable, which is how it broke every synthetic positive control at once.
- One body written two ways hides in an em dash, a trailing `Foundation`, or `for` against `of`. AAO-HNS: the Academy and the Foundation are separately incorporated in DC, all three guideline artifacts name the FOUNDATION, and the em-dash row was the Foundation misnamed (its abbrev already read `AAO-HNSF` and it held the Foundation's CPG index) → merged there, with the Academy entering fresh as `unverified` and the pair allowlisted as ruled-distinct. ASPC: the `of` spelling answers to no second entity → absorbed into the `for` row. A collision is a ROW-LEVEL operation, never a row deletion, because guideline rows sit on both sides.
- Compendium mutation tooling: `.scratch/merge_rows.py` = the only M1.2+ row inserter and the canonical full sort (orgs class→CPGs→alpha; guidelines open-band→class→org→year desc→title). A sweep is total for its org — every guideline row of an org whose emitted `swept` is a date is deleted, then the report's rows insert, so the sweep and not the seed defines that org's rows; `blocked(...)` deletes nothing; `queued|in-progress|done` rows outrank a sweep, are kept verbatim, and their suppressed corrections print for a MAIN ruling. Reports are the source of truth → repair a report and re-merge, never hand-edit the compendium; every repair lands as an idempotent script so the wave stays re-derivable. `.scratch/apply_fixes.py` + `.scratch/splice_compendium.py` = M1.1-only; they reject sweep/provisional states and erase harvest → never reuse for M1.2 rows.
- Index-harvest technique: the `https://r.jina.ai/<url>` reader proxy renders indexes that return scripted-UA 403 (CDC, HRSA, HICPAC, ClinicalInfo, ACIP, ASCO) and costs one fetch. `www.cdc.gov` refuses headless Chrome `--dump-dom` too, so the reader proxy and the authenticated browser are the only routes into CDC and a DOM-fetch batch there yields zero bytes however long it runs; Cloudflare/Akamai challenge pages still need the authenticated browser. Enumerate from an org's data layer where one exists (USPSTF `data.uspreventiveservicestaskforce.org/api/json`, VA PBM `/formularyadvisor/DOC.json`, ACOG Coveo, ACR `acsearch.acr.org/list`) — one fetch replaces whole-site crawling.
- Presentation rule (user): durable files read as this project's own findings — reference only this project's own paths, commits + findings.
- M1.3b regeneration paths (scratch-local, gitignored): terminalization = `.scratch/fix_m1u3b_terminal.py` (idempotent; embeds MAIN rulings incl. HVS/SCPC no→unverified overrides, SGIM/SHM sources, mechanism map); crosswalk = `.scratch/m1u3b/build_crosswalk.py` cache-only replay byte-identical, gated by `.scratch/m1u3b/validate_crosswalk.py` (production-time gate — post-fixer its coverage predicate reports the ruled rows as extra BY DESIGN; post-fixer state is `check_compendium.py --terminal`'s); gap list = `.scratch/m1u3b/gapcheck/build_gaps.py` cache-only replay. Absent ⇒ re-derive from compendium header records + scout/crosswalk reports under `.scratch/agents/`.
- M3.1 v1-schema regeneration paths (scratch-local, gitignored; port scheduled with M3.7 close (corpus fully v1; gates can run off committed inputs)): unit gate `.scratch/m3u1/gate_m3u1.py` (17 green x layout/provenance/safety/determinism/consult + 17 canonical reds + aggregate co-load both orders + chaining + legacy-default byte check; expectations = MAIN rulings, fixtures pulled from `.scratch/m3scout/probes`, `.scratch/m3u1/manifest-work/regions`, and the wave-2 teammate fixture dirs `.scratch/m3u1/{test/inputs,rev/parses,rev2/fixtures}` + `.scratch/m3u1/gate-fixtures/chain.*`). Differential `.scratch/m3u1/diff_m3u1.py` runs the tracked compiler against the independent oracle snapshot `.scratch/m3u1/orc-oracle/` (copied out of worktree `wt/orc-m3u1` before deletion; `run.sh` takes `APE_TREE`) and compares canonically-renamed clause multisets, so emission order and ordinal integers classify as named divergence kinds rather than noise. Both need the staged APE at `.scratch/m3scout/ape-stage`; absent ⇒ restage by copying `vendor/ape` there and building `grammar.plp` (`swipl -O -f none -F none -g "working_directory(_, '<stage>/prolog/parser'), [fit_to_plp], halt." -t halt`). Shape manifest gate `.scratch/m3u1/validate_manifest.py` + walker `.scratch/m3u1/drs_families.pl` re-derive every manifest row's families by parsing; contract of record = `.scratch/contracts/m3u1.md`.
- M3.2 contract = `.scratch/contracts/m3u2.md` (FINAL; F2=ctx-edge decision record + P1-P14 incl. mid-unit batch rulings: P13 detail vocabulary + position-over-content precedence — a forbidden-POSITION NAF classifies without descent; P14 Q4 accessibility — operator/NAF-box domains contribute nothing to Deps). Regeneration: map/spike reports `.scratch/agents/{map,spike}-m3u2*.md`; spike compiler deltas `.scratch/m3u2/spikes/{ctxterm,ctxedge}/` (ace_to_pl.pl + delta.diff vs d7e31fb). Corpus shape facts M3.2 rode on: manifest report `.scratch/agents/prod-m3u1.md` (75/79 admit=m3.2, families histogram, zero NAF need); nested-consequent-implication = the dominant rule shape (S24-06 parse). D9 `for` AMENDED: NO vendor fork — ulex `prep(for,for)` alone parses the verb-attached for-PP on the byte-frozen vendor tree; determiner required.
- M3.2 unit gates + differential (scratch-local, gitignored; port scheduled with M3.7 close alongside M3.1's): `.scratch/m3u2/gate_m3u2.py` = stage-pin (tracked↔staged lexicon byte-compare) + white-box NAF safety probes (`ace_to_pl:v1_naf_safe_clause` — the escape/binding scans are ACE-surface-unreachable, Q3) + white-box curry guard probes (`ace_to_pl:v1_curry_step/3` isolated nonempty-domain/sibling blocks + singleton positive — surface negatives couple both guards, rev2 F01) + 7 deep greens (F2 edge order/box/Deps, S24-06 Horn split, NAF render, P6 strength, for-PP) + 5 detail-pinned reds + aggregate co-load (cross-doc modal chain + corpus-global NAF differential) + the 73-case verdict suite. Suite = `.scratch/m3u2/suite/` (test-m3u2 phase-2 encoding of MAIN batch rulings, one dir per case ace/ulex/expect + runner.py, env `M3U2_TREE`/`M3U2_STAGE`; AD02 MAIN-corrected to position-over-content). Oracle = `.scratch/m3u2/oracle/` (orc-m3u2 contract-only reference: projector.pl + run.sh taking `APE_TREE` + selftest.pl + build_results.py with `M3U2_ROOT`). Differential = `.scratch/m3u2/diff_m3u2.py` (bare invocation green from the primary root; re-derives BOTH sides live against the shared stage; canonical variable-renamed clause multisets; main-side docid lowercased + docid arg neutralized — the tree enforces README `[a-z0-9-]+` as `usage,docid` rc=2; RULED_DIVERGENCES = bounded waivers per rev F-04: reject-detail entries pin main detail byte-exact; context-shape entries need pin + full multiset equality after the EXACT ruled P14 transformation of the oracle rows (`p14_transform` drops box-internal referents from Deps lists, derived from each row's own operator-edge structure) + re-lettering, so any second defect either side, Deps drifts included, fails UNRULED; the 4 standing entries = oracle pre-P14 deltas). All need the staged APE at `.scratch/m3scout/ape-stage` (restage recipe = M3.1 note).
- v1 schema selection is an explicit compiler input (`schema=v1` as the exact trailing argv token), never inferred from document content; `tools/goal.py` red-probe runner appends it for `tests/red/*--v1-*` cases alone. A ulex file literally named `schema=v1` would misroute — reserved token, unsupported filename. `proof` (v1-only, after the selector) is the same kind of reserved token: in the ulex slot it rejects as `usage,ulex_arg(proof)` rc=2.
- M3.3 unit gate + differential (scratch-local, gitignored; port scheduled with M3.7 close alongside M3.1/M3.2's): `.scratch/m3u3/gate_m3u3.py` = driver (exports `M3U3_TREE`/`M3U3_STAGE` + prepends `.toolchain/bin`) over `.scratch/m3u3/suite/` — 67 cases, one dir per case ace/ulex/expect + runner.py + payload_check.pl, test-m3u3's phase-2 encoding of MAIN batch rulings (group IR, witness slots, payload bytes, derivation rejects, argv, aggregate, migration validator, collision scans, frozen layout). Differential `.scratch/m3u3/diff_m3u3.py` drives the tracked compiler over orc-m3u3's own roster (`.scratch/m3u3/oracle/build_results.py` `cases()`) and compares product sha256 + payload sha256 + rejection bytes against its `results-digest.tsv`: 88 cases, 0 divergences, no waivers. Suite reds MAIN corrected at harvest: AG03/AG04/AG09-dup rc 2→1; AV03/AV04 usage detail relaxed to the `ace_to_pl_error(usage,` prefix; variant-order check rewritten per sentence 1..k (it had read multi-sentence payloads as variants); GP05/PB02 counts ([4],[7])→([8],[3]), currying pulling universals into the body; MV05 mutator renames BOTH inflections. Both need the staged APE at `.scratch/m3scout/ape-stage` (restage recipe = M3.1 note). Decisive chain, in order: `goal.py check` → `regen.py --check` → gate_m3u1 → gate_m3u2 → gate_m3u3 → diff_m3u3 → fresh `goal.py compile cdc-2022-opioid` → `git diff --quiet -- guidelines/`.
- M3.4 production A regeneration paths (scratch-local, gitignored): `.scratch/m3u4/{probe_candidates.sh,validate_doc.py,consolidate_lexicon.py,splice_notes.py}` + `.scratch/m3u4/harvest/` (ulex-p1/, ulex-p2/, notes-rows-p1.tsv, notes-rows-p2.tsv, MAIN-fixed imp04 ace+ulex) = the migration's durable replay input set: producer ACE → `guidelines/cdc-2022-opioid/ace/`, `consolidate_lexicon.py <guideline-dir> <ulex-dir...>` rebuilds lexicon.ulex deterministically (rerun prints `file=unchanged`), `splice_notes.py <tsv...>` replaces projection-notes rows by docid, `tools/goal.emm` `v1_docids` grows docid-sorted + `regen.py --regenerate`, `validate_doc.py <docid>` (env `M3U4_STAGE`) = per-doc structural validator. gate_m3u1's legacy-default byte check reads the pinned pre-migration snapshot `.scratch/m3u1/gate-fixtures/legacy-corpus/` — migration moves docs out of the legacy path; the snapshot keeps the legacy compiler gateable.
- M3.5 production B regeneration paths (scratch-local, gitignored): contract `.scratch/contracts/m3u5.md` (m3u4 P1-P6/R1-R5 + R6 only-restrictor→conditional prohibition, R7 might→may, R8 meta-negation); probe `.scratch/m3u5/probe_candidates.sh` → probe.tsv (31/31 green); MAIN fidelity sheet `.scratch/m3u5/MAIN-fidelity-sheet.md`; harvest replay set `.scratch/m3u5/harvest/{ulex-p1,ulex-p2,notes-rows-p1.tsv,notes-rows-p2.tsv}` (imp07 D9 fix applied in harvest ulex-p2 + notes-p2; worktree originals deleted at close). Integration tools = M3.4's (`validate_doc.py`, `consolidate_lexicon.py`, `splice_notes.py`), unchanged.
- Bounded replay (ace_to_pl.pl, both P7 per-doc + P9 aggregate paths): `v1_bounded_head_call` = `call_with_depth_limit` 4000 inside `call_with_inference_limit` 10^6; witness facts `asserta`'d so they precede rule clauses in search order; either bound exceeded = ordinary underivability (DR06 family). Cause at composition scale: classical-negation antecedents render `guideline_operator(actual,C,-)` body goals with free C that resolve against sibling `-` rule heads (own + other docs') before the witness fact → exponential backtracking; 2 docs suffice. Regression = suite AG10 (minimal 2-doc negation pair, 120s timeout harness; pre-fix compiler = rc=124/timeout FAIL, fixed = green). Verdicts unaffected — clause order + bounds shape search only.
- v1 freeze mechanics to re-read before touching them: the aggregate replay asserts witnesses into `user`, so `aggregate_declare_indicators` dynamic-declares every ABI predicate BEFORE the composition loads — loading first makes them static and the assert dies `permission_error(modify,static_procedure)`. The ulex-wide reserved scan reads lemma/data slots only (every ulex category writes the surface form in argument 1, which never reaches the product), so both collision scans report ONE canonical detail per offending entry — M3.1's `collision-verb` ruling (lemma, not surface) is the pin. Lexicon liveness is per LEXEME, never per surface form: the shipped corpus holds infpl-only (`detect-life-threatening-warning`) and finsg-only (`offer-naloxone`) lexemes, so a per-surface rule rejects the corpus it exists to protect.
- M3.6 production C regeneration paths (scratch-local, gitignored): contract `.scratch/contracts/m3u6.md` (m3u4 P1-P6 + R6-applied/R9-R12; P7 terminal); probe `.scratch/m3u6/probe_candidates.sh` -> probe.tsv (30/30 green); source baseline `.scratch/m3u6/source-regions.txt`; MAIN fidelity sheet `.scratch/m3u6/MAIN-fidelity-sheet.md` (30/30 ACCEPT, zero MAIN fixes); harvest replay set `.scratch/m3u6/harvest/{ulex-p1,ulex-p2,notes-rows-p1.tsv,notes-rows-p2.tsv}` (worktree originals deleted at close). Integration tools = M3.4's (`validate_doc.py`, `consolidate_lexicon.py`, `splice_notes.py`), unchanged. Terminal state: migrated=79/79, lexicon 487 entries zero pn_sg/mega, aggregate 79/257 fwd+rev. gate_m3u3 MV05 entry-picker generalized (quoted fixture-era branch preserved, unquoted iv fallback) — at terminal the quoted-mega precondition is vacuous by design; both-inflection rename semantics unchanged. Suite harness git setup pins `gc.auto=0` + `maintenance.auto=false` — detached auto-gc raced TemporaryDirectory teardown (spurious MV06/MV08 `Directory not empty` OSError).
