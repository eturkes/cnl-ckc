# memory

- Charter = `.agent/standing-instructions.md` (user scope directive in standing form; authoritative). Vehicle = Claude Code built-in `/goal` stop-condition loop (re-arms at every stop attempt; survives session resume; canonical argument = disjunctive terminal — (a) user stop request alone meets the goal mid-worklist, (b) compendium exhaustion; `/goal clear` disarms): procedure + canonical argument = `docs/REFERENCE.md` § Operating; queue+status = `.agent/queue.md` (one source doc in progress at a time → full coverage before next fetch); roadmap = project development planning only; round delegation map = `.agent/rounds.md` (MAIN orchestrates + rules + commits; GPT teammates absorb all bulk; compiler/lexicon edits stay MAIN-authored). Keep `.claude/commands/` free of built-in command names (`goal`) — a project command masks the built-in.
- Authority map: simplified overview → root `README.md`; pipeline + audit story → `docs/REFERENCE.md` (holds the pre-split root README's technical content — archive/scratch refs to "README § …" resolve there); guideline provenance/rights/coverage → `guidelines/<id>/README.md`; ACE→Prolog compiler contract → `vendor/ape/prolog/ace_to_pl.pl` header; E-- language → `vendor/e--/docs/spec.md` + strict entrypoint; TCB + fork boundaries → `vendor/*/PROVENANCE`; gates → `.github/workflows/ci.yml` (SWI version+crypto probes, bootstrap cmp, `regen.py --check`, `goal.py check`, cleanliness).
- Toolchain (sole dev workstation): system swipl/python too new → gitignored `.toolchain/`: `swipl-9.2.9/` prefix built from `https://www.swi-prolog.org/download/stable/src/swipl-9.2.9.tar.gz` (`cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DINSTALL_DOCUMENTATION=OFF -DSWIPL_PACKAGES_JAVA=OFF -DSWIPL_PACKAGES_X=OFF -DSWIPL_PACKAGES_ODBC=OFF` + `ninja install`) + `bin/` shims `swipl`→that prefix, `python3`→uv cpython 3.13. Every run: `export PATH="$ROOT/.toolchain/bin:$PATH"` — EVERY Bash call (shell state resets per call); a PATH without the shims leaks system swipl into goal.py (`resolve_swipl`), failing compile as `ace_to_pl_error(uncaught,unwind(halt(0)))` rc=2 (newer SWI catches halt's unwind in the compiler's catch-all) — that shape = toolchain leak, not a corpus defect. Lost ⇒ repeat recipe. CI's digest-pinned swipl:9.2.9 container = reference runtime; python floor = 3.11 (supported invocations use `-P`, a 3.11+ flag; container Debian python3 = 3.11 → hold to the 3.11 stdlib surface; pathlib `follow_symlinks` kwarg = 3.12+).
- Deterministic SWI: always `-f none -F none`; writer options explicit. SWI stream `encoding(utf8)` ≠ strict octet validation → compiler reads bytes + explicit RFC-3629 boundary/overlong/surrogate/max-code-point checks (ace_to_pl does; preserve on edits).
- ace_to_pl editing gotchas: read DRS via `arg/3` + `==` exclusively (unification silently instantiates canonical vars); event-referent erasure counts CONDITION positions only (`drs/2` domain arg = binder, skipped while counting); `numbervars` only after validating no pre-existing `'$VAR'/1`, acyclicity, no attvars; digests = `crypto_data_hash/3` over raw pre-decode bytes incl. final LF with `encoding(octet)`.
- Clex = `vendor/clex/clex_lexicon.pl` (97,527 facts, verbatim upstream pin 20960a5c; `Pristine: yes` PROVENANCE class → fork-notice gate verifies MANIFEST.sha256 digests + zero notices instead of git log; battery incl. 8 pristine fixtures = `.scratch/polish-forknotice/fixtures.py`). `stage_ape` overlays it onto the staged demo `prolog/lexicon/clex_lexicon.pl` → every parse (corpus + tests/red) reads full Clex; vendor/ape's 8-fact demo file = dead at runtime, kept to avoid churning its fork notice. Clex is authoritative for the words it provides: a ulex entry duplicating one byte-for-byte rejects (`lexicon-redundant`, normalization = strip quotes/whitespace/final dot; restoration pruned 178/814 ulex entries — replay `.scratch/clex-restore/prune_ulex.py`). Same normalization pins ulex-internal duplicates (`lexicon-duplicate`). Ulex lookup MERGES with clex per category (lexicon_interface.pl falls through; no suppression) → a ulex entry sharing a surface with any non-byte-identical clex fact rejects (`lexicon-shadow`) unless `guidelines/<id>/audit/lexicon-shadow.tsv` rules that exact ulex-entry × sorted `+`-joined clex-reading-set pair (pinned header `ulex_entry	clex_entries	ruling`; stale/dup/malformed rows reject `lexicon-shadow(-file)`; 13 live rulings — regenerate `.scratch/clex-restore/emit_rulings.py`; re-pinning Clex upstream re-opens them by design). Battery = `.scratch/polish-lexshadow/battery.py` (12 fixtures, worktree pattern), shadow/redundancy enumerator = `dryrun.py` beside it. Any lexicon.ulex byte change rewrites the ulex digest in all 186 `guideline_document/3` records → recompile the whole corpus.
- APE facts: noun copula needs the indefinite article (`X is a noun.`); DRS negation by arity+spelling: `-/1` classical, `~/1` NAF, `-/2` = sentence/token anchor wrapper only; Ulex `pn_sg/3` WordForm is case-sensitive to the ACE surface; unknown content words without ulex → `ape_messages`.
- Strict E-- authoring: the language rejects comments, indexing + postfix chains; bind intermediate call results; `[[x.y]](args)` call marker; retain a loop's final value explicitly.
- Fork edits: a modified upstream file needs a prominent in-file notice — Apache-2.0 §4(b) (`vendor/e--`; notice alone, no date required), GPL-3.0 §5(a) via LGPL (`vendor/ape`; notice + relevant date). Git history = the sole fork change record. `goal.py check` `fork-notice` enforces it (first check to run): per `vendor/<tree>/PROVENANCE` keys `Import commit:` + `License:` + `First-party files:` (tree-relative, comma-separated), modified = tracked ∩ (`git log <import>..HEAD` ∪ `git diff HEAD`) − first-party; a notice = a line holding both `Modified` and `fork` inside the first 40 lines; GPL family additionally needs an ISO `YYYY-MM-DD` in that line; an unmodified file carrying a notice is a violation, as is a zero-modified-file total. Notice date = the file's first fork-edit commit date, write-once; later edits keep it unchanged (any modification date satisfies GPL "relevant date"; git history records every later change — the gate checks date presence only, so drift is rule-enforced here, not gate-enforced). CI checkout runs `fetch-depth: 0` — the gate needs full history. Battery = `.scratch/polish-forknotice/fixtures.py` (12 synthetic single-tree repos driven through the committed `goal.py`; PASS = meter line, FAIL = pinned `goal: fork-notice:` detail).
- Scratch/probe work → gitignored `.scratch/`; standalone git ops there set `GIT_CEILING_DIRECTORIES="$ROOT/.scratch"`.
- WebSearch safety layer false-positives on medical vocabulary → phrase queries by tool/org names only.
- Compendium + harvest technique (M1 record; consumed at compendium edits, queue promotion, fetch/access work) = `.agent/archive/harvest-technique.md`: source-discovery independence law, access classing + probe law, issuer/custody + manifest exactness rules, merge_rows replacement law + mutation tooling, MMWR/HRSA rulings, compendium gate split + deep-gate regeneration, index-harvest routes.
- Presentation rule (user): durable files read as this project's own findings — reference only this project's own paths, commits + findings.
- v1 = the compiler's sole schema; invocation `<ape-tree> <docid> [ulex] [proof]`, nothing inferred from document content. The `schema=v1` selector retired with the pre-v1 legacy emission path — a future schema = version bump + a new explicit selector. `proof` stays a reserved argv token: a bare third token `proof` selects proof mode (a ulex literally named `proof` is unaddressable); the explicit ulex+proof form `<tree> <docid> proof proof` rejects `usage,ulex_arg(proof)` rc=2.
- M3 evidence (scratch-local, gitignored; standing ruling: unit gates/suites/oracles stay scratch unit evidence with regeneration contracts — M1.5 precedent): contracts `.scratch/contracts/m3u1.md`…`m3u7.md`; shared APE stage `.scratch/m3scout/ape-stage` (restage = copy `vendor/ape` there, then `swipl -O -f none -F none -g "working_directory(_, '<stage>/prolog/parser'), [fit_to_plp], halt." -t halt`); unit gates `.scratch/m3u{1,2,3}/gate_m3u*.py`; differentials `diff_m3u{1,2,3,7}.py`; suites `.scratch/m3u{2,3,7}/suite/runner.py`. Unit gates + differentials = era-bound evidence, NOT standing gates: gate_m3u1/2/3 + diff_m3u7 pin the legacy `schema=v1` argv contract dropped at dd5bc53 → red under every current compiler; replay only from their own M3-era commits. Standing decisive chain = `goal.py check` → `regen.py --check` → fresh `goal.py compile cdc-2022-opioid` → `git diff --quiet -- guidelines/`. Committed authority = `goal.py check` + `tests/red/`, where every probe pins class + rc + byte-exact stderr through its required `.expect` sidecar (regenerate with `.scratch/m3rev/gen_expect.py --write`; pins come from the goal-shaped overlaid stage `.scratch/polish-redstage/ape-stage` — full Clex, restage recipe in gen_expect.py docstring, guard refuses a demo-lexicon stage; the m3scout stage keeps the demo lexicon as m3 gate evidence — never overlay it); the m3 suites stay scratch evidence (suite port dropped — feature-complete ruling). Migration replay: `consolidate_lexicon.py` + `splice_notes.py` still run on main, but `.scratch/m3u4/validate_doc.py` is commit-bound — it greps `mega_lemmas_text` out of `tools/goal.py`, removed at M3.7, and fails `harness: mega_lemmas_text not found` → replay M3.4–M3.6 from that unit's own commit. M3 review probes: `.scratch/m3rev/payload_battery.py` (9 aggregate-payload cases), `.scratch/m3rev/leftrec/` (load-order divergence probe + `probe_recursion_mode.py` = 5 synthetic controls for the committed `recursion-check` mode — clean, direct, rename-apart-only, NAF-wrapped, non-leftmost; a COMMITTED fixture is impossible, `check_prolog_inventory` rejects every tracked `.pl` outside `vendor/ape/` + `guidelines/*/pl/`), `.scratch/m3rev/redtests/` (teammate red tests preserved from the deleted review worktrees).
- Bounded replay (ace_to_pl.pl, both P7 per-doc + P9 aggregate paths): `v1_bounded_head_call` = `call_with_depth_limit` 4000 inside `call_with_inference_limit` 10^6; witness facts `asserta`'d so they precede rule clauses in search order; either bound exceeded = ordinary underivability (DR06 family). Cause at composition scale: classical-negation antecedents render `guideline_operator(actual,C,-)` body goals with free C that resolve against sibling `-` rule heads (own + other docs') before the witness fact → exponential backtracking; 2 docs suffice. Regression = suite AG10 (minimal 2-doc negation pair, 120s timeout harness; pre-fix compiler = rc=124/timeout FAIL, fixed = green). Verdicts unaffected — clause order + bounds shape search only.
- Coverage-ledger gate (`check_coverage`, goal.emm): per-guideline coverage.tsv — pinned 3-line generic header (further `#` comment lines allowed before rows only), 5-col rows, status grammar (`ace(<docid>)`/`restates(<id>)`/`uncovered(<class>: <reason>)`/`pending`, classes heading|process|external|aim|descriptive|notice), per-file region closure against each evidence file's `identify the N payloads below` census + bracketed `[<id> | ...]` locator ids (locator-less evidence like box3 closes on count alone), ace↔docid bijection, restates = single-step to a non-restated existing target; meter `goal: coverage ok <id> <n> regions; ace=<a> restates=<r> uncovered=<u> pending=<p>`. Document completion per docs/REFERENCE.md § Operating = check green ∧ pending=0. Unit battery (scratch unit evidence, M1.5 precedent): `.scratch/goalrounds/coverage_battery.py` — 20 fixtures, 1 positive control stopped at swipl-exec via `SWIPL=/nonexistent` (structural stage completes, no compile), 19 negatives pinning byte-exact first-violation lines; self-managed detached worktree copies the PRIMARY tree's goal.py, so uncommitted validator edits are testable; positive-control meter computed from the worktree's own coverage.tsv.
- M4 plan evidence (`.scratch/m4plan/`; planrev report `.scratch/agents/planrev-m4.md`): strict-E-- WSGI spike `ui_spike.emm`/`.py` — compile + serve + form POST + file write green; the spike's `write_text` is NOT atomic (real protocol = M4.4: exclusive temp + flush + fsync + `os.replace` + CAS). E-- UI bindings: `Use <dotted>.` emits `import <dotted>` (submodules reachable); tuple values via `[[tuple]](<a, b>)` (no tuple literal); strict E-- has `Try:`/`Catch <name>:` (M4.1, spec §5.4) — class-name handlers (dotted ok), no binding var, no blanket-except idiom, one-or-more Catch, source-order first-match, `Exit with`/`SystemExit` bypasses `Catch Exception` (SystemExit ∉ Exception) while `Catch SystemExit`/`Catch BaseException` catch it, unmatched propagates; `python3 -P` omits the script dir from `sys.path` ⇒ one `.emm` per entry point. APE question probes = `question_probes.sh` → `.out` (replayable, 9 families): wh → `question(drs(... query(Ref,who|which|what) ...))`; existential yes/no → bare question box; universal yes/no → implication `=>` inside the box (NOT an executable conjunction — rejects in v1 queries); modal wh → `should(drs(...))` nested; negated wh → `-drs`; disjunctive wh → `v(drs,drs)`; unknown content words still gate on ulex.
- v1 freeze mechanics to re-read before touching them: the aggregate replay asserts witnesses into `user`, so `aggregate_declare_indicators` dynamic-declares every ABI predicate BEFORE the composition loads — loading first makes them static and the assert dies `permission_error(modify,static_procedure)`. The ulex-wide reserved scan reads lemma/data slots only (every ulex category writes the surface form in argument 1, which never reaches the product), so both collision scans report ONE canonical detail per offending entry — M3.1's `collision-verb` ruling (lemma, not surface) is the pin. Lexicon liveness is per LEXEME, never per surface form: the shipped corpus holds infpl-only (`detect-life-threatening-warning`) and finsg-only (`offer-naloxone`) lexemes, so a per-surface rule rejects the corpus it exists to protect. Aggregate coverage derives from the LOADED composition's own sentence identities (the `'$guideline_id'(_,DocId,S,_,_)` subterms its clauses carry), never from the payload stream — payloads are evidence, not authority. Per-sentence VARIANT counts are NOT derivable the same way: rule-head `Deps` are variables in the product, so a dropped LAST variant keeps coverage complete and numbering contiguous (open residue; detection needs a schema change — dropped at the feature-complete ruling).
- M4.1 strict-fixture machinery: gate = `check_strict_fixtures` in goal.emm (root+family closed inventory, class→rc map syntax|slot|non-ascii|control-char→1 usage|io→2, red byte-`.expect` + `strict:<class>:` prefix, green byte-`.golden`, inline canonical-detector subprocess check, meter `goal: strict fixtures ok <r> red <g> green`). Scratch validators (durable copies `.scratch/m4u1/battery/`, all take env `M4U1_ROOT` default cwd; ports dropped — feature-complete ruling): behavior `python3 -P .scratch/m4u1/battery/behavior_battery.py` (4/4); gate self-coverage `python3 -P .scratch/m4u1/battery/gatecheck_battery.py` (21 synthetic-worktree cases + 23 detector rows; needs clean HEAD); fixture regeneration identity `python3 -P .scratch/m4u1/battery/build_test_fixtures.py` then `diff -rq` vs `tests/strict` (writes behavior fixtures script-relative); red-first replay `git worktree add --detach <tmp> 9ee3065 && cp -r tests/strict <tmp>/tests/ && M4U1_ROOT=<tmp> python3 -P .scratch/m4u1/battery/redfirst_runner.py` (expect `47/54 target mismatches; 7 pre-existing controls`). Red-first evidence table + 58-row ruled matrix: `.scratch/agents/test-m4u1.md`; fixture-prediction provenance commit e36d52d (test branch; suite copied into tree, generator archived).
- M4.2 adjudication machinery: gates in goal.emm — `check_adjudication` (re-derives `audit/review-manifest.tsv` in memory, byte-compares vs committed → stale/missing manifest = violation + regen hint; validates `audit/adjudication.tsv` against the derived manifest; meter `goal: adjudication <id> approved=<a> rejected=<r> stale=<s> unreviewed=<u>`; absent ledger = green all-unreviewed) + `check_adjudication_fixtures` (tests/adjudication differential; meter `goal: adjudication fixtures ok <r> red <g> green`); CLI `review-manifest <id>` (writer) + `ledger-validate <ledger> <manifest> <label>`. `semantic_clause_sha256` hashes retained clause lines only (drops `%` comments, `:- ` directives + the document record; exactly-one-record check) → pure ulex append stales nothing. Contract + rulings = `.scratch/contracts/m4u2-rulings.md`. Scratch batteries (repo-root cwd; goal.py = module-level dispatch, never import): `.scratch/m4u2/rev/` = review_oracle.py (independent D1-D3 oracle, 186/186) + component_sensitivity.py (per-component column locality; also credits rev2 A05-A07) + derivation/boundary/gatecheck batteries + red_f01-f04 (f03/f04 anchor README beside `__file__` → run a copy w/ path sed-swapped to docs/REFERENCE.md, their pre-split target); `.scratch/m4u2/rev2/` = mutation_campaign.py (30/30 killed) + determinism_sweep.py (10/10) + append_stability (disposable worktree: `M4U2_ROOT=<wt> python3 -P .scratch/m4u2/rev2/append_stability`, state under `<wt>/.scratch/rev2-m4u2/`); `.scratch/m4u2-fixture-harness.py` = independent fixture generator (`write|check`; repo root = parents[1] of its own path → copy under `<root>/.scratch/` to retarget). Full `goal.py check` >10 min + block-buffered → background to a log, read at completion.
- M4.3 reviewer-UI machinery: `tools/ui.emm`→`ui.py` (serve|render|check|request; wsgiref loopback; viewmodel T1-T8; H5 header set + `Allow: GET` on 405; esc_text/url_seg/palette selftests; check = double-render byte compare + page invariants + dangling-href closure). Gates in goal.emm — `check_ui` = live half (subprocess `ui.py check`, first-nonempty-stderr relay, meter `goal: ui ok <g> guidelines <p> pages`) + fixture half (`tests/ui/` 80 red + 11 green (M4.4): required-name enforcement, AM7 four-column `case.tsv` argv/rc/stderr/stdout with `@TREE@`/`@OUT@` + `-`=empty + serve forbidden, `tree-order.txt`/`empty-dirs.txt` sidecar materialization into a temp tree (git holds neither empty dirs nor creation order), golden tree byte-compare, `assertions.tsv` contains/absent hex rows, per-green `ui.py check` rc0; meter `goal: ui fixtures ok <r> red <g> green`). Ledger-validate subprocess pins `cwd=<repo root>` (goal.py module-level require is cwd-bound; polish row). Census regex parity goal.py↔ui.py: `identify the ([0-9]+) payloads below`. AM6 golden regeneration = `python3 -P tools/ui.py render <out> tests/ui/green/<case>/tree` after a contract-conformant change. Contract + rulings = `.scratch/contracts/m4u3.md`. Scratch evidence (gitignored; committed-python policy bars tests/ui/gen): `.scratch/m4u3/gen/` = independent fixture generator `ui_fixture_gen.py` (write|check|inspect; diff-blind authored; owns 704 files, preserves golden/ + gen/) + runner `run_ui_fixtures.py` (env `UI_PY=<abs ui.py>`; fails on zero cases); `.scratch/m4u3/` probe_min.py 5/5, contrast.py 7/7, serve-battery, shots/ (verified picker/guideline/doc/stale/hostile). Mutation kill table + determinism matrix = `.scratch/agents/rev2-m4u3.md` (mutant scoring = `UI_PY=<mutant copy> run_ui_fixtures.py`; M24/M36 = RV14 scratch-kill residuals).
- M4.4 machinery: contract of record `.scratch/contracts/m4u4.md` v1.2. AM6 wholesale regen `.scratch/regen-ui-fixtures.sh` (harness-parity runner splits argv on single spaces PRESERVING empty tokens; once-per-case scratch materialization — POST rows never touch committed trees; `after/` refreshed from final scratch state). Scratch battery `.scratch/m4u4-battery.py` 16 probes (WSGI CL laws, real clock, fs faults — dest guard detail `ledger not a regular file`, model manifest gate precedes write faults — derive arity, crash temp randomness, literal-space CT, harness-invalid trio via same-device `cp -al` clone incl `.git`+`.agent`, serve token lifecycle); B7 shots + rev acceptance copy `rev-m4u4-red.py` in `.scratch/m4u4-evidence/`. The 16-probe battery = ERA-BOUND (pre-bundle-v2: adapted replay passes 4/16); replay from M4.4-era commits.
- AM6 hazard (bitten twice in M4.4): fixture regen rewrites case-authored stream pins to CURRENT impl bytes → a contract-divergent impl regenerates itself green (usage `*` + empty-segment 405 escaped this way; reviewer red tests caught both). After any regen over diff-blind-authored pins, diff the regen churn against the contract before crediting green.
- goal_py_path (ui.emm) resolves goal.py from sys.argv[0] AT REQUEST TIME — correct for every committed invocation shape; embedded import must pin argv first (battery load_ui pattern: exec source with argv=["tools/ui.py"], swallow the usage SystemExit).
- M4.3 latent close-order defect, closed in M4.4: M4.3's green check ran pre-`git add` and `git ls-files` reads the index → committed HEAD was red on fixture pl/ inventory. Fix shipped in check_prolog_inventory (authorizes tests/ui/*/*/tree/guidelines/*/pl/*.pl). Close order = stage THEN decisive check.
- M4.5 question mode: contract of record `.scratch/contracts/m4u5.md` (R0-R10 + R11 rulings 1-17 + notation amendment); invocation/payload/reject vocab = docs/REFERENCE.md `### Question projection` + ace_to_pl.pl header. Laws easily lost: query sentence law (R11.2) = two-level with the post-parse count in a query-only `accept_or_reject` clause BEFORE the shared empty-DRS guard (lone whitespace line → `query_sentences(0)`, document mode keeps `empty_drs`); every question/modal box passes `query_box_check/1` (drs/2 + proper-list dom/conds → else `invalid_drs_shape`; non-drs question payload stays `leaf(question/1)`); strip/scan/markers all classify the `query_cond_inner/2` anchor-stripped term — anchored modal = parser-unreachable (real APE emits bare wrappers) and compiles byte-equal to bare; Stage A form = condition-order walker (conds + drs/2 condition args only, payload args never read); emitted reject bytes = canonical ignore_ops (`leaf(/(relation,3))`). Scratch evidence (durable copies under `.scratch/m4u5/`, worktrees deleted at close; regeneration contracts, M1.5 precedent; all run from repo root, toolchain PATH): suite = `python3 -P .scratch/m4u5/suite/runner.py target --compiler <abs ace_to_pl.pl> --tree <abs .scratch/polish-redstage/ape-stage> --helper-map <abs>/.scratch/m4u5/suite/helper_map.json` ⇒ `target ok cases=165 passed=165` (~3.5 min; helper goals = MAIN-authored templates over `ace_to_pl:` internals, runner substitutes `{input}`/`{expected}`, byte-compares stdout); direct helpers = `swipl -q -f none -F none -s .scratch/m4u5/direct_helpers.pl -g dh_main -t 'halt(9)'` 17/17 (parser-unreachable defenses; NO argv seam); reviewer batteries (cwd-anchored: assets resolve `<cwd>/.scratch/m4u5/`) = `M4U5_ROOT=$PWD python3 -P .scratch/m4u5/red_phase2` 6/6 + `red_readme_phase2` 2/2; oracle differential = `sh .scratch/m4u5/diffprobes.sh N` rows 1-15 (row 13 = ruled whose divergence vs recorded oracle hash); question `.expect` pins regenerate via `.scratch/m3rev/gen_expect.py --write` (redstage guard); rev2 campaign (126-case corpus + 51-mutant matrix + 800-cell determinism) = `PYTHONDONTWRITEBYTECODE=1 python3 -P .scratch/m4u5/rev2/phase2_replay.py --compiler <abs ace_to_pl.pl> --stage <abs redstage> --root <abs repo root> --state /tmp/<fresh> [--case <id>]` ⇒ `replay 126/126 passed`; Stage-A defense probes `.scratch/m4u5/f5probe.pl`, anchored-modal equivalence `.scratch/m4u5/f3probe.pl` (both `swipl … -s <f> -g probe_main -t 'halt(9)'`). `-g main` name is argv-special in SWI → scratch probe entrypoints use another name.
- M4.6 query-answer machinery (suite/differential = ERA-BOUND scratch evidence: M4.7's writer extension reds suite case G001, the differential now measures 11/15 — 4 edge manifests name the deleted `wt/orc-m4u6` worktree and the harness exits 0 despite fails; runner.py:96 + depth.tsv share the disposition; replay from M4.6-era commits): contract of record `.scratch/contracts/m4u6.md` (v1.2 + v1.3 Verdicts + v1.3.1/v1.3.2 amendments = the law). Invocation/payload/reject vocab = docs/REFERENCE.md `### Query answers` + ace_to_pl.pl header; answer mode `-- answer <answer-manifest.tsv> <query-pl>` rc0 = 2 stdout lines, rc1 = proof class, rc2 = check_load/usage/uncaught. Laws easily lost: census physical EOF ⟺ `end_of_file` read ∧ equal `byte_count` (zero-width read; literal `end_of_file.` = data; byte_count works on open_string streams, char_count off-by-one); record shape requires wrapper functors `ace_sha256(_)`/`ulex(_)` pre-nonground — payload defects reach `record_ace_sha256`/`record_ulex` once ground; `set_stream(S, file_name(File))` after open_string → verbatim deterministic syntax-error terms; solve-time runtime exceptions = `uncaught` rc2 one line via main's catch; sol/1 payload = Prolog list in answer-manifest order incl. singleton; T21 bounded solve = inner-inference exhaustion cumulative across backtracking (depth limit alone backtracks past an exceeded branch to a shallow proof ⇒ integer result ⇒ yes); T30 canonical writer emits graphic atoms bare (`goal_foreign(;,2)`) — writer bytes = sole expected-byte authority. Scratch evidence (regeneration contracts, M1.5 precedent; repo root cwd, toolchain PATH): suite `python3 -P .scratch/m4u6/suite/runner.py target --compiler <abs ace_to_pl.pl> --stage <abs .scratch/m4u6/sigstage/ape-stage> --root <abs repo root>` ⇒ `target ok cases=97 passed=97` (sigstage = redstage recipe: vendor/ape copy + Clex overlay + fit_to_plp precompile); committed `tests/queries/` (12 red + 4 green) regenerates via `python3 -P .scratch/m4u6/suite/generate_fixtures.py --compiler <abs compiler> --stage <abs stage> --worktree <abs root>` (idempotent, digest-pinned; the malformed-query-file case carries one inert `TBD-impl` answers file = inventory filler, freshness rejects first); oracle differential `sh .scratch/m4u6/differential.sh` 15/15 byte-identical (independent oracle `.scratch/m4u6/orc/answer_ref.pl`, entrypoint `-g ref_main`, selftest `selftest_v13.py`); rev2 G08/G09 seam kills = scratch-only evidence (mechanics = report F23/F24; port dropped — feature-complete ruling). outer-W composition rejection owns no committed case — suite T17 + differential row cover it.
- M4.7 proof traces: contract of record `.scratch/contracts/m4u7.md` (v1..v1.3 + rulings v1.3.1-v1.3.8 = the law). Invocation/payload/reject vocab = docs/REFERENCE.md `### Proof traces` + ace_to_pl.pl header; trace mode `-- trace <answer-manifest.tsv> <query-pl> <answers-pl>` rc0 = trace artifact stdout, rc2 = one stderr line `ace_to_pl_error(check_load,answers_file(<why>)).`. Laws easily lost: answers_file why ORDER = term_count → record_shape → record_nonground → record_version → qid_mismatch → record_query_sha256 → query_sha256_mismatch → result_shape → result_mode_mismatch → solutions_list → solution_shape(I) → solution_values(I) → solution_arity(I,E,A); `@/2` cyclic-notation record = record_shape (custody reader never reconstructs cycles, v1.3.8); clause_sha256 = sha256(canonical clause-line re-render + LF) through the document writer from the PRISTINE decompiled clause — emitter-definitional, hand-spelled fixture lines re-render before hashing (v1.3.1/v1.3.8), foreign spellings then fail the validator digest join; NAF digest body: singleton `\+ G` bare, conjunction `\+ (G1, G2)` (v1.3.1); goal.py trace scanner token classes = ASCII-definitional (trace_ascii_digit at 10 digit gates; unquoted atoms `[a-z][a-zA-Z0-9_]*`; non-ASCII in gated position → scan None → parse violation; Python isdigit/isalpha are Unicode-wide and crash int() on superscripts — v1.3.7); `% S<n>:` block marker head = ASCII digits no leading zero else comment (v1.3.6); proof roots = clause nodes only, `proved([])` invalid (v1.3.3/v1.3.4); D26 single writer-guard detail `trace_unserializable`; trace-reject fixture lane = SINGULAR `trace-reject/`, paired `<qid>.answers`+`<qid>.expect`, qid = filename stem, partner line `trace-reject member without partner: <case>/<qid>.<ext>`. Scratch evidence (repo-root cwd, toolchain PATH): suite `python3 .scratch/m4u7/suite/runner.py target` (plain python3, NOT -P — sibling imports; modes list|self-check|baseline|target, --case/--root/--compiler/--stage) ⇒ `target ok cases=80 checks=332`; committed `tests/queries/` (24 red + 9 green, floors pinned in goal.emm) regenerates via `python3 .scratch/m4u7/suite/generate_fixtures.py` (2× tree-hash idempotent; product comment cites this bullet); byte archives = tags `archive/m4u7-suite` (test HEAD e8d9139) / `archive/m4u7-diff` (39-case MAIN↔orc harness, c756a17) / `archive/m4u7-orc` (independent oracle, cb9f811); worktrees + wt/ branches deleted at close. Suite stays scratch-local (regen law: all first-party tracked .py = E---generated; a tracked-Python suite reds `regen --check`; port dropped — feature-complete ruling). rev2 campaign: 50 mutants, 41 killed, survivors ruled contract v1.3.9 (M03/M06/M07/M36 equivalent-accepted; M41/M42/M48-50 = real scratch-suite gaps, kill recipes in the report; port dropped); determinism 64/64 byte-stable rc0 across cwd/locale/manifest/ambient-op + writer × PYTHONHASHSEED extremes; LC_ALL=C adds SWI UTF-8 source-load stderr warnings with identical bytes → empty-stderr assertions need a UTF-8 locale.
- M4.13 committed-corpus law (contract v5.5): the reviewer surface reads HEAD, never the working tree. `resolve_corpus` (ui.emm) = dirt probe (`git diff --name-only -z HEAD -- guidelines` ∪ `git ls-files --others --exclude-standard -z -- guidelines`, minus `*/audit/adjudication.tsv` — the server writes that file, so its writes must never count as dirt) → filesystem mode when git is unusable ∨ root ≠ `git rev-parse --show-toplevel` ∨ dirt empty ∨ `git archive` fails; else `git archive --format=tar HEAD guidelines` → temp tree, member-by-member extract (absolute/`..`/non-regular reject), live ledgers overlaid, released after each command. Model carries BOTH paths: `path` = corpus (artifact reads, asset serving, POST `derive-review-manifest`), `real_path` = working tree (ledger CAS + write, `ace_commit_hex`); the `committed_corpus` flag rides beside them. `ace_commit_hex(path, docid, committed)` keeps the dirty gate in filesystem mode alone; under a snapshot it reads `git log -1 --format=%H HEAD -- <ace>` ⇒ every UI-written decision names a commit ⇒ every records-page Version cell links. `check` is the one command pinned to the working tree (`worktree_corpus`) — reading HEAD would blind the pre-commit gate to the round being committed. Consequences: an uncommitted edit neither shows nor outdates a decision; a never-committed document is unlisted until committed. The law is INVISIBLE by user ruling (v5.6): no notice, no state column, nothing names committed-versus-uncommitted — so only fixtures can carry it. Committed evidence = the two `worktree/` green cases below (cases b/c/d/f); `.scratch/m4u13-committed-corpus.py` 9/9 keeps the residue a/e/h/i (clean-git-equals-plain-tree, ledger-write-is-not-dirt, below-toplevel corpus, snapshot released) and remains the sole carrier for them (committed-twin port dropped — feature-complete ruling). Its case e is white-box (`load_ui()` execs ui.py with argv pinned, then calls `uncommitted_guidelines`) because the dirt-exclusion of `*/audit/adjudication.tsv` lost every external observable when the notice went. Fixture trees without `worktree/` stay in filesystem mode (below repo root / in /tmp) ⇒ zero golden churn. Targeted gate runner (full `goal.py check` = ~20 min): `python3 -P .scratch/run-check-ui.py [<gate-fn> ...]` (default `check_ui`, ~23 s; execs goal.py with argv pinned, swallows the usage SystemExit, then calls the named gate functions; gate names are the goal.py identifiers — `check_copy_register`, `check_copy_chain_slot`, `check_adjudication_fixtures`, `check_prolog_inventory`).
- M4.13 bundle v2 + git-corpus fixtures (contract v5.6): `review_sha256` = sha256 of `bundle v2 <docid>` + ace + coverage + payload + clauses; the projection-notes component left with the `Differences from the source` panel it fed, so the manifest is 6 fields and `check_projection_ledger` keeps the notes artifact under a structural gate that no page reads. Migration = `python3 -P .scratch/m4u13-bundle-v2.py` (idempotent from a clean checkout). Lesson: four POST red cases had planted their subject drift IN the notes digest; v2 erased that carrier and they silently flipped 409→303 — when a digest component is dropped, census every fixture that depends on the digest DIFFERING, not just on its value. Git-corpus fixture capability = a `worktree/` sidecar beside `tree/`: `ui_materialize_tree` triggers on it, `ui_commit_worktree` commits `tree/` as HEAD then lays `worktree/` on top uncommitted, `ui_git_env` pins identity + dates + `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` so a fixture commit hash is a function of the fixture bytes (verified byte-stable across rebuilds), `.scratch/regen-ui-fixtures.sh` mirrors the same pins in `git_fixture`, and `fixture_pl_path` accepts `worktree` beside `tree`. Goldens still render from the committed `tree/`, so golden equality alone proves the render ignored the working tree. Builder = `python3 -P .scratch/m4u13-git-fixtures.py` → `tests/ui/green/git-uncommitted-edit` (dirty ACE + regenerated manifest + committed approval + a POST row; `after/` pins the dirt so the case cannot pass vacuously) and `tests/ui/green/git-untracked-document` (complete never-committed document; `render.stdout.expect` `docs=3` vs `check.stdout.expect` `docs=4` states the split in two pinned meters). Non-vacuity probe = render each materialized case with and without `.git`: without git the first diverges on 4 pages and the second emits an extra `doc/n-1.html`, and the POST answers 409 with no ledger row instead of 303 with a commit.
- Doc-page source↔ACE highlight (user-directed; identity = AUTHORED ALIGNMENT — lexical word/phrase matching retired by user ruling; alignment written at ACE author time, helpful-over-exhaustive): artifact = `guidelines/<gid>/align/<docid>.tsv`, rows `group<TAB>side(src|ace)<TAB>start<TAB>span` — canonical decimals (str(int(x)) must round-trip), start = 0-based char offset into ui payload_line (src) / raw ace_text (ace), span verbatim nonempty no tab/newline. ui.emm machinery: `hl_parse_align` (validation order: trailing newline → empty file → per-row 4 fields → group int → start int → empty span → side vocab → range → byte-match at offset; then groups both-sided → per-side disjoint; err strings = selftest pins) + `hl_marked_html` (cursor walk; gaps esc, ace gaps kw-pass) + `hl_kw_html` (whole-token stopword → span.kw) + `hl_slice` = `text.__getitem__(slice(a,b))` (E-- lacks slice syntax; dunder-call idiom). Load = doc_render_data: missing file → align None → plain render, no legend; invalid → render err `ui: viewmodel: <gid> doc <docid> align: <detail>` ⇒ check_ui corpus render = the structural gate. COMMITTED-CORPUS LAW applies: align/ renders from HEAD staging — uncommitted align files are invisible to serve/render (write → commit → visual QA). Display index = groups ordered by first ace-side offset, dense t0..; hue = index mod 12; hover (`main:has(mark.tN:hover)`) = exact identity channel; click = second emphasis level via the UI's SOLE script (`build_hl_script` canonical inline JS, user-directed exception to the zero-JS law): click on a mark → its group's marks get `hl-pick` + main gets `hl-click`, CSS fades all other `pre.prose` content to palette `faded` #9ca3af while picked marks keep full contrast; second click clears (back to hover mode), mouseout of the group clears (JS `mouseout` w/ relatedTarget same-group guard), toggle uncheck clears; print block resets fade colors (identical selectors after the screen rules — later wins). Script law: page_invariant_name requires count(`<script`)==count(hl_script_html) and ≤1 (byte-pinned canonical block; foreign/doubled script = `script` violation, selftest cases); CSP = `script-src 'sha256-<b64(sha256(exact element body incl. wrapping \n))>'`; visible_text strips `<script>`..`</script>`; ui.py literals ride the copy register → JS/CSS strings must avoid the banned css tokens (`transition`/`animation`/`box-shadow`…) + alnum-then-`!` (exclamatory) ⇒ spell ` !== ` spaced, `=== false` over `!x`; 48 hover classes; print strip + palette-hex selftests retained (palette selftest counts EVERY `#` in css_text against palette hexes ⇒ selectors stay id-free: `input.hl-toggle` class, not `#id`). Authoring = `goal.py align <gid> <docid>` stdin rows `group<TAB>side<TAB>occurrence<TAB>span` (occ = 1-based raw-substring count, non-overlapping left-to-right; substring counting includes hits inside longer words — ace `opioid-therapy` occ1 sits inside `nonopioid-therapy`, standalone = occ2) → resolves offsets against check_coverage payload (digest custody ⇒ byte-equal to ui payload) + validates both-sidedness/overlap + writes canonical artifact (ace rows then src, start-sorted); goal.emm `align_command`/`align_nth_start`/`align_int_field`. Rubric = REFERENCE Operating step 4 Align: link each ACE domain term to the passage text it came from; smallest span a reviewer would point to; sub-spans of hyphenated terms fine (`category-B` inside `category-B-recommendation`); word-form pairs authored, not derived (`assess`↔`assessment`); leave uncorresponding text plain; any ACE/passage edit → re-author (render stops on stale offsets). Legend `.hl-note` = one sentence “Try hovering and clicking on highlighted terms for different levels of emphasis.” behind a `Highlighting` checkbox (`input.hl-toggle`, default checked = highlighting on), renders when count>0 (script rides the same guard); toggle-off CSS `body:has(input.hl-toggle:not(:checked))` neutralizes mark + kw styling (id-weight-free, beats hover by order+specificity). Behavior law = selftest_violation hl cases (slice, int-field canonicals, 3-group render, compound sub-span striping `[category-B]-[recommendation]` ↔ `category: B`, offset-disambiguated repeated span, missing-align plain+kw, 11 parse-error string pins) + green fixture tests/ui/green/highlight (h-1 multi-group + multi-word src span `nonopioid therapies` as ONE mark + unmarked tail sentence; h-2 per-side-verbatim diacritic `opioid-naïve patients`↔`opioid-naive-patient` + Brisk unmarked; h-3 NO align file → no marks/legend; h-4 compound sub-span striping + one group with 2 src spans; h-5 unaligned compound tail `-dose` plain; 26 hex assertion rows incl. toggle+legend line, script prefix, h-3 absent-script) + red align-span-mismatch/align-overlap/align-one-sided (check rc1 stderr pins). Fixture pins 84 red 15 green (goal.emm red_required + counts). Copy register recomputes (1957 after click-emphasis tier). verdicts assertions: `absent checked` retired (`:checked` now in every page's CSS) → exact unchecked-radio contains-pins. Committed exemplars: rec01 = 17 groups 45 spans (category-B/evidence-type-3 sub-span groups; g7 src occ1+occ3 while `prescribing opioid therapy` covers occ2 — overlap partitioning pattern), rec08-imp13 = 13 groups 31 spans (fall-risk ↔ `risk`+`falls` 2-src-span group). Wave law: every ACE doc needs an align file (REFERENCE completeness bullet); inventory gate = goal.emm `check_alignment` beside `check_pl_inventory` in check_command's per-guideline loop (align/*.tsv set-equal to ace docids, regular files only, violations `align-dir`/`missing-align`/`align-entry`/`align-inventory`). Corpus census: 337 files, 2731 groups, 8556 spans; groups/doc median 7, p90 13, max 26, min 2; content gate = check_ui corpus render (hl_parse_align). align/ flows into dist automatically (guidelines/** derivation) = shipped provenance; release-manifest stales per normal guidelines/ rule. AM6 corollary: any mark/CSS change → regen ALL fixture expects+goldens then sweep every green assertions.tsv (`.scratch/audit-ui-assertions.py`; in-process check runner = `python3 -P .scratch/run-check-ui.py check_ui check_copy_register check_prolog_inventory`); red request/POST stdout goldens embed page CSS → `python3 -P .scratch/regen-ui-fixtures.py red` rebakes them (review diff: CSS + intended mark lines only).
- KB export (M4 review dist track): `tools/dist.emm`→`dist.py` (standalone derivation + build CLI) + goal.emm `check_dist` (after `check_copy_register`; loads dist.py as the sys.argv[0] sibling via exec of the source prefix before the `\nargv = list(sys.argv)` marker) + `goal.py release-manifest` writer. ALL derivation reads HEAD (git show/ls-tree), never the working tree; input-head = last commit touching `guidelines/` + `vendor/ape/prolog/ace_to_pl.pl` + `vendor/clex/clex_lexicon.pl`; archive name + bag root = `cnl-ckc-kb-g<head12>`; `release-manifest.tsv` stales on commits touching guidelines/, the vendored compiler/lexicon, `NOTICE`, or the docs/REFERENCE.md schema section (README-dist embeds it; regen recipe in docs/REFERENCE.md Operating §Close). check_dist meter `goal: dist ok <g> guidelines <m> members <b> bytes` | `goal: dist blocked rejected=<r> contested=<c>` (blocked skips the live build only; probes always run). Red suite = `tests/dist/red.sh` (62 cases, ~80s; d07 cases clone HEAD ⇒ run post-commit); per-guideline `rights.tsv` header `profile	statement	url	retrieved	note`, profiles redistributable|reconstructable|restricted, row 1 = operative.
- M4.13 review-UI design-law close: contract of record `.scratch/contracts/m4u13.md` (v1..v4; v4 = fix-wave rulings). Copy law lives in TWO gates — L1 `check_copy_register` (goal.emm; stdlib `ast` literal walk over generated `tools/ui.py`; five registers; scan-target marker `def page_invariant_name(` forbids retargeting) + L2 `page_invariant_name` (ui.emm; runs inside the existing `ui.py check` per-page sweep; per page: scope-line singleton -> copy-sha256 -> copy-bundle-differs -> copy-hex, where a hex run reds only when it contains a digit). Visible-text extraction = strip `<style>`/`<pre>`/comments, drop tags, SPACE-join, then `html.unescape` (browser equivalence; split-across-tags stays green by design; all attributes exempt). `selftest_violation` carries 13 page-invariant negatives beside the escaping/palette rows and runs BEFORE rendering, so L2 mutants die with zero fixture/golden churn. `check_copy_chain_slot` (first statement of `check_ui`) AST-pins `check_copy_register()` as the statement directly after `check_ui()` in `check_command`. Print law: `@media print` hides nav/skip/form/`.verdict-entry` and forces disclosure content open via `details::details-content { content-visibility: visible }` (Chromium 131+; the older `details > *:not(summary)` form does NOT work). Scratch evidence (repo-root cwd, toolchain PATH): `bash .scratch/m4u13-capture-qa.sh` = idempotent clinician-QA rebuild (14 screens + 3 print sheets + README index under `.scratch/m4u13-qa/`, state pages rendered from a throwaway demo ledger over the live corpus, response bodies replayed from committed request argv); `python3 -P .scratch/m4u13-survivor-kills.py` = 12/12 adopted-survivor kill evidence (ui mutants through the in-namespace selftest, goal mutants through a temp root that needs a `vendor/ape/prolog/ace_to_pl.pl` stub for goal.py's import-time require); `python3 -P .scratch/regen-ui-fixtures.py [green|red...]` = wholesale expect+golden regen via goal.py's own materializer, `fails=0` required after any CSS/DOM change; `.scratch/regen-ui-goldens.py` = per-case golden regen (harness-mirror argv/env incl. `ui_git_env` sha1 pins; used for the M4-review fixture additions; port dropped — feature-complete ruling); diff-blind suite `python3 -P .scratch/m4u13-evidence/suite/runner.py target --root <repo>` = 48/48 at claim time (ERA-BOUND: now 53 cases → 50/53; case 33 renders the removed differences panel, 49+52 index the retired col7); teammate batteries preserved at `.scratch/m4u13-evidence/{rev,rev2}/` (rev print/entity probes need `webcap` + `pdftotext`).
- M5 Rust rewrite OPEN: plan of record = `.agent/archive/rust-rewrite-plan.md` (read at unit start); split law = `.agent/standing-instructions.md`. While open: legacy `regen.py --check` + `goal.py check` stay CI-authoritative and green at every commit; `rust/` rides beside; each unit proves parity by dual-run differential before any legacy deletion; corpus + committed query artifacts byte-stable except plan-ruled re-pins. First Rust touch: serena `language_servers` += rust; `.gitignore` + settings `Read()` deny += `rust/target/`. Tooling survey evidence = `.scratch/agents/rverif-1.md` (scratch; M5.1 re-verifies its claims live at pinned-toolchain install).
