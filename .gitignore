/.claude/settings.local.json
/.claude/.headroom_wrap_marker.json
/.scratch/
/.toolchain/
/dist/
.goal.tmp.*/
__pycache__/
rust/target/
# vendored-crate ballast whose extensions red legacy repo-wide inventory
# gates (regen unauthorized-python, prolog inventory); cargo builds green
# without them (cold-verified) — they never compile on x86_64-linux
rust/vendor/**/*.py
rust/vendor/**/*.pl
rust/vendor/**/*.emm
