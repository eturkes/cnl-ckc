# Modified from upstream e-- in the cnl-ckc fork; see PROVENANCE.
"""E-- (English--) package.

This fork ships only the strict pipeline: run
``python3 -P -m e_minus_minus.strict`` and audit the modules it imports.
The upstream lazy transpiler API and CLI are removed.
"""
